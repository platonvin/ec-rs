#![feature(prelude_import)]
#![feature(macro_metavar_expr)]
#![feature(macro_metavar_expr_concat)]
#![feature(decl_macro)]
#[macro_use]
extern crate std;
#[prelude_import]
use std::prelude::rust_2024::*;
use ecs::*;
pub struct Position {
    pub x: f32,
    pub y: f32,
}
#[automatically_derived]
impl ::core::default::Default for Position {
    #[inline]
    fn default() -> Position {
        Position {
            x: ::core::default::Default::default(),
            y: ::core::default::Default::default(),
        }
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for Position {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::debug_struct_field2_finish(
            f,
            "Position",
            "x",
            &self.x,
            "y",
            &&self.y,
        )
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for Position {}
#[automatically_derived]
impl ::core::clone::Clone for Position {
    #[inline]
    fn clone(&self) -> Position {
        let _: ::core::clone::AssertParamIsClone<f32>;
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for Position {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for Position {}
#[automatically_derived]
impl ::core::cmp::PartialEq for Position {
    #[inline]
    fn eq(&self, other: &Position) -> bool {
        self.x == other.x && self.y == other.y
    }
}
pub struct Velocity {
    pub x: f32,
    pub y: f32,
}
#[automatically_derived]
impl ::core::default::Default for Velocity {
    #[inline]
    fn default() -> Velocity {
        Velocity {
            x: ::core::default::Default::default(),
            y: ::core::default::Default::default(),
        }
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for Velocity {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::debug_struct_field2_finish(
            f,
            "Velocity",
            "x",
            &self.x,
            "y",
            &&self.y,
        )
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for Velocity {}
#[automatically_derived]
impl ::core::clone::Clone for Velocity {
    #[inline]
    fn clone(&self) -> Velocity {
        let _: ::core::clone::AssertParamIsClone<f32>;
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for Velocity {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for Velocity {}
#[automatically_derived]
impl ::core::cmp::PartialEq for Velocity {
    #[inline]
    fn eq(&self, other: &Velocity) -> bool {
        self.x == other.x && self.y == other.y
    }
}
pub struct Health {
    pub val: i32,
}
#[automatically_derived]
impl ::core::default::Default for Health {
    #[inline]
    fn default() -> Health {
        Health {
            val: ::core::default::Default::default(),
        }
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for Health {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::debug_struct_field1_finish(
            f,
            "Health",
            "val",
            &&self.val,
        )
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for Health {}
#[automatically_derived]
impl ::core::clone::Clone for Health {
    #[inline]
    fn clone(&self) -> Health {
        let _: ::core::clone::AssertParamIsClone<i32>;
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for Health {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for Health {}
#[automatically_derived]
impl ::core::cmp::PartialEq for Health {
    #[inline]
    fn eq(&self, other: &Health) -> bool {
        self.val == other.val
    }
}
pub struct ScStone;
#[automatically_derived]
impl ::core::default::Default for ScStone {
    #[inline]
    fn default() -> ScStone {
        ScStone {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScStone {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScStone")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScStone {}
#[automatically_derived]
impl ::core::clone::Clone for ScStone {
    #[inline]
    fn clone(&self) -> ScStone {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScStone {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScStone {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScStone {
    #[inline]
    fn eq(&self, other: &ScStone) -> bool {
        true
    }
}
pub struct ScFreeze;
#[automatically_derived]
impl ::core::default::Default for ScFreeze {
    #[inline]
    fn default() -> ScFreeze {
        ScFreeze {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFreeze {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFreeze")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFreeze {}
#[automatically_derived]
impl ::core::clone::Clone for ScFreeze {
    #[inline]
    fn clone(&self) -> ScFreeze {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFreeze {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFreeze {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFreeze {
    #[inline]
    fn eq(&self, other: &ScFreeze) -> bool {
        true
    }
}
pub struct ScStun;
#[automatically_derived]
impl ::core::default::Default for ScStun {
    #[inline]
    fn default() -> ScStun {
        ScStun {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScStun {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScStun")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScStun {}
#[automatically_derived]
impl ::core::clone::Clone for ScStun {
    #[inline]
    fn clone(&self) -> ScStun {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScStun {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScStun {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScStun {
    #[inline]
    fn eq(&self, other: &ScStun) -> bool {
        true
    }
}
pub struct ScSleep;
#[automatically_derived]
impl ::core::default::Default for ScSleep {
    #[inline]
    fn default() -> ScSleep {
        ScSleep {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSleep {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSleep")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSleep {}
#[automatically_derived]
impl ::core::clone::Clone for ScSleep {
    #[inline]
    fn clone(&self) -> ScSleep {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSleep {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSleep {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSleep {
    #[inline]
    fn eq(&self, other: &ScSleep) -> bool {
        true
    }
}
pub struct ScPoison;
#[automatically_derived]
impl ::core::default::Default for ScPoison {
    #[inline]
    fn default() -> ScPoison {
        ScPoison {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPoison {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPoison")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPoison {}
#[automatically_derived]
impl ::core::clone::Clone for ScPoison {
    #[inline]
    fn clone(&self) -> ScPoison {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPoison {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPoison {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPoison {
    #[inline]
    fn eq(&self, other: &ScPoison) -> bool {
        true
    }
}
pub struct ScCurse;
#[automatically_derived]
impl ::core::default::Default for ScCurse {
    #[inline]
    fn default() -> ScCurse {
        ScCurse {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCurse {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCurse")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCurse {}
#[automatically_derived]
impl ::core::clone::Clone for ScCurse {
    #[inline]
    fn clone(&self) -> ScCurse {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCurse {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCurse {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCurse {
    #[inline]
    fn eq(&self, other: &ScCurse) -> bool {
        true
    }
}
pub struct ScSilence;
#[automatically_derived]
impl ::core::default::Default for ScSilence {
    #[inline]
    fn default() -> ScSilence {
        ScSilence {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSilence {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSilence")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSilence {}
#[automatically_derived]
impl ::core::clone::Clone for ScSilence {
    #[inline]
    fn clone(&self) -> ScSilence {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSilence {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSilence {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSilence {
    #[inline]
    fn eq(&self, other: &ScSilence) -> bool {
        true
    }
}
pub struct ScConfusion;
#[automatically_derived]
impl ::core::default::Default for ScConfusion {
    #[inline]
    fn default() -> ScConfusion {
        ScConfusion {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScConfusion {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScConfusion")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScConfusion {}
#[automatically_derived]
impl ::core::clone::Clone for ScConfusion {
    #[inline]
    fn clone(&self) -> ScConfusion {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScConfusion {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScConfusion {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScConfusion {
    #[inline]
    fn eq(&self, other: &ScConfusion) -> bool {
        true
    }
}
pub struct ScBlind;
#[automatically_derived]
impl ::core::default::Default for ScBlind {
    #[inline]
    fn default() -> ScBlind {
        ScBlind {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBlind {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBlind")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBlind {}
#[automatically_derived]
impl ::core::clone::Clone for ScBlind {
    #[inline]
    fn clone(&self) -> ScBlind {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBlind {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBlind {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBlind {
    #[inline]
    fn eq(&self, other: &ScBlind) -> bool {
        true
    }
}
pub struct ScBleeding;
#[automatically_derived]
impl ::core::default::Default for ScBleeding {
    #[inline]
    fn default() -> ScBleeding {
        ScBleeding {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBleeding {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBleeding")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBleeding {}
#[automatically_derived]
impl ::core::clone::Clone for ScBleeding {
    #[inline]
    fn clone(&self) -> ScBleeding {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBleeding {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBleeding {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBleeding {
    #[inline]
    fn eq(&self, other: &ScBleeding) -> bool {
        true
    }
}
pub struct ScDpoison;
#[automatically_derived]
impl ::core::default::Default for ScDpoison {
    #[inline]
    fn default() -> ScDpoison {
        ScDpoison {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDpoison {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDpoison")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDpoison {}
#[automatically_derived]
impl ::core::clone::Clone for ScDpoison {
    #[inline]
    fn clone(&self) -> ScDpoison {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDpoison {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDpoison {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDpoison {
    #[inline]
    fn eq(&self, other: &ScDpoison) -> bool {
        true
    }
}
pub struct ScFear;
#[automatically_derived]
impl ::core::default::Default for ScFear {
    #[inline]
    fn default() -> ScFear {
        ScFear {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFear {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFear")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFear {}
#[automatically_derived]
impl ::core::clone::Clone for ScFear {
    #[inline]
    fn clone(&self) -> ScFear {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFear {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFear {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFear {
    #[inline]
    fn eq(&self, other: &ScFear) -> bool {
        true
    }
}
pub struct ScBurning;
#[automatically_derived]
impl ::core::default::Default for ScBurning {
    #[inline]
    fn default() -> ScBurning {
        ScBurning {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBurning {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBurning")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBurning {}
#[automatically_derived]
impl ::core::clone::Clone for ScBurning {
    #[inline]
    fn clone(&self) -> ScBurning {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBurning {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBurning {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBurning {
    #[inline]
    fn eq(&self, other: &ScBurning) -> bool {
        true
    }
}
pub struct ScCrystalize;
#[automatically_derived]
impl ::core::default::Default for ScCrystalize {
    #[inline]
    fn default() -> ScCrystalize {
        ScCrystalize {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCrystalize {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCrystalize")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCrystalize {}
#[automatically_derived]
impl ::core::clone::Clone for ScCrystalize {
    #[inline]
    fn clone(&self) -> ScCrystalize {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCrystalize {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCrystalize {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCrystalize {
    #[inline]
    fn eq(&self, other: &ScCrystalize) -> bool {
        true
    }
}
pub struct ScWhiteImprison;
#[automatically_derived]
impl ::core::default::Default for ScWhiteImprison {
    #[inline]
    fn default() -> ScWhiteImprison {
        ScWhiteImprison {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWhiteImprison {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWhiteImprison")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWhiteImprison {}
#[automatically_derived]
impl ::core::clone::Clone for ScWhiteImprison {
    #[inline]
    fn clone(&self) -> ScWhiteImprison {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWhiteImprison {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWhiteImprison {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWhiteImprison {
    #[inline]
    fn eq(&self, other: &ScWhiteImprison) -> bool {
        true
    }
}
pub struct ScDeepSleep;
#[automatically_derived]
impl ::core::default::Default for ScDeepSleep {
    #[inline]
    fn default() -> ScDeepSleep {
        ScDeepSleep {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDeepSleep {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDeepSleep")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDeepSleep {}
#[automatically_derived]
impl ::core::clone::Clone for ScDeepSleep {
    #[inline]
    fn clone(&self) -> ScDeepSleep {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDeepSleep {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDeepSleep {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDeepSleep {
    #[inline]
    fn eq(&self, other: &ScDeepSleep) -> bool {
        true
    }
}
pub struct ScBerserk;
#[automatically_derived]
impl ::core::default::Default for ScBerserk {
    #[inline]
    fn default() -> ScBerserk {
        ScBerserk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBerserk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBerserk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBerserk {}
#[automatically_derived]
impl ::core::clone::Clone for ScBerserk {
    #[inline]
    fn clone(&self) -> ScBerserk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBerserk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBerserk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBerserk {
    #[inline]
    fn eq(&self, other: &ScBerserk) -> bool {
        true
    }
}
pub struct ScSaturdayNightFever;
#[automatically_derived]
impl ::core::default::Default for ScSaturdayNightFever {
    #[inline]
    fn default() -> ScSaturdayNightFever {
        ScSaturdayNightFever {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSaturdayNightFever {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSaturdayNightFever")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSaturdayNightFever {}
#[automatically_derived]
impl ::core::clone::Clone for ScSaturdayNightFever {
    #[inline]
    fn clone(&self) -> ScSaturdayNightFever {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSaturdayNightFever {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSaturdayNightFever {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSaturdayNightFever {
    #[inline]
    fn eq(&self, other: &ScSaturdayNightFever) -> bool {
        true
    }
}
pub struct ScBloodyLust;
#[automatically_derived]
impl ::core::default::Default for ScBloodyLust {
    #[inline]
    fn default() -> ScBloodyLust {
        ScBloodyLust {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBloodyLust {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBloodyLust")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBloodyLust {}
#[automatically_derived]
impl ::core::clone::Clone for ScBloodyLust {
    #[inline]
    fn clone(&self) -> ScBloodyLust {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBloodyLust {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBloodyLust {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBloodyLust {
    #[inline]
    fn eq(&self, other: &ScBloodyLust) -> bool {
        true
    }
}
pub struct ScSteelBody;
#[automatically_derived]
impl ::core::default::Default for ScSteelBody {
    #[inline]
    fn default() -> ScSteelBody {
        ScSteelBody {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSteelBody {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSteelBody")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSteelBody {}
#[automatically_derived]
impl ::core::clone::Clone for ScSteelBody {
    #[inline]
    fn clone(&self) -> ScSteelBody {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSteelBody {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSteelBody {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSteelBody {
    #[inline]
    fn eq(&self, other: &ScSteelBody) -> bool {
        true
    }
}
pub struct ScBladeStop;
#[automatically_derived]
impl ::core::default::Default for ScBladeStop {
    #[inline]
    fn default() -> ScBladeStop {
        ScBladeStop {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBladeStop {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBladeStop")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBladeStop {}
#[automatically_derived]
impl ::core::clone::Clone for ScBladeStop {
    #[inline]
    fn clone(&self) -> ScBladeStop {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBladeStop {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBladeStop {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBladeStop {
    #[inline]
    fn eq(&self, other: &ScBladeStop) -> bool {
        true
    }
}
pub struct ScAuraBlade;
#[automatically_derived]
impl ::core::default::Default for ScAuraBlade {
    #[inline]
    fn default() -> ScAuraBlade {
        ScAuraBlade {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAuraBlade {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAuraBlade")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAuraBlade {}
#[automatically_derived]
impl ::core::clone::Clone for ScAuraBlade {
    #[inline]
    fn clone(&self) -> ScAuraBlade {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAuraBlade {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAuraBlade {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAuraBlade {
    #[inline]
    fn eq(&self, other: &ScAuraBlade) -> bool {
        true
    }
}
pub struct ScExplosionSpirits;
#[automatically_derived]
impl ::core::default::Default for ScExplosionSpirits {
    #[inline]
    fn default() -> ScExplosionSpirits {
        ScExplosionSpirits {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScExplosionSpirits {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScExplosionSpirits")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScExplosionSpirits {}
#[automatically_derived]
impl ::core::clone::Clone for ScExplosionSpirits {
    #[inline]
    fn clone(&self) -> ScExplosionSpirits {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScExplosionSpirits {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScExplosionSpirits {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScExplosionSpirits {
    #[inline]
    fn eq(&self, other: &ScExplosionSpirits) -> bool {
        true
    }
}
pub struct ScEnergyCoat;
#[automatically_derived]
impl ::core::default::Default for ScEnergyCoat {
    #[inline]
    fn default() -> ScEnergyCoat {
        ScEnergyCoat {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEnergyCoat {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEnergyCoat")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEnergyCoat {}
#[automatically_derived]
impl ::core::clone::Clone for ScEnergyCoat {
    #[inline]
    fn clone(&self) -> ScEnergyCoat {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEnergyCoat {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEnergyCoat {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEnergyCoat {
    #[inline]
    fn eq(&self, other: &ScEnergyCoat) -> bool {
        true
    }
}
pub struct ScOverThrust;
#[automatically_derived]
impl ::core::default::Default for ScOverThrust {
    #[inline]
    fn default() -> ScOverThrust {
        ScOverThrust {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScOverThrust {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScOverThrust")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScOverThrust {}
#[automatically_derived]
impl ::core::clone::Clone for ScOverThrust {
    #[inline]
    fn clone(&self) -> ScOverThrust {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScOverThrust {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScOverThrust {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScOverThrust {
    #[inline]
    fn eq(&self, other: &ScOverThrust) -> bool {
        true
    }
}
pub struct ScQuicken;
#[automatically_derived]
impl ::core::default::Default for ScQuicken {
    #[inline]
    fn default() -> ScQuicken {
        ScQuicken {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScQuicken {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScQuicken")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScQuicken {}
#[automatically_derived]
impl ::core::clone::Clone for ScQuicken {
    #[inline]
    fn clone(&self) -> ScQuicken {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScQuicken {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScQuicken {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScQuicken {
    #[inline]
    fn eq(&self, other: &ScQuicken) -> bool {
        true
    }
}
pub struct ScAngelus;
#[automatically_derived]
impl ::core::default::Default for ScAngelus {
    #[inline]
    fn default() -> ScAngelus {
        ScAngelus {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAngelus {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAngelus")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAngelus {}
#[automatically_derived]
impl ::core::clone::Clone for ScAngelus {
    #[inline]
    fn clone(&self) -> ScAngelus {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAngelus {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAngelus {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAngelus {
    #[inline]
    fn eq(&self, other: &ScAngelus) -> bool {
        true
    }
}
pub struct ScSignumCrucis;
#[automatically_derived]
impl ::core::default::Default for ScSignumCrucis {
    #[inline]
    fn default() -> ScSignumCrucis {
        ScSignumCrucis {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSignumCrucis {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSignumCrucis")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSignumCrucis {}
#[automatically_derived]
impl ::core::clone::Clone for ScSignumCrucis {
    #[inline]
    fn clone(&self) -> ScSignumCrucis {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSignumCrucis {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSignumCrucis {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSignumCrucis {
    #[inline]
    fn eq(&self, other: &ScSignumCrucis) -> bool {
        true
    }
}
pub struct ScAssumptio;
#[automatically_derived]
impl ::core::default::Default for ScAssumptio {
    #[inline]
    fn default() -> ScAssumptio {
        ScAssumptio {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAssumptio {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAssumptio")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAssumptio {}
#[automatically_derived]
impl ::core::clone::Clone for ScAssumptio {
    #[inline]
    fn clone(&self) -> ScAssumptio {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAssumptio {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAssumptio {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAssumptio {
    #[inline]
    fn eq(&self, other: &ScAssumptio) -> bool {
        true
    }
}
pub struct ScKaite;
#[automatically_derived]
impl ::core::default::Default for ScKaite {
    #[inline]
    fn default() -> ScKaite {
        ScKaite {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScKaite {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScKaite")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScKaite {}
#[automatically_derived]
impl ::core::clone::Clone for ScKaite {
    #[inline]
    fn clone(&self) -> ScKaite {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScKaite {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScKaite {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScKaite {
    #[inline]
    fn eq(&self, other: &ScKaite) -> bool {
        true
    }
}
pub struct ScBunsin;
#[automatically_derived]
impl ::core::default::Default for ScBunsin {
    #[inline]
    fn default() -> ScBunsin {
        ScBunsin {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBunsin {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBunsin")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBunsin {}
#[automatically_derived]
impl ::core::clone::Clone for ScBunsin {
    #[inline]
    fn clone(&self) -> ScBunsin {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBunsin {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBunsin {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBunsin {
    #[inline]
    fn eq(&self, other: &ScBunsin) -> bool {
        true
    }
}
pub struct ScSoulLink;
#[automatically_derived]
impl ::core::default::Default for ScSoulLink {
    #[inline]
    fn default() -> ScSoulLink {
        ScSoulLink {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSoulLink {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSoulLink")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSoulLink {}
#[automatically_derived]
impl ::core::clone::Clone for ScSoulLink {
    #[inline]
    fn clone(&self) -> ScSoulLink {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSoulLink {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSoulLink {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSoulLink {
    #[inline]
    fn eq(&self, other: &ScSoulLink) -> bool {
        true
    }
}
pub struct ScWarm;
#[automatically_derived]
impl ::core::default::Default for ScWarm {
    #[inline]
    fn default() -> ScWarm {
        ScWarm {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWarm {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWarm")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWarm {}
#[automatically_derived]
impl ::core::clone::Clone for ScWarm {
    #[inline]
    fn clone(&self) -> ScWarm {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWarm {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWarm {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWarm {
    #[inline]
    fn eq(&self, other: &ScWarm) -> bool {
        true
    }
}
pub struct ScLightBlade;
#[automatically_derived]
impl ::core::default::Default for ScLightBlade {
    #[inline]
    fn default() -> ScLightBlade {
        ScLightBlade {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScLightBlade {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScLightBlade")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScLightBlade {}
#[automatically_derived]
impl ::core::clone::Clone for ScLightBlade {
    #[inline]
    fn clone(&self) -> ScLightBlade {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScLightBlade {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScLightBlade {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScLightBlade {
    #[inline]
    fn eq(&self, other: &ScLightBlade) -> bool {
        true
    }
}
pub struct ScMoonlit;
#[automatically_derived]
impl ::core::default::Default for ScMoonlit {
    #[inline]
    fn default() -> ScMoonlit {
        ScMoonlit {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMoonlit {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMoonlit")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMoonlit {}
#[automatically_derived]
impl ::core::clone::Clone for ScMoonlit {
    #[inline]
    fn clone(&self) -> ScMoonlit {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMoonlit {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMoonlit {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMoonlit {
    #[inline]
    fn eq(&self, other: &ScMoonlit) -> bool {
        true
    }
}
pub struct ScMarionette;
#[automatically_derived]
impl ::core::default::Default for ScMarionette {
    #[inline]
    fn default() -> ScMarionette {
        ScMarionette {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMarionette {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMarionette")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMarionette {}
#[automatically_derived]
impl ::core::clone::Clone for ScMarionette {
    #[inline]
    fn clone(&self) -> ScMarionette {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMarionette {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMarionette {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMarionette {
    #[inline]
    fn eq(&self, other: &ScMarionette) -> bool {
        true
    }
}
pub struct ScUndead;
#[automatically_derived]
impl ::core::default::Default for ScUndead {
    #[inline]
    fn default() -> ScUndead {
        ScUndead {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScUndead {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScUndead")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScUndead {}
#[automatically_derived]
impl ::core::clone::Clone for ScUndead {
    #[inline]
    fn clone(&self) -> ScUndead {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScUndead {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScUndead {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScUndead {
    #[inline]
    fn eq(&self, other: &ScUndead) -> bool {
        true
    }
}
pub struct ScContract;
#[automatically_derived]
impl ::core::default::Default for ScContract {
    #[inline]
    fn default() -> ScContract {
        ScContract {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScContract {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScContract")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScContract {}
#[automatically_derived]
impl ::core::clone::Clone for ScContract {
    #[inline]
    fn clone(&self) -> ScContract {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScContract {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScContract {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScContract {
    #[inline]
    fn eq(&self, other: &ScContract) -> bool {
        true
    }
}
pub struct ScSight;
#[automatically_derived]
impl ::core::default::Default for ScSight {
    #[inline]
    fn default() -> ScSight {
        ScSight {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSight {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSight")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSight {}
#[automatically_derived]
impl ::core::clone::Clone for ScSight {
    #[inline]
    fn clone(&self) -> ScSight {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSight {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSight {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSight {
    #[inline]
    fn eq(&self, other: &ScSight) -> bool {
        true
    }
}
pub struct ScHide;
#[automatically_derived]
impl ::core::default::Default for ScHide {
    #[inline]
    fn default() -> ScHide {
        ScHide {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScHide {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScHide")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScHide {}
#[automatically_derived]
impl ::core::clone::Clone for ScHide {
    #[inline]
    fn clone(&self) -> ScHide {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScHide {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScHide {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScHide {
    #[inline]
    fn eq(&self, other: &ScHide) -> bool {
        true
    }
}
pub struct ScCloaking;
#[automatically_derived]
impl ::core::default::Default for ScCloaking {
    #[inline]
    fn default() -> ScCloaking {
        ScCloaking {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCloaking {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCloaking")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCloaking {}
#[automatically_derived]
impl ::core::clone::Clone for ScCloaking {
    #[inline]
    fn clone(&self) -> ScCloaking {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCloaking {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCloaking {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCloaking {
    #[inline]
    fn eq(&self, other: &ScCloaking) -> bool {
        true
    }
}
pub struct ScChaseWalk;
#[automatically_derived]
impl ::core::default::Default for ScChaseWalk {
    #[inline]
    fn default() -> ScChaseWalk {
        ScChaseWalk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScChaseWalk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScChaseWalk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScChaseWalk {}
#[automatically_derived]
impl ::core::clone::Clone for ScChaseWalk {
    #[inline]
    fn clone(&self) -> ScChaseWalk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScChaseWalk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScChaseWalk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScChaseWalk {
    #[inline]
    fn eq(&self, other: &ScChaseWalk) -> bool {
        true
    }
}
pub struct ScRuwach;
#[automatically_derived]
impl ::core::default::Default for ScRuwach {
    #[inline]
    fn default() -> ScRuwach {
        ScRuwach {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScRuwach {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScRuwach")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScRuwach {}
#[automatically_derived]
impl ::core::clone::Clone for ScRuwach {
    #[inline]
    fn clone(&self) -> ScRuwach {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScRuwach {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScRuwach {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScRuwach {
    #[inline]
    fn eq(&self, other: &ScRuwach) -> bool {
        true
    }
}
pub struct ScFlying;
#[automatically_derived]
impl ::core::default::Default for ScFlying {
    #[inline]
    fn default() -> ScFlying {
        ScFlying {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFlying {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFlying")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFlying {}
#[automatically_derived]
impl ::core::clone::Clone for ScFlying {
    #[inline]
    fn clone(&self) -> ScFlying {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFlying {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFlying {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFlying {
    #[inline]
    fn eq(&self, other: &ScFlying) -> bool {
        true
    }
}
pub struct ScWug;
#[automatically_derived]
impl ::core::default::Default for ScWug {
    #[inline]
    fn default() -> ScWug {
        ScWug {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWug {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWug")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWug {}
#[automatically_derived]
impl ::core::clone::Clone for ScWug {
    #[inline]
    fn clone(&self) -> ScWug {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWug {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWug {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWug {
    #[inline]
    fn eq(&self, other: &ScWug) -> bool {
        true
    }
}
pub struct ScWugRider;
#[automatically_derived]
impl ::core::default::Default for ScWugRider {
    #[inline]
    fn default() -> ScWugRider {
        ScWugRider {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWugRider {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWugRider")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWugRider {}
#[automatically_derived]
impl ::core::clone::Clone for ScWugRider {
    #[inline]
    fn clone(&self) -> ScWugRider {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWugRider {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWugRider {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWugRider {
    #[inline]
    fn eq(&self, other: &ScWugRider) -> bool {
        true
    }
}
pub struct ScMadogear;
#[automatically_derived]
impl ::core::default::Default for ScMadogear {
    #[inline]
    fn default() -> ScMadogear {
        ScMadogear {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMadogear {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMadogear")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMadogear {}
#[automatically_derived]
impl ::core::clone::Clone for ScMadogear {
    #[inline]
    fn clone(&self) -> ScMadogear {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMadogear {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMadogear {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMadogear {
    #[inline]
    fn eq(&self, other: &ScMadogear) -> bool {
        true
    }
}
pub struct ScXmas;
#[automatically_derived]
impl ::core::default::Default for ScXmas {
    #[inline]
    fn default() -> ScXmas {
        ScXmas {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScXmas {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScXmas")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScXmas {}
#[automatically_derived]
impl ::core::clone::Clone for ScXmas {
    #[inline]
    fn clone(&self) -> ScXmas {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScXmas {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScXmas {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScXmas {
    #[inline]
    fn eq(&self, other: &ScXmas) -> bool {
        true
    }
}
pub struct ScSummer;
#[automatically_derived]
impl ::core::default::Default for ScSummer {
    #[inline]
    fn default() -> ScSummer {
        ScSummer {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSummer {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSummer")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSummer {}
#[automatically_derived]
impl ::core::clone::Clone for ScSummer {
    #[inline]
    fn clone(&self) -> ScSummer {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSummer {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSummer {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSummer {
    #[inline]
    fn eq(&self, other: &ScSummer) -> bool {
        true
    }
}
pub struct ScDragon1;
#[automatically_derived]
impl ::core::default::Default for ScDragon1 {
    #[inline]
    fn default() -> ScDragon1 {
        ScDragon1 {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDragon1 {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDragon1")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDragon1 {}
#[automatically_derived]
impl ::core::clone::Clone for ScDragon1 {
    #[inline]
    fn clone(&self) -> ScDragon1 {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDragon1 {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDragon1 {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDragon1 {
    #[inline]
    fn eq(&self, other: &ScDragon1) -> bool {
        true
    }
}
pub struct ScDragon2;
#[automatically_derived]
impl ::core::default::Default for ScDragon2 {
    #[inline]
    fn default() -> ScDragon2 {
        ScDragon2 {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDragon2 {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDragon2")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDragon2 {}
#[automatically_derived]
impl ::core::clone::Clone for ScDragon2 {
    #[inline]
    fn clone(&self) -> ScDragon2 {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDragon2 {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDragon2 {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDragon2 {
    #[inline]
    fn eq(&self, other: &ScDragon2) -> bool {
        true
    }
}
pub struct ScDragon3;
#[automatically_derived]
impl ::core::default::Default for ScDragon3 {
    #[inline]
    fn default() -> ScDragon3 {
        ScDragon3 {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDragon3 {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDragon3")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDragon3 {}
#[automatically_derived]
impl ::core::clone::Clone for ScDragon3 {
    #[inline]
    fn clone(&self) -> ScDragon3 {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDragon3 {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDragon3 {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDragon3 {
    #[inline]
    fn eq(&self, other: &ScDragon3) -> bool {
        true
    }
}
pub struct ScDragon4;
#[automatically_derived]
impl ::core::default::Default for ScDragon4 {
    #[inline]
    fn default() -> ScDragon4 {
        ScDragon4 {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDragon4 {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDragon4")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDragon4 {}
#[automatically_derived]
impl ::core::clone::Clone for ScDragon4 {
    #[inline]
    fn clone(&self) -> ScDragon4 {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDragon4 {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDragon4 {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDragon4 {
    #[inline]
    fn eq(&self, other: &ScDragon4) -> bool {
        true
    }
}
pub struct ScDragon5;
#[automatically_derived]
impl ::core::default::Default for ScDragon5 {
    #[inline]
    fn default() -> ScDragon5 {
        ScDragon5 {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDragon5 {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDragon5")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDragon5 {}
#[automatically_derived]
impl ::core::clone::Clone for ScDragon5 {
    #[inline]
    fn clone(&self) -> ScDragon5 {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDragon5 {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDragon5 {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDragon5 {
    #[inline]
    fn eq(&self, other: &ScDragon5) -> bool {
        true
    }
}
pub struct ScHanbok;
#[automatically_derived]
impl ::core::default::Default for ScHanbok {
    #[inline]
    fn default() -> ScHanbok {
        ScHanbok {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScHanbok {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScHanbok")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScHanbok {}
#[automatically_derived]
impl ::core::clone::Clone for ScHanbok {
    #[inline]
    fn clone(&self) -> ScHanbok {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScHanbok {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScHanbok {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScHanbok {
    #[inline]
    fn eq(&self, other: &ScHanbok) -> bool {
        true
    }
}
pub struct ScOktoberfest;
#[automatically_derived]
impl ::core::default::Default for ScOktoberfest {
    #[inline]
    fn default() -> ScOktoberfest {
        ScOktoberfest {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScOktoberfest {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScOktoberfest")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScOktoberfest {}
#[automatically_derived]
impl ::core::clone::Clone for ScOktoberfest {
    #[inline]
    fn clone(&self) -> ScOktoberfest {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScOktoberfest {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScOktoberfest {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScOktoberfest {
    #[inline]
    fn eq(&self, other: &ScOktoberfest) -> bool {
        true
    }
}
pub struct ScCartBoost;
#[automatically_derived]
impl ::core::default::Default for ScCartBoost {
    #[inline]
    fn default() -> ScCartBoost {
        ScCartBoost {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCartBoost {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCartBoost")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCartBoost {}
#[automatically_derived]
impl ::core::clone::Clone for ScCartBoost {
    #[inline]
    fn clone(&self) -> ScCartBoost {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCartBoost {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCartBoost {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCartBoost {
    #[inline]
    fn eq(&self, other: &ScCartBoost) -> bool {
        true
    }
}
pub struct ScWindWalk;
#[automatically_derived]
impl ::core::default::Default for ScWindWalk {
    #[inline]
    fn default() -> ScWindWalk {
        ScWindWalk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWindWalk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWindWalk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWindWalk {}
#[automatically_derived]
impl ::core::clone::Clone for ScWindWalk {
    #[inline]
    fn clone(&self) -> ScWindWalk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWindWalk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWindWalk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWindWalk {
    #[inline]
    fn eq(&self, other: &ScWindWalk) -> bool {
        true
    }
}
pub struct ScIncreaseAgi;
#[automatically_derived]
impl ::core::default::Default for ScIncreaseAgi {
    #[inline]
    fn default() -> ScIncreaseAgi {
        ScIncreaseAgi {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScIncreaseAgi {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScIncreaseAgi")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScIncreaseAgi {}
#[automatically_derived]
impl ::core::clone::Clone for ScIncreaseAgi {
    #[inline]
    fn clone(&self) -> ScIncreaseAgi {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScIncreaseAgi {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScIncreaseAgi {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScIncreaseAgi {
    #[inline]
    fn eq(&self, other: &ScIncreaseAgi) -> bool {
        true
    }
}
pub struct ScDecreaseAgi;
#[automatically_derived]
impl ::core::default::Default for ScDecreaseAgi {
    #[inline]
    fn default() -> ScDecreaseAgi {
        ScDecreaseAgi {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDecreaseAgi {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDecreaseAgi")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDecreaseAgi {}
#[automatically_derived]
impl ::core::clone::Clone for ScDecreaseAgi {
    #[inline]
    fn clone(&self) -> ScDecreaseAgi {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDecreaseAgi {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDecreaseAgi {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDecreaseAgi {
    #[inline]
    fn eq(&self, other: &ScDecreaseAgi) -> bool {
        true
    }
}
pub struct ScSlowGrace;
#[automatically_derived]
impl ::core::default::Default for ScSlowGrace {
    #[inline]
    fn default() -> ScSlowGrace {
        ScSlowGrace {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSlowGrace {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSlowGrace")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSlowGrace {}
#[automatically_derived]
impl ::core::clone::Clone for ScSlowGrace {
    #[inline]
    fn clone(&self) -> ScSlowGrace {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSlowGrace {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSlowGrace {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSlowGrace {
    #[inline]
    fn eq(&self, other: &ScSlowGrace) -> bool {
        true
    }
}
pub struct ScPlatinumAlter;
#[automatically_derived]
impl ::core::default::Default for ScPlatinumAlter {
    #[inline]
    fn default() -> ScPlatinumAlter {
        ScPlatinumAlter {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPlatinumAlter {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPlatinumAlter")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPlatinumAlter {}
#[automatically_derived]
impl ::core::clone::Clone for ScPlatinumAlter {
    #[inline]
    fn clone(&self) -> ScPlatinumAlter {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPlatinumAlter {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPlatinumAlter {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPlatinumAlter {
    #[inline]
    fn eq(&self, other: &ScPlatinumAlter) -> bool {
        true
    }
}
pub struct ScMadnessCanceler;
#[automatically_derived]
impl ::core::default::Default for ScMadnessCanceler {
    #[inline]
    fn default() -> ScMadnessCanceler {
        ScMadnessCanceler {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMadnessCanceler {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMadnessCanceler")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMadnessCanceler {}
#[automatically_derived]
impl ::core::clone::Clone for ScMadnessCanceler {
    #[inline]
    fn clone(&self) -> ScMadnessCanceler {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMadnessCanceler {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMadnessCanceler {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMadnessCanceler {
    #[inline]
    fn eq(&self, other: &ScMadnessCanceler) -> bool {
        true
    }
}
pub struct ScOneHand;
#[automatically_derived]
impl ::core::default::Default for ScOneHand {
    #[inline]
    fn default() -> ScOneHand {
        ScOneHand {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScOneHand {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScOneHand")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScOneHand {}
#[automatically_derived]
impl ::core::clone::Clone for ScOneHand {
    #[inline]
    fn clone(&self) -> ScOneHand {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScOneHand {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScOneHand {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScOneHand {
    #[inline]
    fn eq(&self, other: &ScOneHand) -> bool {
        true
    }
}
pub struct ScTarotCard;
#[automatically_derived]
impl ::core::default::Default for ScTarotCard {
    #[inline]
    fn default() -> ScTarotCard {
        ScTarotCard {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScTarotCard {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScTarotCard")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScTarotCard {}
#[automatically_derived]
impl ::core::clone::Clone for ScTarotCard {
    #[inline]
    fn clone(&self) -> ScTarotCard {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScTarotCard {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScTarotCard {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScTarotCard {
    #[inline]
    fn eq(&self, other: &ScTarotCard) -> bool {
        true
    }
}
pub struct ScRefresh;
#[automatically_derived]
impl ::core::default::Default for ScRefresh {
    #[inline]
    fn default() -> ScRefresh {
        ScRefresh {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScRefresh {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScRefresh")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScRefresh {}
#[automatically_derived]
impl ::core::clone::Clone for ScRefresh {
    #[inline]
    fn clone(&self) -> ScRefresh {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScRefresh {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScRefresh {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScRefresh {
    #[inline]
    fn eq(&self, other: &ScRefresh) -> bool {
        true
    }
}
pub struct ScLuxAnima;
#[automatically_derived]
impl ::core::default::Default for ScLuxAnima {
    #[inline]
    fn default() -> ScLuxAnima {
        ScLuxAnima {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScLuxAnima {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScLuxAnima")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScLuxAnima {}
#[automatically_derived]
impl ::core::clone::Clone for ScLuxAnima {
    #[inline]
    fn clone(&self) -> ScLuxAnima {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScLuxAnima {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScLuxAnima {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScLuxAnima {
    #[inline]
    fn eq(&self, other: &ScLuxAnima) -> bool {
        true
    }
}
pub struct ScHermode;
#[automatically_derived]
impl ::core::default::Default for ScHermode {
    #[inline]
    fn default() -> ScHermode {
        ScHermode {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScHermode {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScHermode")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScHermode {}
#[automatically_derived]
impl ::core::clone::Clone for ScHermode {
    #[inline]
    fn clone(&self) -> ScHermode {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScHermode {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScHermode {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScHermode {
    #[inline]
    fn eq(&self, other: &ScHermode) -> bool {
        true
    }
}
pub struct ScGospel;
#[automatically_derived]
impl ::core::default::Default for ScGospel {
    #[inline]
    fn default() -> ScGospel {
        ScGospel {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScGospel {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScGospel")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScGospel {}
#[automatically_derived]
impl ::core::clone::Clone for ScGospel {
    #[inline]
    fn clone(&self) -> ScGospel {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScGospel {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScGospel {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScGospel {
    #[inline]
    fn eq(&self, other: &ScGospel) -> bool {
        true
    }
}
pub struct ScInspiration;
#[automatically_derived]
impl ::core::default::Default for ScInspiration {
    #[inline]
    fn default() -> ScInspiration {
        ScInspiration {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScInspiration {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScInspiration")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScInspiration {}
#[automatically_derived]
impl ::core::clone::Clone for ScInspiration {
    #[inline]
    fn clone(&self) -> ScInspiration {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScInspiration {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScInspiration {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScInspiration {
    #[inline]
    fn eq(&self, other: &ScInspiration) -> bool {
        true
    }
}
pub struct ScBanishingBuster;
#[automatically_derived]
impl ::core::default::Default for ScBanishingBuster {
    #[inline]
    fn default() -> ScBanishingBuster {
        ScBanishingBuster {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBanishingBuster {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBanishingBuster")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBanishingBuster {}
#[automatically_derived]
impl ::core::clone::Clone for ScBanishingBuster {
    #[inline]
    fn clone(&self) -> ScBanishingBuster {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBanishingBuster {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBanishingBuster {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBanishingBuster {
    #[inline]
    fn eq(&self, other: &ScBanishingBuster) -> bool {
        true
    }
}
pub struct ScClearance;
#[automatically_derived]
impl ::core::default::Default for ScClearance {
    #[inline]
    fn default() -> ScClearance {
        ScClearance {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScClearance {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScClearance")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScClearance {}
#[automatically_derived]
impl ::core::clone::Clone for ScClearance {
    #[inline]
    fn clone(&self) -> ScClearance {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScClearance {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScClearance {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScClearance {
    #[inline]
    fn eq(&self, other: &ScClearance) -> bool {
        true
    }
}
pub struct ScDispell;
#[automatically_derived]
impl ::core::default::Default for ScDispell {
    #[inline]
    fn default() -> ScDispell {
        ScDispell {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDispell {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDispell")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDispell {}
#[automatically_derived]
impl ::core::clone::Clone for ScDispell {
    #[inline]
    fn clone(&self) -> ScDispell {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDispell {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDispell {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDispell {
    #[inline]
    fn eq(&self, other: &ScDispell) -> bool {
        true
    }
}
pub struct ScElementalChange;
#[automatically_derived]
impl ::core::default::Default for ScElementalChange {
    #[inline]
    fn default() -> ScElementalChange {
        ScElementalChange {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScElementalChange {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScElementalChange")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScElementalChange {}
#[automatically_derived]
impl ::core::clone::Clone for ScElementalChange {
    #[inline]
    fn clone(&self) -> ScElementalChange {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScElementalChange {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScElementalChange {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScElementalChange {
    #[inline]
    fn eq(&self, other: &ScElementalChange) -> bool {
        true
    }
}
pub struct ScTidalWeapon;
#[automatically_derived]
impl ::core::default::Default for ScTidalWeapon {
    #[inline]
    fn default() -> ScTidalWeapon {
        ScTidalWeapon {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScTidalWeapon {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScTidalWeapon")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScTidalWeapon {}
#[automatically_derived]
impl ::core::clone::Clone for ScTidalWeapon {
    #[inline]
    fn clone(&self) -> ScTidalWeapon {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScTidalWeapon {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScTidalWeapon {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScTidalWeapon {
    #[inline]
    fn eq(&self, other: &ScTidalWeapon) -> bool {
        true
    }
}
pub struct ScWaterScreen;
#[automatically_derived]
impl ::core::default::Default for ScWaterScreen {
    #[inline]
    fn default() -> ScWaterScreen {
        ScWaterScreen {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWaterScreen {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWaterScreen")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWaterScreen {}
#[automatically_derived]
impl ::core::clone::Clone for ScWaterScreen {
    #[inline]
    fn clone(&self) -> ScWaterScreen {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWaterScreen {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWaterScreen {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWaterScreen {
    #[inline]
    fn eq(&self, other: &ScWaterScreen) -> bool {
        true
    }
}
pub struct ScCpWeapon;
#[automatically_derived]
impl ::core::default::Default for ScCpWeapon {
    #[inline]
    fn default() -> ScCpWeapon {
        ScCpWeapon {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCpWeapon {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCpWeapon")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCpWeapon {}
#[automatically_derived]
impl ::core::clone::Clone for ScCpWeapon {
    #[inline]
    fn clone(&self) -> ScCpWeapon {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCpWeapon {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCpWeapon {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCpWeapon {
    #[inline]
    fn eq(&self, other: &ScCpWeapon) -> bool {
        true
    }
}
pub struct ScCpArmor;
#[automatically_derived]
impl ::core::default::Default for ScCpArmor {
    #[inline]
    fn default() -> ScCpArmor {
        ScCpArmor {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCpArmor {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCpArmor")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCpArmor {}
#[automatically_derived]
impl ::core::clone::Clone for ScCpArmor {
    #[inline]
    fn clone(&self) -> ScCpArmor {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCpArmor {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCpArmor {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCpArmor {
    #[inline]
    fn eq(&self, other: &ScCpArmor) -> bool {
        true
    }
}
pub struct ScCpShield;
#[automatically_derived]
impl ::core::default::Default for ScCpShield {
    #[inline]
    fn default() -> ScCpShield {
        ScCpShield {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCpShield {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCpShield")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCpShield {}
#[automatically_derived]
impl ::core::clone::Clone for ScCpShield {
    #[inline]
    fn clone(&self) -> ScCpShield {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCpShield {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCpShield {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCpShield {
    #[inline]
    fn eq(&self, other: &ScCpShield) -> bool {
        true
    }
}
pub struct ScCpHelm;
#[automatically_derived]
impl ::core::default::Default for ScCpHelm {
    #[inline]
    fn default() -> ScCpHelm {
        ScCpHelm {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCpHelm {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCpHelm")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCpHelm {}
#[automatically_derived]
impl ::core::clone::Clone for ScCpHelm {
    #[inline]
    fn clone(&self) -> ScCpHelm {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCpHelm {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCpHelm {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCpHelm {
    #[inline]
    fn eq(&self, other: &ScCpHelm) -> bool {
        true
    }
}
pub struct ScMaximizePower;
#[automatically_derived]
impl ::core::default::Default for ScMaximizePower {
    #[inline]
    fn default() -> ScMaximizePower {
        ScMaximizePower {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMaximizePower {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMaximizePower")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMaximizePower {}
#[automatically_derived]
impl ::core::clone::Clone for ScMaximizePower {
    #[inline]
    fn clone(&self) -> ScMaximizePower {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMaximizePower {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMaximizePower {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMaximizePower {
    #[inline]
    fn eq(&self, other: &ScMaximizePower) -> bool {
        true
    }
}
pub struct ScEdp;
#[automatically_derived]
impl ::core::default::Default for ScEdp {
    #[inline]
    fn default() -> ScEdp {
        ScEdp {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEdp {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEdp")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEdp {}
#[automatically_derived]
impl ::core::clone::Clone for ScEdp {
    #[inline]
    fn clone(&self) -> ScEdp {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEdp {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEdp {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEdp {
    #[inline]
    fn eq(&self, other: &ScEdp) -> bool {
        true
    }
}
pub struct ScAdrenaline;
#[automatically_derived]
impl ::core::default::Default for ScAdrenaline {
    #[inline]
    fn default() -> ScAdrenaline {
        ScAdrenaline {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAdrenaline {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAdrenaline")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAdrenaline {}
#[automatically_derived]
impl ::core::clone::Clone for ScAdrenaline {
    #[inline]
    fn clone(&self) -> ScAdrenaline {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAdrenaline {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAdrenaline {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAdrenaline {
    #[inline]
    fn eq(&self, other: &ScAdrenaline) -> bool {
        true
    }
}
pub struct ScWeaponPerfection;
#[automatically_derived]
impl ::core::default::Default for ScWeaponPerfection {
    #[inline]
    fn default() -> ScWeaponPerfection {
        ScWeaponPerfection {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWeaponPerfection {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWeaponPerfection")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWeaponPerfection {}
#[automatically_derived]
impl ::core::clone::Clone for ScWeaponPerfection {
    #[inline]
    fn clone(&self) -> ScWeaponPerfection {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWeaponPerfection {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWeaponPerfection {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWeaponPerfection {
    #[inline]
    fn eq(&self, other: &ScWeaponPerfection) -> bool {
        true
    }
}
pub struct ScOverThrustMax;
#[automatically_derived]
impl ::core::default::Default for ScOverThrustMax {
    #[inline]
    fn default() -> ScOverThrustMax {
        ScOverThrustMax {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScOverThrustMax {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScOverThrustMax")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScOverThrustMax {}
#[automatically_derived]
impl ::core::clone::Clone for ScOverThrustMax {
    #[inline]
    fn clone(&self) -> ScOverThrustMax {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScOverThrustMax {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScOverThrustMax {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScOverThrustMax {
    #[inline]
    fn eq(&self, other: &ScOverThrustMax) -> bool {
        true
    }
}
pub struct ScPowerThrust;
#[automatically_derived]
impl ::core::default::Default for ScPowerThrust {
    #[inline]
    fn default() -> ScPowerThrust {
        ScPowerThrust {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPowerThrust {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPowerThrust")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPowerThrust {}
#[automatically_derived]
impl ::core::clone::Clone for ScPowerThrust {
    #[inline]
    fn clone(&self) -> ScPowerThrust {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPowerThrust {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPowerThrust {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPowerThrust {
    #[inline]
    fn eq(&self, other: &ScPowerThrust) -> bool {
        true
    }
}
pub struct ScMagicPower;
#[automatically_derived]
impl ::core::default::Default for ScMagicPower {
    #[inline]
    fn default() -> ScMagicPower {
        ScMagicPower {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMagicPower {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMagicPower")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMagicPower {}
#[automatically_derived]
impl ::core::clone::Clone for ScMagicPower {
    #[inline]
    fn clone(&self) -> ScMagicPower {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMagicPower {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMagicPower {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMagicPower {
    #[inline]
    fn eq(&self, other: &ScMagicPower) -> bool {
        true
    }
}
pub struct ScPreserve;
#[automatically_derived]
impl ::core::default::Default for ScPreserve {
    #[inline]
    fn default() -> ScPreserve {
        ScPreserve {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPreserve {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPreserve")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPreserve {}
#[automatically_derived]
impl ::core::clone::Clone for ScPreserve {
    #[inline]
    fn clone(&self) -> ScPreserve {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPreserve {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPreserve {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPreserve {
    #[inline]
    fn eq(&self, other: &ScPreserve) -> bool {
        true
    }
}
pub struct ScEnchantBlade;
#[automatically_derived]
impl ::core::default::Default for ScEnchantBlade {
    #[inline]
    fn default() -> ScEnchantBlade {
        ScEnchantBlade {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEnchantBlade {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEnchantBlade")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEnchantBlade {}
#[automatically_derived]
impl ::core::clone::Clone for ScEnchantBlade {
    #[inline]
    fn clone(&self) -> ScEnchantBlade {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEnchantBlade {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEnchantBlade {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEnchantBlade {
    #[inline]
    fn eq(&self, other: &ScEnchantBlade) -> bool {
        true
    }
}
pub struct ScDeathBound;
#[automatically_derived]
impl ::core::default::Default for ScDeathBound {
    #[inline]
    fn default() -> ScDeathBound {
        ScDeathBound {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDeathBound {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDeathBound")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDeathBound {}
#[automatically_derived]
impl ::core::clone::Clone for ScDeathBound {
    #[inline]
    fn clone(&self) -> ScDeathBound {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDeathBound {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDeathBound {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDeathBound {
    #[inline]
    fn eq(&self, other: &ScDeathBound) -> bool {
        true
    }
}
pub struct ScMillenniumShield;
#[automatically_derived]
impl ::core::default::Default for ScMillenniumShield {
    #[inline]
    fn default() -> ScMillenniumShield {
        ScMillenniumShield {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMillenniumShield {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMillenniumShield")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMillenniumShield {}
#[automatically_derived]
impl ::core::clone::Clone for ScMillenniumShield {
    #[inline]
    fn clone(&self) -> ScMillenniumShield {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMillenniumShield {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMillenniumShield {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMillenniumShield {
    #[inline]
    fn eq(&self, other: &ScMillenniumShield) -> bool {
        true
    }
}
pub struct ScCrushStrike;
#[automatically_derived]
impl ::core::default::Default for ScCrushStrike {
    #[inline]
    fn default() -> ScCrushStrike {
        ScCrushStrike {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCrushStrike {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCrushStrike")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCrushStrike {}
#[automatically_derived]
impl ::core::clone::Clone for ScCrushStrike {
    #[inline]
    fn clone(&self) -> ScCrushStrike {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCrushStrike {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCrushStrike {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCrushStrike {
    #[inline]
    fn eq(&self, other: &ScCrushStrike) -> bool {
        true
    }
}
pub struct ScRebirth;
#[automatically_derived]
impl ::core::default::Default for ScRebirth {
    #[inline]
    fn default() -> ScRebirth {
        ScRebirth {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScRebirth {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScRebirth")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScRebirth {}
#[automatically_derived]
impl ::core::clone::Clone for ScRebirth {
    #[inline]
    fn clone(&self) -> ScRebirth {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScRebirth {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScRebirth {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScRebirth {
    #[inline]
    fn eq(&self, other: &ScRebirth) -> bool {
        true
    }
}
pub struct ScSpellBreaker;
#[automatically_derived]
impl ::core::default::Default for ScSpellBreaker {
    #[inline]
    fn default() -> ScSpellBreaker {
        ScSpellBreaker {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSpellBreaker {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSpellBreaker")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSpellBreaker {}
#[automatically_derived]
impl ::core::clone::Clone for ScSpellBreaker {
    #[inline]
    fn clone(&self) -> ScSpellBreaker {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSpellBreaker {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSpellBreaker {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSpellBreaker {
    #[inline]
    fn eq(&self, other: &ScSpellBreaker) -> bool {
        true
    }
}
pub struct ScEnchantPoison;
#[automatically_derived]
impl ::core::default::Default for ScEnchantPoison {
    #[inline]
    fn default() -> ScEnchantPoison {
        ScEnchantPoison {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEnchantPoison {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEnchantPoison")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEnchantPoison {}
#[automatically_derived]
impl ::core::clone::Clone for ScEnchantPoison {
    #[inline]
    fn clone(&self) -> ScEnchantPoison {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEnchantPoison {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEnchantPoison {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEnchantPoison {
    #[inline]
    fn eq(&self, other: &ScEnchantPoison) -> bool {
        true
    }
}
pub struct ScPoisonReact;
#[automatically_derived]
impl ::core::default::Default for ScPoisonReact {
    #[inline]
    fn default() -> ScPoisonReact {
        ScPoisonReact {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPoisonReact {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPoisonReact")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPoisonReact {}
#[automatically_derived]
impl ::core::clone::Clone for ScPoisonReact {
    #[inline]
    fn clone(&self) -> ScPoisonReact {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPoisonReact {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPoisonReact {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPoisonReact {
    #[inline]
    fn eq(&self, other: &ScPoisonReact) -> bool {
        true
    }
}
pub struct ScAspersio;
#[automatically_derived]
impl ::core::default::Default for ScAspersio {
    #[inline]
    fn default() -> ScAspersio {
        ScAspersio {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAspersio {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAspersio")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAspersio {}
#[automatically_derived]
impl ::core::clone::Clone for ScAspersio {
    #[inline]
    fn clone(&self) -> ScAspersio {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAspersio {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAspersio {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAspersio {
    #[inline]
    fn eq(&self, other: &ScAspersio) -> bool {
        true
    }
}
pub struct ScBenedictum;
#[automatically_derived]
impl ::core::default::Default for ScBenedictum {
    #[inline]
    fn default() -> ScBenedictum {
        ScBenedictum {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBenedictum {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBenedictum")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBenedictum {}
#[automatically_derived]
impl ::core::clone::Clone for ScBenedictum {
    #[inline]
    fn clone(&self) -> ScBenedictum {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBenedictum {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBenedictum {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBenedictum {
    #[inline]
    fn eq(&self, other: &ScBenedictum) -> bool {
        true
    }
}
pub struct ScKyrie;
#[automatically_derived]
impl ::core::default::Default for ScKyrie {
    #[inline]
    fn default() -> ScKyrie {
        ScKyrie {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScKyrie {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScKyrie")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScKyrie {}
#[automatically_derived]
impl ::core::clone::Clone for ScKyrie {
    #[inline]
    fn clone(&self) -> ScKyrie {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScKyrie {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScKyrie {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScKyrie {
    #[inline]
    fn eq(&self, other: &ScKyrie) -> bool {
        true
    }
}
pub struct ScMagnificat;
#[automatically_derived]
impl ::core::default::Default for ScMagnificat {
    #[inline]
    fn default() -> ScMagnificat {
        ScMagnificat {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMagnificat {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMagnificat")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMagnificat {}
#[automatically_derived]
impl ::core::clone::Clone for ScMagnificat {
    #[inline]
    fn clone(&self) -> ScMagnificat {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMagnificat {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMagnificat {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMagnificat {
    #[inline]
    fn eq(&self, other: &ScMagnificat) -> bool {
        true
    }
}
pub struct ScGloria;
#[automatically_derived]
impl ::core::default::Default for ScGloria {
    #[inline]
    fn default() -> ScGloria {
        ScGloria {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScGloria {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScGloria")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScGloria {}
#[automatically_derived]
impl ::core::clone::Clone for ScGloria {
    #[inline]
    fn clone(&self) -> ScGloria {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScGloria {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScGloria {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScGloria {
    #[inline]
    fn eq(&self, other: &ScGloria) -> bool {
        true
    }
}
pub struct ScLexDivina;
#[automatically_derived]
impl ::core::default::Default for ScLexDivina {
    #[inline]
    fn default() -> ScLexDivina {
        ScLexDivina {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScLexDivina {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScLexDivina")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScLexDivina {}
#[automatically_derived]
impl ::core::clone::Clone for ScLexDivina {
    #[inline]
    fn clone(&self) -> ScLexDivina {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScLexDivina {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScLexDivina {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScLexDivina {
    #[inline]
    fn eq(&self, other: &ScLexDivina) -> bool {
        true
    }
}
pub struct ScLexAeterna;
#[automatically_derived]
impl ::core::default::Default for ScLexAeterna {
    #[inline]
    fn default() -> ScLexAeterna {
        ScLexAeterna {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScLexAeterna {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScLexAeterna")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScLexAeterna {}
#[automatically_derived]
impl ::core::clone::Clone for ScLexAeterna {
    #[inline]
    fn clone(&self) -> ScLexAeterna {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScLexAeterna {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScLexAeterna {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScLexAeterna {
    #[inline]
    fn eq(&self, other: &ScLexAeterna) -> bool {
        true
    }
}
pub struct ScAdoramus;
#[automatically_derived]
impl ::core::default::Default for ScAdoramus {
    #[inline]
    fn default() -> ScAdoramus {
        ScAdoramus {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAdoramus {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAdoramus")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAdoramus {}
#[automatically_derived]
impl ::core::clone::Clone for ScAdoramus {
    #[inline]
    fn clone(&self) -> ScAdoramus {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAdoramus {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAdoramus {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAdoramus {
    #[inline]
    fn eq(&self, other: &ScAdoramus) -> bool {
        true
    }
}
pub struct ScDupleLight;
#[automatically_derived]
impl ::core::default::Default for ScDupleLight {
    #[inline]
    fn default() -> ScDupleLight {
        ScDupleLight {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDupleLight {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDupleLight")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDupleLight {}
#[automatically_derived]
impl ::core::clone::Clone for ScDupleLight {
    #[inline]
    fn clone(&self) -> ScDupleLight {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDupleLight {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDupleLight {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDupleLight {
    #[inline]
    fn eq(&self, other: &ScDupleLight) -> bool {
        true
    }
}
pub struct ScCloakingExceed;
#[automatically_derived]
impl ::core::default::Default for ScCloakingExceed {
    #[inline]
    fn default() -> ScCloakingExceed {
        ScCloakingExceed {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCloakingExceed {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCloakingExceed")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCloakingExceed {}
#[automatically_derived]
impl ::core::clone::Clone for ScCloakingExceed {
    #[inline]
    fn clone(&self) -> ScCloakingExceed {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCloakingExceed {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCloakingExceed {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCloakingExceed {
    #[inline]
    fn eq(&self, other: &ScCloakingExceed) -> bool {
        true
    }
}
pub struct ScRollingCutter;
#[automatically_derived]
impl ::core::default::Default for ScRollingCutter {
    #[inline]
    fn default() -> ScRollingCutter {
        ScRollingCutter {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScRollingCutter {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScRollingCutter")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScRollingCutter {}
#[automatically_derived]
impl ::core::clone::Clone for ScRollingCutter {
    #[inline]
    fn clone(&self) -> ScRollingCutter {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScRollingCutter {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScRollingCutter {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScRollingCutter {
    #[inline]
    fn eq(&self, other: &ScRollingCutter) -> bool {
        true
    }
}
pub struct ScCrossImpact;
#[automatically_derived]
impl ::core::default::Default for ScCrossImpact {
    #[inline]
    fn default() -> ScCrossImpact {
        ScCrossImpact {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCrossImpact {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCrossImpact")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCrossImpact {}
#[automatically_derived]
impl ::core::clone::Clone for ScCrossImpact {
    #[inline]
    fn clone(&self) -> ScCrossImpact {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCrossImpact {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCrossImpact {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCrossImpact {
    #[inline]
    fn eq(&self, other: &ScCrossImpact) -> bool {
        true
    }
}
pub struct ScDarkClaw;
#[automatically_derived]
impl ::core::default::Default for ScDarkClaw {
    #[inline]
    fn default() -> ScDarkClaw {
        ScDarkClaw {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDarkClaw {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDarkClaw")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDarkClaw {}
#[automatically_derived]
impl ::core::clone::Clone for ScDarkClaw {
    #[inline]
    fn clone(&self) -> ScDarkClaw {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDarkClaw {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDarkClaw {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDarkClaw {
    #[inline]
    fn eq(&self, other: &ScDarkClaw) -> bool {
        true
    }
}
pub struct ScReproduce;
#[automatically_derived]
impl ::core::default::Default for ScReproduce {
    #[inline]
    fn default() -> ScReproduce {
        ScReproduce {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScReproduce {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScReproduce")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScReproduce {}
#[automatically_derived]
impl ::core::clone::Clone for ScReproduce {
    #[inline]
    fn clone(&self) -> ScReproduce {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScReproduce {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScReproduce {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScReproduce {
    #[inline]
    fn eq(&self, other: &ScReproduce) -> bool {
        true
    }
}
pub struct ScAutoShadowSpell;
#[automatically_derived]
impl ::core::default::Default for ScAutoShadowSpell {
    #[inline]
    fn default() -> ScAutoShadowSpell {
        ScAutoShadowSpell {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAutoShadowSpell {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAutoShadowSpell")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAutoShadowSpell {}
#[automatically_derived]
impl ::core::clone::Clone for ScAutoShadowSpell {
    #[inline]
    fn clone(&self) -> ScAutoShadowSpell {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAutoShadowSpell {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAutoShadowSpell {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAutoShadowSpell {
    #[inline]
    fn eq(&self, other: &ScAutoShadowSpell) -> bool {
        true
    }
}
pub struct ScShadowForm;
#[automatically_derived]
impl ::core::default::Default for ScShadowForm {
    #[inline]
    fn default() -> ScShadowForm {
        ScShadowForm {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScShadowForm {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScShadowForm")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScShadowForm {}
#[automatically_derived]
impl ::core::clone::Clone for ScShadowForm {
    #[inline]
    fn clone(&self) -> ScShadowForm {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScShadowForm {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScShadowForm {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScShadowForm {
    #[inline]
    fn eq(&self, other: &ScShadowForm) -> bool {
        true
    }
}
pub struct ScFatalMenace;
#[automatically_derived]
impl ::core::default::Default for ScFatalMenace {
    #[inline]
    fn default() -> ScFatalMenace {
        ScFatalMenace {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFatalMenace {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFatalMenace")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFatalMenace {}
#[automatically_derived]
impl ::core::clone::Clone for ScFatalMenace {
    #[inline]
    fn clone(&self) -> ScFatalMenace {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFatalMenace {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFatalMenace {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFatalMenace {
    #[inline]
    fn eq(&self, other: &ScFatalMenace) -> bool {
        true
    }
}
pub struct ScReadingSpellBook;
#[automatically_derived]
impl ::core::default::Default for ScReadingSpellBook {
    #[inline]
    fn default() -> ScReadingSpellBook {
        ScReadingSpellBook {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScReadingSpellBook {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScReadingSpellBook")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScReadingSpellBook {}
#[automatically_derived]
impl ::core::clone::Clone for ScReadingSpellBook {
    #[inline]
    fn clone(&self) -> ScReadingSpellBook {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScReadingSpellBook {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScReadingSpellBook {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScReadingSpellBook {
    #[inline]
    fn eq(&self, other: &ScReadingSpellBook) -> bool {
        true
    }
}
pub struct ScFreezingSpell;
#[automatically_derived]
impl ::core::default::Default for ScFreezingSpell {
    #[inline]
    fn default() -> ScFreezingSpell {
        ScFreezingSpell {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFreezingSpell {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFreezingSpell")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFreezingSpell {}
#[automatically_derived]
impl ::core::clone::Clone for ScFreezingSpell {
    #[inline]
    fn clone(&self) -> ScFreezingSpell {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFreezingSpell {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFreezingSpell {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFreezingSpell {
    #[inline]
    fn eq(&self, other: &ScFreezingSpell) -> bool {
        true
    }
}
pub struct ScSummonBall;
#[automatically_derived]
impl ::core::default::Default for ScSummonBall {
    #[inline]
    fn default() -> ScSummonBall {
        ScSummonBall {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSummonBall {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSummonBall")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSummonBall {}
#[automatically_derived]
impl ::core::clone::Clone for ScSummonBall {
    #[inline]
    fn clone(&self) -> ScSummonBall {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSummonBall {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSummonBall {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSummonBall {
    #[inline]
    fn eq(&self, other: &ScSummonBall) -> bool {
        true
    }
}
pub struct ScGentleTouch;
#[automatically_derived]
impl ::core::default::Default for ScGentleTouch {
    #[inline]
    fn default() -> ScGentleTouch {
        ScGentleTouch {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScGentleTouch {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScGentleTouch")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScGentleTouch {}
#[automatically_derived]
impl ::core::clone::Clone for ScGentleTouch {
    #[inline]
    fn clone(&self) -> ScGentleTouch {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScGentleTouch {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScGentleTouch {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScGentleTouch {
    #[inline]
    fn eq(&self, other: &ScGentleTouch) -> bool {
        true
    }
}
pub struct ScRisingDragon;
#[automatically_derived]
impl ::core::default::Default for ScRisingDragon {
    #[inline]
    fn default() -> ScRisingDragon {
        ScRisingDragon {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScRisingDragon {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScRisingDragon")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScRisingDragon {}
#[automatically_derived]
impl ::core::clone::Clone for ScRisingDragon {
    #[inline]
    fn clone(&self) -> ScRisingDragon {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScRisingDragon {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScRisingDragon {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScRisingDragon {
    #[inline]
    fn eq(&self, other: &ScRisingDragon) -> bool {
        true
    }
}
pub struct ScWargRider;
#[automatically_derived]
impl ::core::default::Default for ScWargRider {
    #[inline]
    fn default() -> ScWargRider {
        ScWargRider {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScWargRider {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScWargRider")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScWargRider {}
#[automatically_derived]
impl ::core::clone::Clone for ScWargRider {
    #[inline]
    fn clone(&self) -> ScWargRider {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScWargRider {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScWargRider {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScWargRider {
    #[inline]
    fn eq(&self, other: &ScWargRider) -> bool {
        true
    }
}
pub struct ScFearBreeze;
#[automatically_derived]
impl ::core::default::Default for ScFearBreeze {
    #[inline]
    fn default() -> ScFearBreeze {
        ScFearBreeze {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFearBreeze {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFearBreeze")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFearBreeze {}
#[automatically_derived]
impl ::core::clone::Clone for ScFearBreeze {
    #[inline]
    fn clone(&self) -> ScFearBreeze {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFearBreeze {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFearBreeze {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFearBreeze {
    #[inline]
    fn eq(&self, other: &ScFearBreeze) -> bool {
        true
    }
}
pub struct ScAimedBolt;
#[automatically_derived]
impl ::core::default::Default for ScAimedBolt {
    #[inline]
    fn default() -> ScAimedBolt {
        ScAimedBolt {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAimedBolt {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAimedBolt")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAimedBolt {}
#[automatically_derived]
impl ::core::clone::Clone for ScAimedBolt {
    #[inline]
    fn clone(&self) -> ScAimedBolt {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAimedBolt {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAimedBolt {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAimedBolt {
    #[inline]
    fn eq(&self, other: &ScAimedBolt) -> bool {
        true
    }
}
pub struct ScSongOfLutie;
#[automatically_derived]
impl ::core::default::Default for ScSongOfLutie {
    #[inline]
    fn default() -> ScSongOfLutie {
        ScSongOfLutie {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSongOfLutie {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSongOfLutie")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSongOfLutie {}
#[automatically_derived]
impl ::core::clone::Clone for ScSongOfLutie {
    #[inline]
    fn clone(&self) -> ScSongOfLutie {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSongOfLutie {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSongOfLutie {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSongOfLutie {
    #[inline]
    fn eq(&self, other: &ScSongOfLutie) -> bool {
        true
    }
}
pub struct ScDrumOnTheBattlefield;
#[automatically_derived]
impl ::core::default::Default for ScDrumOnTheBattlefield {
    #[inline]
    fn default() -> ScDrumOnTheBattlefield {
        ScDrumOnTheBattlefield {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDrumOnTheBattlefield {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDrumOnTheBattlefield")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDrumOnTheBattlefield {}
#[automatically_derived]
impl ::core::clone::Clone for ScDrumOnTheBattlefield {
    #[inline]
    fn clone(&self) -> ScDrumOnTheBattlefield {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDrumOnTheBattlefield {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDrumOnTheBattlefield {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDrumOnTheBattlefield {
    #[inline]
    fn eq(&self, other: &ScDrumOnTheBattlefield) -> bool {
        true
    }
}
pub struct ScBlessing;
#[automatically_derived]
impl ::core::default::Default for ScBlessing {
    #[inline]
    fn default() -> ScBlessing {
        ScBlessing {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBlessing {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBlessing")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBlessing {}
#[automatically_derived]
impl ::core::clone::Clone for ScBlessing {
    #[inline]
    fn clone(&self) -> ScBlessing {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBlessing {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBlessing {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBlessing {
    #[inline]
    fn eq(&self, other: &ScBlessing) -> bool {
        true
    }
}
pub struct ScFoodStr;
#[automatically_derived]
impl ::core::default::Default for ScFoodStr {
    #[inline]
    fn default() -> ScFoodStr {
        ScFoodStr {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodStr {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodStr")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodStr {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodStr {
    #[inline]
    fn clone(&self) -> ScFoodStr {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodStr {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodStr {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodStr {
    #[inline]
    fn eq(&self, other: &ScFoodStr) -> bool {
        true
    }
}
pub struct ScFoodAgi;
#[automatically_derived]
impl ::core::default::Default for ScFoodAgi {
    #[inline]
    fn default() -> ScFoodAgi {
        ScFoodAgi {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodAgi {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodAgi")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodAgi {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodAgi {
    #[inline]
    fn clone(&self) -> ScFoodAgi {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodAgi {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodAgi {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodAgi {
    #[inline]
    fn eq(&self, other: &ScFoodAgi) -> bool {
        true
    }
}
pub struct ScFoodVit;
#[automatically_derived]
impl ::core::default::Default for ScFoodVit {
    #[inline]
    fn default() -> ScFoodVit {
        ScFoodVit {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodVit {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodVit")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodVit {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodVit {
    #[inline]
    fn clone(&self) -> ScFoodVit {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodVit {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodVit {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodVit {
    #[inline]
    fn eq(&self, other: &ScFoodVit) -> bool {
        true
    }
}
pub struct ScFoodInt;
#[automatically_derived]
impl ::core::default::Default for ScFoodInt {
    #[inline]
    fn default() -> ScFoodInt {
        ScFoodInt {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodInt {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodInt")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodInt {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodInt {
    #[inline]
    fn clone(&self) -> ScFoodInt {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodInt {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodInt {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodInt {
    #[inline]
    fn eq(&self, other: &ScFoodInt) -> bool {
        true
    }
}
pub struct ScFoodDex;
#[automatically_derived]
impl ::core::default::Default for ScFoodDex {
    #[inline]
    fn default() -> ScFoodDex {
        ScFoodDex {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodDex {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodDex")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodDex {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodDex {
    #[inline]
    fn clone(&self) -> ScFoodDex {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodDex {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodDex {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodDex {
    #[inline]
    fn eq(&self, other: &ScFoodDex) -> bool {
        true
    }
}
pub struct ScFoodLuk;
#[automatically_derived]
impl ::core::default::Default for ScFoodLuk {
    #[inline]
    fn default() -> ScFoodLuk {
        ScFoodLuk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFoodLuk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFoodLuk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFoodLuk {}
#[automatically_derived]
impl ::core::clone::Clone for ScFoodLuk {
    #[inline]
    fn clone(&self) -> ScFoodLuk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFoodLuk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFoodLuk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFoodLuk {
    #[inline]
    fn eq(&self, other: &ScFoodLuk) -> bool {
        true
    }
}
pub struct ScEndure;
#[automatically_derived]
impl ::core::default::Default for ScEndure {
    #[inline]
    fn default() -> ScEndure {
        ScEndure {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEndure {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEndure")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEndure {}
#[automatically_derived]
impl ::core::clone::Clone for ScEndure {
    #[inline]
    fn clone(&self) -> ScEndure {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEndure {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEndure {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEndure {
    #[inline]
    fn eq(&self, other: &ScEndure) -> bool {
        true
    }
}
pub struct ScAutoguard;
#[automatically_derived]
impl ::core::default::Default for ScAutoguard {
    #[inline]
    fn default() -> ScAutoguard {
        ScAutoguard {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAutoguard {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAutoguard")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAutoguard {}
#[automatically_derived]
impl ::core::clone::Clone for ScAutoguard {
    #[inline]
    fn clone(&self) -> ScAutoguard {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAutoguard {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAutoguard {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAutoguard {
    #[inline]
    fn eq(&self, other: &ScAutoguard) -> bool {
        true
    }
}
pub struct ScDefender;
#[automatically_derived]
impl ::core::default::Default for ScDefender {
    #[inline]
    fn default() -> ScDefender {
        ScDefender {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScDefender {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScDefender")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScDefender {}
#[automatically_derived]
impl ::core::clone::Clone for ScDefender {
    #[inline]
    fn clone(&self) -> ScDefender {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScDefender {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScDefender {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScDefender {
    #[inline]
    fn eq(&self, other: &ScDefender) -> bool {
        true
    }
}
pub struct ScReflectShield;
#[automatically_derived]
impl ::core::default::Default for ScReflectShield {
    #[inline]
    fn default() -> ScReflectShield {
        ScReflectShield {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScReflectShield {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScReflectShield")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScReflectShield {}
#[automatically_derived]
impl ::core::clone::Clone for ScReflectShield {
    #[inline]
    fn clone(&self) -> ScReflectShield {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScReflectShield {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScReflectShield {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScReflectShield {
    #[inline]
    fn eq(&self, other: &ScReflectShield) -> bool {
        true
    }
}
pub struct ScPrestige;
#[automatically_derived]
impl ::core::default::Default for ScPrestige {
    #[inline]
    fn default() -> ScPrestige {
        ScPrestige {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPrestige {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPrestige")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPrestige {}
#[automatically_derived]
impl ::core::clone::Clone for ScPrestige {
    #[inline]
    fn clone(&self) -> ScPrestige {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPrestige {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPrestige {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPrestige {
    #[inline]
    fn eq(&self, other: &ScPrestige) -> bool {
        true
    }
}
pub struct ScBanding;
#[automatically_derived]
impl ::core::default::Default for ScBanding {
    #[inline]
    fn default() -> ScBanding {
        ScBanding {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBanding {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBanding")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBanding {}
#[automatically_derived]
impl ::core::clone::Clone for ScBanding {
    #[inline]
    fn clone(&self) -> ScBanding {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBanding {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBanding {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBanding {
    #[inline]
    fn eq(&self, other: &ScBanding) -> bool {
        true
    }
}
pub struct ScLastStand;
#[automatically_derived]
impl ::core::default::Default for ScLastStand {
    #[inline]
    fn default() -> ScLastStand {
        ScLastStand {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScLastStand {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScLastStand")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScLastStand {}
#[automatically_derived]
impl ::core::clone::Clone for ScLastStand {
    #[inline]
    fn clone(&self) -> ScLastStand {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScLastStand {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScLastStand {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScLastStand {
    #[inline]
    fn eq(&self, other: &ScLastStand) -> bool {
        true
    }
}
pub struct ScGatlingFever;
#[automatically_derived]
impl ::core::default::Default for ScGatlingFever {
    #[inline]
    fn default() -> ScGatlingFever {
        ScGatlingFever {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScGatlingFever {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScGatlingFever")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScGatlingFever {}
#[automatically_derived]
impl ::core::clone::Clone for ScGatlingFever {
    #[inline]
    fn clone(&self) -> ScGatlingFever {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScGatlingFever {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScGatlingFever {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScGatlingFever {
    #[inline]
    fn eq(&self, other: &ScGatlingFever) -> bool {
        true
    }
}
pub struct ScHeatBarrel;
#[automatically_derived]
impl ::core::default::Default for ScHeatBarrel {
    #[inline]
    fn default() -> ScHeatBarrel {
        ScHeatBarrel {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScHeatBarrel {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScHeatBarrel")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScHeatBarrel {}
#[automatically_derived]
impl ::core::clone::Clone for ScHeatBarrel {
    #[inline]
    fn clone(&self) -> ScHeatBarrel {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScHeatBarrel {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScHeatBarrel {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScHeatBarrel {
    #[inline]
    fn eq(&self, other: &ScHeatBarrel) -> bool {
        true
    }
}
pub struct ScAntiMaterialBlast;
#[automatically_derived]
impl ::core::default::Default for ScAntiMaterialBlast {
    #[inline]
    fn default() -> ScAntiMaterialBlast {
        ScAntiMaterialBlast {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScAntiMaterialBlast {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScAntiMaterialBlast")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScAntiMaterialBlast {}
#[automatically_derived]
impl ::core::clone::Clone for ScAntiMaterialBlast {
    #[inline]
    fn clone(&self) -> ScAntiMaterialBlast {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScAntiMaterialBlast {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScAntiMaterialBlast {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScAntiMaterialBlast {
    #[inline]
    fn eq(&self, other: &ScAntiMaterialBlast) -> bool {
        true
    }
}
pub struct ScEternalChain;
#[automatically_derived]
impl ::core::default::Default for ScEternalChain {
    #[inline]
    fn default() -> ScEternalChain {
        ScEternalChain {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScEternalChain {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScEternalChain")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScEternalChain {}
#[automatically_derived]
impl ::core::clone::Clone for ScEternalChain {
    #[inline]
    fn clone(&self) -> ScEternalChain {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScEternalChain {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScEternalChain {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScEternalChain {
    #[inline]
    fn eq(&self, other: &ScEternalChain) -> bool {
        true
    }
}
pub struct ScShadowLeap;
#[automatically_derived]
impl ::core::default::Default for ScShadowLeap {
    #[inline]
    fn default() -> ScShadowLeap {
        ScShadowLeap {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScShadowLeap {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScShadowLeap")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScShadowLeap {}
#[automatically_derived]
impl ::core::clone::Clone for ScShadowLeap {
    #[inline]
    fn clone(&self) -> ScShadowLeap {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScShadowLeap {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScShadowLeap {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScShadowLeap {
    #[inline]
    fn eq(&self, other: &ScShadowLeap) -> bool {
        true
    }
}
pub struct ScMirrorImage;
#[automatically_derived]
impl ::core::default::Default for ScMirrorImage {
    #[inline]
    fn default() -> ScMirrorImage {
        ScMirrorImage {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMirrorImage {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMirrorImage")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMirrorImage {}
#[automatically_derived]
impl ::core::clone::Clone for ScMirrorImage {
    #[inline]
    fn clone(&self) -> ScMirrorImage {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMirrorImage {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMirrorImage {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMirrorImage {
    #[inline]
    fn eq(&self, other: &ScMirrorImage) -> bool {
        true
    }
}
pub struct ScKunaiExplosion;
#[automatically_derived]
impl ::core::default::Default for ScKunaiExplosion {
    #[inline]
    fn default() -> ScKunaiExplosion {
        ScKunaiExplosion {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScKunaiExplosion {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScKunaiExplosion")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScKunaiExplosion {}
#[automatically_derived]
impl ::core::clone::Clone for ScKunaiExplosion {
    #[inline]
    fn clone(&self) -> ScKunaiExplosion {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScKunaiExplosion {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScKunaiExplosion {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScKunaiExplosion {
    #[inline]
    fn eq(&self, other: &ScKunaiExplosion) -> bool {
        true
    }
}
pub struct ScSwirlingPetal;
#[automatically_derived]
impl ::core::default::Default for ScSwirlingPetal {
    #[inline]
    fn default() -> ScSwirlingPetal {
        ScSwirlingPetal {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSwirlingPetal {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSwirlingPetal")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSwirlingPetal {}
#[automatically_derived]
impl ::core::clone::Clone for ScSwirlingPetal {
    #[inline]
    fn clone(&self) -> ScSwirlingPetal {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSwirlingPetal {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSwirlingPetal {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSwirlingPetal {
    #[inline]
    fn eq(&self, other: &ScSwirlingPetal) -> bool {
        true
    }
}
pub struct ScCrossSlash;
#[automatically_derived]
impl ::core::default::Default for ScCrossSlash {
    #[inline]
    fn default() -> ScCrossSlash {
        ScCrossSlash {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCrossSlash {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCrossSlash")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCrossSlash {}
#[automatically_derived]
impl ::core::clone::Clone for ScCrossSlash {
    #[inline]
    fn clone(&self) -> ScCrossSlash {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCrossSlash {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCrossSlash {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCrossSlash {
    #[inline]
    fn eq(&self, other: &ScCrossSlash) -> bool {
        true
    }
}
pub struct ScCatnipMeteor;
#[automatically_derived]
impl ::core::default::Default for ScCatnipMeteor {
    #[inline]
    fn default() -> ScCatnipMeteor {
        ScCatnipMeteor {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCatnipMeteor {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCatnipMeteor")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCatnipMeteor {}
#[automatically_derived]
impl ::core::clone::Clone for ScCatnipMeteor {
    #[inline]
    fn clone(&self) -> ScCatnipMeteor {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCatnipMeteor {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCatnipMeteor {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCatnipMeteor {
    #[inline]
    fn eq(&self, other: &ScCatnipMeteor) -> bool {
        true
    }
}
pub struct ScPickyPeck;
#[automatically_derived]
impl ::core::default::Default for ScPickyPeck {
    #[inline]
    fn default() -> ScPickyPeck {
        ScPickyPeck {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPickyPeck {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPickyPeck")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPickyPeck {}
#[automatically_derived]
impl ::core::clone::Clone for ScPickyPeck {
    #[inline]
    fn clone(&self) -> ScPickyPeck {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPickyPeck {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPickyPeck {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPickyPeck {
    #[inline]
    fn eq(&self, other: &ScPickyPeck) -> bool {
        true
    }
}
pub struct ScScarOfTarou;
#[automatically_derived]
impl ::core::default::Default for ScScarOfTarou {
    #[inline]
    fn default() -> ScScarOfTarou {
        ScScarOfTarou {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScScarOfTarou {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScScarOfTarou")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScScarOfTarou {}
#[automatically_derived]
impl ::core::clone::Clone for ScScarOfTarou {
    #[inline]
    fn clone(&self) -> ScScarOfTarou {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScScarOfTarou {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScScarOfTarou {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScScarOfTarou {
    #[inline]
    fn eq(&self, other: &ScScarOfTarou) -> bool {
        true
    }
}
pub struct ScSolarBurst;
#[automatically_derived]
impl ::core::default::Default for ScSolarBurst {
    #[inline]
    fn default() -> ScSolarBurst {
        ScSolarBurst {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSolarBurst {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSolarBurst")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSolarBurst {}
#[automatically_derived]
impl ::core::clone::Clone for ScSolarBurst {
    #[inline]
    fn clone(&self) -> ScSolarBurst {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSolarBurst {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSolarBurst {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSolarBurst {
    #[inline]
    fn eq(&self, other: &ScSolarBurst) -> bool {
        true
    }
}
pub struct ScFullMoonKick;
#[automatically_derived]
impl ::core::default::Default for ScFullMoonKick {
    #[inline]
    fn default() -> ScFullMoonKick {
        ScFullMoonKick {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFullMoonKick {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFullMoonKick")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFullMoonKick {}
#[automatically_derived]
impl ::core::clone::Clone for ScFullMoonKick {
    #[inline]
    fn clone(&self) -> ScFullMoonKick {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFullMoonKick {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFullMoonKick {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFullMoonKick {
    #[inline]
    fn eq(&self, other: &ScFullMoonKick) -> bool {
        true
    }
}
pub struct ScFallingStar;
#[automatically_derived]
impl ::core::default::Default for ScFallingStar {
    #[inline]
    fn default() -> ScFallingStar {
        ScFallingStar {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFallingStar {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFallingStar")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFallingStar {}
#[automatically_derived]
impl ::core::clone::Clone for ScFallingStar {
    #[inline]
    fn clone(&self) -> ScFallingStar {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFallingStar {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFallingStar {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFallingStar {
    #[inline]
    fn eq(&self, other: &ScFallingStar) -> bool {
        true
    }
}
pub struct ScSoulReap;
#[automatically_derived]
impl ::core::default::Default for ScSoulReap {
    #[inline]
    fn default() -> ScSoulReap {
        ScSoulReap {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSoulReap {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSoulReap")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSoulReap {}
#[automatically_derived]
impl ::core::clone::Clone for ScSoulReap {
    #[inline]
    fn clone(&self) -> ScSoulReap {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSoulReap {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSoulReap {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSoulReap {
    #[inline]
    fn eq(&self, other: &ScSoulReap) -> bool {
        true
    }
}
pub struct ScCurseOfSoul;
#[automatically_derived]
impl ::core::default::Default for ScCurseOfSoul {
    #[inline]
    fn default() -> ScCurseOfSoul {
        ScCurseOfSoul {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScCurseOfSoul {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScCurseOfSoul")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScCurseOfSoul {}
#[automatically_derived]
impl ::core::clone::Clone for ScCurseOfSoul {
    #[inline]
    fn clone(&self) -> ScCurseOfSoul {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScCurseOfSoul {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScCurseOfSoul {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScCurseOfSoul {
    #[inline]
    fn eq(&self, other: &ScCurseOfSoul) -> bool {
        true
    }
}
pub struct ScSoulUnity;
#[automatically_derived]
impl ::core::default::Default for ScSoulUnity {
    #[inline]
    fn default() -> ScSoulUnity {
        ScSoulUnity {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSoulUnity {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSoulUnity")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSoulUnity {}
#[automatically_derived]
impl ::core::clone::Clone for ScSoulUnity {
    #[inline]
    fn clone(&self) -> ScSoulUnity {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSoulUnity {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSoulUnity {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSoulUnity {
    #[inline]
    fn eq(&self, other: &ScSoulUnity) -> bool {
        true
    }
}
pub struct ScPalletPaint;
#[automatically_derived]
impl ::core::default::Default for ScPalletPaint {
    #[inline]
    fn default() -> ScPalletPaint {
        ScPalletPaint {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPalletPaint {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPalletPaint")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPalletPaint {}
#[automatically_derived]
impl ::core::clone::Clone for ScPalletPaint {
    #[inline]
    fn clone(&self) -> ScPalletPaint {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPalletPaint {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPalletPaint {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPalletPaint {
    #[inline]
    fn eq(&self, other: &ScPalletPaint) -> bool {
        true
    }
}
pub struct ScPyrotechnic;
#[automatically_derived]
impl ::core::default::Default for ScPyrotechnic {
    #[inline]
    fn default() -> ScPyrotechnic {
        ScPyrotechnic {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScPyrotechnic {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScPyrotechnic")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScPyrotechnic {}
#[automatically_derived]
impl ::core::clone::Clone for ScPyrotechnic {
    #[inline]
    fn clone(&self) -> ScPyrotechnic {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScPyrotechnic {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScPyrotechnic {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScPyrotechnic {
    #[inline]
    fn eq(&self, other: &ScPyrotechnic) -> bool {
        true
    }
}
pub struct ScThornTrap;
#[automatically_derived]
impl ::core::default::Default for ScThornTrap {
    #[inline]
    fn default() -> ScThornTrap {
        ScThornTrap {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScThornTrap {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScThornTrap")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScThornTrap {}
#[automatically_derived]
impl ::core::clone::Clone for ScThornTrap {
    #[inline]
    fn clone(&self) -> ScThornTrap {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScThornTrap {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScThornTrap {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScThornTrap {
    #[inline]
    fn eq(&self, other: &ScThornTrap) -> bool {
        true
    }
}
pub struct ScBloodSucker;
#[automatically_derived]
impl ::core::default::Default for ScBloodSucker {
    #[inline]
    fn default() -> ScBloodSucker {
        ScBloodSucker {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScBloodSucker {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScBloodSucker")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScBloodSucker {}
#[automatically_derived]
impl ::core::clone::Clone for ScBloodSucker {
    #[inline]
    fn clone(&self) -> ScBloodSucker {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScBloodSucker {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScBloodSucker {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScBloodSucker {
    #[inline]
    fn eq(&self, other: &ScBloodSucker) -> bool {
        true
    }
}
pub struct ScSporeExplosion;
#[automatically_derived]
impl ::core::default::Default for ScSporeExplosion {
    #[inline]
    fn default() -> ScSporeExplosion {
        ScSporeExplosion {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSporeExplosion {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSporeExplosion")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSporeExplosion {}
#[automatically_derived]
impl ::core::clone::Clone for ScSporeExplosion {
    #[inline]
    fn clone(&self) -> ScSporeExplosion {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSporeExplosion {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSporeExplosion {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSporeExplosion {
    #[inline]
    fn eq(&self, other: &ScSporeExplosion) -> bool {
        true
    }
}
pub struct ScMasquerade;
#[automatically_derived]
impl ::core::default::Default for ScMasquerade {
    #[inline]
    fn default() -> ScMasquerade {
        ScMasquerade {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMasquerade {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMasquerade")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMasquerade {}
#[automatically_derived]
impl ::core::clone::Clone for ScMasquerade {
    #[inline]
    fn clone(&self) -> ScMasquerade {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMasquerade {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMasquerade {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMasquerade {
    #[inline]
    fn eq(&self, other: &ScMasquerade) -> bool {
        true
    }
}
pub struct ScFullThrottle;
#[automatically_derived]
impl ::core::default::Default for ScFullThrottle {
    #[inline]
    fn default() -> ScFullThrottle {
        ScFullThrottle {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScFullThrottle {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScFullThrottle")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScFullThrottle {}
#[automatically_derived]
impl ::core::clone::Clone for ScFullThrottle {
    #[inline]
    fn clone(&self) -> ScFullThrottle {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScFullThrottle {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScFullThrottle {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScFullThrottle {
    #[inline]
    fn eq(&self, other: &ScFullThrottle) -> bool {
        true
    }
}
pub struct ScReboundShield;
#[automatically_derived]
impl ::core::default::Default for ScReboundShield {
    #[inline]
    fn default() -> ScReboundShield {
        ScReboundShield {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScReboundShield {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScReboundShield")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScReboundShield {}
#[automatically_derived]
impl ::core::clone::Clone for ScReboundShield {
    #[inline]
    fn clone(&self) -> ScReboundShield {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScReboundShield {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScReboundShield {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScReboundShield {
    #[inline]
    fn eq(&self, other: &ScReboundShield) -> bool {
        true
    }
}
pub struct ScStripAccessory;
#[automatically_derived]
impl ::core::default::Default for ScStripAccessory {
    #[inline]
    fn default() -> ScStripAccessory {
        ScStripAccessory {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScStripAccessory {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScStripAccessory")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScStripAccessory {}
#[automatically_derived]
impl ::core::clone::Clone for ScStripAccessory {
    #[inline]
    fn clone(&self) -> ScStripAccessory {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScStripAccessory {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScStripAccessory {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScStripAccessory {
    #[inline]
    fn eq(&self, other: &ScStripAccessory) -> bool {
        true
    }
}
pub struct ScInvisibility;
#[automatically_derived]
impl ::core::default::Default for ScInvisibility {
    #[inline]
    fn default() -> ScInvisibility {
        ScInvisibility {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScInvisibility {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScInvisibility")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScInvisibility {}
#[automatically_derived]
impl ::core::clone::Clone for ScInvisibility {
    #[inline]
    fn clone(&self) -> ScInvisibility {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScInvisibility {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScInvisibility {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScInvisibility {
    #[inline]
    fn eq(&self, other: &ScInvisibility) -> bool {
        true
    }
}
pub struct ScMagicalBullet;
#[automatically_derived]
impl ::core::default::Default for ScMagicalBullet {
    #[inline]
    fn default() -> ScMagicalBullet {
        ScMagicalBullet {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScMagicalBullet {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScMagicalBullet")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScMagicalBullet {}
#[automatically_derived]
impl ::core::clone::Clone for ScMagicalBullet {
    #[inline]
    fn clone(&self) -> ScMagicalBullet {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScMagicalBullet {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScMagicalBullet {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScMagicalBullet {
    #[inline]
    fn eq(&self, other: &ScMagicalBullet) -> bool {
        true
    }
}
pub struct ScManuAtk;
#[automatically_derived]
impl ::core::default::Default for ScManuAtk {
    #[inline]
    fn default() -> ScManuAtk {
        ScManuAtk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScManuAtk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScManuAtk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScManuAtk {}
#[automatically_derived]
impl ::core::clone::Clone for ScManuAtk {
    #[inline]
    fn clone(&self) -> ScManuAtk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScManuAtk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScManuAtk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScManuAtk {
    #[inline]
    fn eq(&self, other: &ScManuAtk) -> bool {
        true
    }
}
pub struct ScManuDef;
#[automatically_derived]
impl ::core::default::Default for ScManuDef {
    #[inline]
    fn default() -> ScManuDef {
        ScManuDef {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScManuDef {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScManuDef")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScManuDef {}
#[automatically_derived]
impl ::core::clone::Clone for ScManuDef {
    #[inline]
    fn clone(&self) -> ScManuDef {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScManuDef {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScManuDef {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScManuDef {
    #[inline]
    fn eq(&self, other: &ScManuDef) -> bool {
        true
    }
}
pub struct ScSplAtk;
#[automatically_derived]
impl ::core::default::Default for ScSplAtk {
    #[inline]
    fn default() -> ScSplAtk {
        ScSplAtk {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSplAtk {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSplAtk")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSplAtk {}
#[automatically_derived]
impl ::core::clone::Clone for ScSplAtk {
    #[inline]
    fn clone(&self) -> ScSplAtk {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSplAtk {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSplAtk {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSplAtk {
    #[inline]
    fn eq(&self, other: &ScSplAtk) -> bool {
        true
    }
}
pub struct ScSplDef;
#[automatically_derived]
impl ::core::default::Default for ScSplDef {
    #[inline]
    fn default() -> ScSplDef {
        ScSplDef {}
    }
}
#[automatically_derived]
impl ::core::fmt::Debug for ScSplDef {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(f, "ScSplDef")
    }
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ScSplDef {}
#[automatically_derived]
impl ::core::clone::Clone for ScSplDef {
    #[inline]
    fn clone(&self) -> ScSplDef {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ScSplDef {}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ScSplDef {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ScSplDef {
    #[inline]
    fn eq(&self, other: &ScSplDef) -> bool {
        true
    }
}
#[repr(u8)]
pub enum ArchId {
    #[allow(nonstandard_style)]
    Player,
    #[allow(nonstandard_style)]
    MonsterNormal,
    #[allow(nonstandard_style)]
    MonsterBoss,
    #[allow(nonstandard_style)]
    Homunculus,
    #[allow(nonstandard_style)]
    Mercenary,
    #[allow(nonstandard_style)]
    Pet,
    #[allow(nonstandard_style)]
    Elemental,
    #[allow(nonstandard_style)]
    WugRider,
    #[allow(nonstandard_style)]
    MadogearUser,
    #[allow(nonstandard_style)]
    GeneticCart,
    #[allow(nonstandard_style)]
    RuneKnight,
    #[allow(nonstandard_style)]
    RoyalGuard,
    #[allow(nonstandard_style)]
    Mechanic,
    #[allow(nonstandard_style)]
    GuillotineCross,
    #[allow(nonstandard_style)]
    ShadowChaser,
    #[allow(nonstandard_style)]
    Sorcerer,
    #[allow(nonstandard_style)]
    Warlock,
    #[allow(nonstandard_style)]
    Archbishop,
    #[allow(nonstandard_style)]
    Sura,
    #[allow(nonstandard_style)]
    Ranger,
    #[allow(nonstandard_style)]
    MinstrelWanderer,
    #[allow(nonstandard_style)]
    SuperNovice,
    #[allow(nonstandard_style)]
    Gunslinger,
    #[allow(nonstandard_style)]
    Ninja,
    #[allow(nonstandard_style)]
    KagerouOboro,
    #[allow(nonstandard_style)]
    Rebellion,
    #[allow(nonstandard_style)]
    Summoner,
    #[allow(nonstandard_style)]
    StarEmperor,
    #[allow(nonstandard_style)]
    SoulReaper,
    #[allow(nonstandard_style)]
    Dummy,
}
#[automatically_derived]
#[doc(hidden)]
unsafe impl ::core::clone::TrivialClone for ArchId {}
#[automatically_derived]
impl ::core::clone::Clone for ArchId {
    #[inline]
    fn clone(&self) -> ArchId {
        *self
    }
}
#[automatically_derived]
impl ::core::marker::Copy for ArchId {}
#[automatically_derived]
impl ::core::fmt::Debug for ArchId {
    #[inline]
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        ::core::fmt::Formatter::write_str(
            f,
            match self {
                ArchId::Player => "Player",
                ArchId::MonsterNormal => "MonsterNormal",
                ArchId::MonsterBoss => "MonsterBoss",
                ArchId::Homunculus => "Homunculus",
                ArchId::Mercenary => "Mercenary",
                ArchId::Pet => "Pet",
                ArchId::Elemental => "Elemental",
                ArchId::WugRider => "WugRider",
                ArchId::MadogearUser => "MadogearUser",
                ArchId::GeneticCart => "GeneticCart",
                ArchId::RuneKnight => "RuneKnight",
                ArchId::RoyalGuard => "RoyalGuard",
                ArchId::Mechanic => "Mechanic",
                ArchId::GuillotineCross => "GuillotineCross",
                ArchId::ShadowChaser => "ShadowChaser",
                ArchId::Sorcerer => "Sorcerer",
                ArchId::Warlock => "Warlock",
                ArchId::Archbishop => "Archbishop",
                ArchId::Sura => "Sura",
                ArchId::Ranger => "Ranger",
                ArchId::MinstrelWanderer => "MinstrelWanderer",
                ArchId::SuperNovice => "SuperNovice",
                ArchId::Gunslinger => "Gunslinger",
                ArchId::Ninja => "Ninja",
                ArchId::KagerouOboro => "KagerouOboro",
                ArchId::Rebellion => "Rebellion",
                ArchId::Summoner => "Summoner",
                ArchId::StarEmperor => "StarEmperor",
                ArchId::SoulReaper => "SoulReaper",
                ArchId::Dummy => "Dummy",
            },
        )
    }
}
#[automatically_derived]
impl ::core::marker::StructuralPartialEq for ArchId {}
#[automatically_derived]
impl ::core::cmp::PartialEq for ArchId {
    #[inline]
    fn eq(&self, other: &ArchId) -> bool {
        let __self_discr = ::core::intrinsics::discriminant_value(self);
        let __arg1_discr = ::core::intrinsics::discriminant_value(other);
        __self_discr == __arg1_discr
    }
}
#[automatically_derived]
impl ::core::cmp::Eq for ArchId {
    #[inline]
    #[doc(hidden)]
    #[coverage(off)]
    fn assert_receiver_is_total_eq(&self) -> () {}
}
#[automatically_derived]
impl ::core::hash::Hash for ArchId {
    #[inline]
    fn hash<__H: ::core::hash::Hasher>(&self, state: &mut __H) -> () {
        let __self_discr = ::core::intrinsics::discriminant_value(self);
        ::core::hash::Hash::hash(&__self_discr, state)
    }
}
impl ArchId {
    #[inline]
    pub fn as_u8(self) -> u8 {
        self as u8
    }
}
pub const ARCH_COUNT: usize = 30;
#[allow(nonstandard_style)]
pub enum ArchEntityRefs {
    #[allow(nonstandard_style)]
    Player(PlayerEntityRefs),
    #[allow(nonstandard_style)]
    MonsterNormal(MonsterNormalEntityRefs),
    #[allow(nonstandard_style)]
    MonsterBoss(MonsterBossEntityRefs),
    #[allow(nonstandard_style)]
    Homunculus(HomunculusEntityRefs),
    #[allow(nonstandard_style)]
    Mercenary(MercenaryEntityRefs),
    #[allow(nonstandard_style)]
    Pet(PetEntityRefs),
    #[allow(nonstandard_style)]
    Elemental(ElementalEntityRefs),
    #[allow(nonstandard_style)]
    WugRider(WugRiderEntityRefs),
    #[allow(nonstandard_style)]
    MadogearUser(MadogearUserEntityRefs),
    #[allow(nonstandard_style)]
    GeneticCart(GeneticCartEntityRefs),
    #[allow(nonstandard_style)]
    RuneKnight(RuneKnightEntityRefs),
    #[allow(nonstandard_style)]
    RoyalGuard(RoyalGuardEntityRefs),
    #[allow(nonstandard_style)]
    Mechanic(MechanicEntityRefs),
    #[allow(nonstandard_style)]
    GuillotineCross(GuillotineCrossEntityRefs),
    #[allow(nonstandard_style)]
    ShadowChaser(ShadowChaserEntityRefs),
    #[allow(nonstandard_style)]
    Sorcerer(SorcererEntityRefs),
    #[allow(nonstandard_style)]
    Warlock(WarlockEntityRefs),
    #[allow(nonstandard_style)]
    Archbishop(ArchbishopEntityRefs),
    #[allow(nonstandard_style)]
    Sura(SuraEntityRefs),
    #[allow(nonstandard_style)]
    Ranger(RangerEntityRefs),
    #[allow(nonstandard_style)]
    MinstrelWanderer(MinstrelWandererEntityRefs),
    #[allow(nonstandard_style)]
    SuperNovice(SuperNoviceEntityRefs),
    #[allow(nonstandard_style)]
    Gunslinger(GunslingerEntityRefs),
    #[allow(nonstandard_style)]
    Ninja(NinjaEntityRefs),
    #[allow(nonstandard_style)]
    KagerouOboro(KagerouOboroEntityRefs),
    #[allow(nonstandard_style)]
    Rebellion(RebellionEntityRefs),
    #[allow(nonstandard_style)]
    Summoner(SummonerEntityRefs),
    #[allow(nonstandard_style)]
    StarEmperor(StarEmperorEntityRefs),
    #[allow(nonstandard_style)]
    SoulReaper(SoulReaperEntityRefs),
    #[allow(nonstandard_style)]
    Dummy(DummyEntityRefs),
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for ArchEntityRefs {
    #[inline]
    fn clone(&self) -> ArchEntityRefs {
        match self {
            ArchEntityRefs::Player(__self_0) => {
                ArchEntityRefs::Player(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::MonsterNormal(__self_0) => {
                ArchEntityRefs::MonsterNormal(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::MonsterBoss(__self_0) => {
                ArchEntityRefs::MonsterBoss(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Homunculus(__self_0) => {
                ArchEntityRefs::Homunculus(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Mercenary(__self_0) => {
                ArchEntityRefs::Mercenary(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Pet(__self_0) => {
                ArchEntityRefs::Pet(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Elemental(__self_0) => {
                ArchEntityRefs::Elemental(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::WugRider(__self_0) => {
                ArchEntityRefs::WugRider(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::MadogearUser(__self_0) => {
                ArchEntityRefs::MadogearUser(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::GeneticCart(__self_0) => {
                ArchEntityRefs::GeneticCart(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::RuneKnight(__self_0) => {
                ArchEntityRefs::RuneKnight(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::RoyalGuard(__self_0) => {
                ArchEntityRefs::RoyalGuard(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Mechanic(__self_0) => {
                ArchEntityRefs::Mechanic(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::GuillotineCross(__self_0) => {
                ArchEntityRefs::GuillotineCross(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::ShadowChaser(__self_0) => {
                ArchEntityRefs::ShadowChaser(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Sorcerer(__self_0) => {
                ArchEntityRefs::Sorcerer(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Warlock(__self_0) => {
                ArchEntityRefs::Warlock(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Archbishop(__self_0) => {
                ArchEntityRefs::Archbishop(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Sura(__self_0) => {
                ArchEntityRefs::Sura(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Ranger(__self_0) => {
                ArchEntityRefs::Ranger(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::MinstrelWanderer(__self_0) => {
                ArchEntityRefs::MinstrelWanderer(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::SuperNovice(__self_0) => {
                ArchEntityRefs::SuperNovice(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Gunslinger(__self_0) => {
                ArchEntityRefs::Gunslinger(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Ninja(__self_0) => {
                ArchEntityRefs::Ninja(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::KagerouOboro(__self_0) => {
                ArchEntityRefs::KagerouOboro(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Rebellion(__self_0) => {
                ArchEntityRefs::Rebellion(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Summoner(__self_0) => {
                ArchEntityRefs::Summoner(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::StarEmperor(__self_0) => {
                ArchEntityRefs::StarEmperor(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::SoulReaper(__self_0) => {
                ArchEntityRefs::SoulReaper(::core::clone::Clone::clone(__self_0))
            }
            ArchEntityRefs::Dummy(__self_0) => {
                ArchEntityRefs::Dummy(::core::clone::Clone::clone(__self_0))
            }
        }
    }
}
pub trait AsArchEntityEnumRef {
    fn as_arch_ref(&self) -> ArchEntityRefs;
}
impl AsArchEntityEnumRef for ArchEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        self.clone()
    }
}
pub struct PlayerComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl PlayerComponentStorage {
    pub const N: usize = 11;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<Health>(), std::mem::align_of::<Health>()),
            (std::mem::size_of::<ScBerserk>(), std::mem::align_of::<ScBerserk>()),
            (std::mem::size_of::<ScQuicken>(), std::mem::align_of::<ScQuicken>()),
            (std::mem::size_of::<ScAngelus>(), std::mem::align_of::<ScAngelus>()),
            (std::mem::size_of::<ScAdrenaline>(), std::mem::align_of::<ScAdrenaline>()),
            (
                std::mem::size_of::<ScInspiration>(),
                std::mem::align_of::<ScInspiration>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        Health,
        ScBerserk,
        ScQuicken,
        ScAngelus,
        ScAdrenaline,
        ScInspiration,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScBerserk;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScQuicken;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScAngelus;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScAdrenaline;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScInspiration;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Health>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut Health;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBerserk>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScBerserk;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScQuicken>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScQuicken;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAngelus>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScAngelus;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAdrenaline>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut ScAdrenaline;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScInspiration>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut ScInspiration;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[9];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[10];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScBerserk: ScBerserk,
        ScQuicken: ScQuicken,
        ScAngelus: ScAngelus,
        ScAdrenaline: ScAdrenaline,
        ScInspiration: ScInspiration,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut Health;
            type_base.add(self.len).write(Health);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScBerserk;
            type_base.add(self.len).write(ScBerserk);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScQuicken;
            type_base.add(self.len).write(ScQuicken);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScAngelus;
            type_base.add(self.len).write(ScAngelus);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut ScAdrenaline;
            type_base.add(self.len).write(ScAdrenaline);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut ScInspiration;
            type_base.add(self.len).write(ScInspiration);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[9];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[10];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[Health],
        &[ScBerserk],
        &[ScQuicken],
        &[ScAngelus],
        &[ScAdrenaline],
        &[ScInspiration],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const Health,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScBerserk,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScQuicken,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScAngelus,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const ScAdrenaline,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const ScInspiration,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[9]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[10]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [Health],
        &mut [ScBerserk],
        &mut [ScQuicken],
        &mut [ScAngelus],
        &mut [ScAdrenaline],
        &mut [ScInspiration],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut Health,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScBerserk,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScQuicken,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScAngelus,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut ScAdrenaline,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut ScInspiration,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[9]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[10]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBerserk(&self) -> &[ScBerserk] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBerserk,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScQuicken(&self) -> &[ScQuicken] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScQuicken,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAngelus(&self) -> &[ScAngelus] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAngelus,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAdrenaline(&self) -> &[ScAdrenaline] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAdrenaline,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScInspiration(&self) -> &[ScInspiration] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScInspiration,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health_mut(&self) -> &mut [Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBerserk_mut(&self) -> &mut [ScBerserk] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBerserk,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScQuicken_mut(&self) -> &mut [ScQuicken] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScQuicken,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAngelus_mut(&self) -> &mut [ScAngelus] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAngelus,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAdrenaline_mut(&self) -> &mut [ScAdrenaline] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAdrenaline,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScInspiration_mut(&self) -> &mut [ScInspiration] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScInspiration,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for PlayerComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Health>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBerserk>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScBerserk;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScQuicken>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScQuicken;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAngelus>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScAngelus;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAdrenaline>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScAdrenaline;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScInspiration>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScInspiration;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct PlayerEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub Health: *mut Health,
    pub ScBerserk: *mut ScBerserk,
    pub ScQuicken: *mut ScQuicken,
    pub ScAngelus: *mut ScAngelus,
    pub ScAdrenaline: *mut ScAdrenaline,
    pub ScInspiration: *mut ScInspiration,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for PlayerEntityRefs {
    #[inline]
    fn clone(&self) -> PlayerEntityRefs {
        PlayerEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            Health: ::core::clone::Clone::clone(&self.Health),
            ScBerserk: ::core::clone::Clone::clone(&self.ScBerserk),
            ScQuicken: ::core::clone::Clone::clone(&self.ScQuicken),
            ScAngelus: ::core::clone::Clone::clone(&self.ScAngelus),
            ScAdrenaline: ::core::clone::Clone::clone(&self.ScAdrenaline),
            ScInspiration: ::core::clone::Clone::clone(&self.ScInspiration),
        }
    }
}
impl AsArchEntityEnumRef for PlayerEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Player(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Player {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<PlayerComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Player {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(PlayerComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScBerserk: ScBerserk,
        ScQuicken: ScQuicken,
        ScAngelus: ScAngelus,
        ScAdrenaline: ScAdrenaline,
        ScInspiration: ScInspiration,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                Health,
                ScBerserk,
                ScQuicken,
                ScAngelus,
                ScAdrenaline,
                ScInspiration,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Player.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Player.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> PlayerEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Player) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = PlayerEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            Health: unsafe { storage.Health_mut().get_unchecked_mut(dense_usize) },
            ScBerserk: unsafe { storage.ScBerserk_mut().get_unchecked_mut(dense_usize) },
            ScQuicken: unsafe { storage.ScQuicken_mut().get_unchecked_mut(dense_usize) },
            ScAngelus: unsafe { storage.ScAngelus_mut().get_unchecked_mut(dense_usize) },
            ScAdrenaline: unsafe {
                storage.ScAdrenaline_mut().get_unchecked_mut(dense_usize)
            },
            ScInspiration: unsafe {
                storage.ScInspiration_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        unsafe { (*self.storage.get()).Health() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBerserk(&self) -> &[ScBerserk] {
        unsafe { (*self.storage.get()).ScBerserk() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScQuicken(&self) -> &[ScQuicken] {
        unsafe { (*self.storage.get()).ScQuicken() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAngelus(&self) -> &[ScAngelus] {
        unsafe { (*self.storage.get()).ScAngelus() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAdrenaline(&self) -> &[ScAdrenaline] {
        unsafe { (*self.storage.get()).ScAdrenaline() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScInspiration(&self) -> &[ScInspiration] {
        unsafe { (*self.storage.get()).ScInspiration() }
    }
}
pub struct MonsterNormalComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MonsterNormalComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<Health>(), std::mem::align_of::<Health>()),
            (std::mem::size_of::<ScPoison>(), std::mem::align_of::<ScPoison>()),
            (std::mem::size_of::<ScBleeding>(), std::mem::align_of::<ScBleeding>()),
            (std::mem::size_of::<ScCurse>(), std::mem::align_of::<ScCurse>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, Health, ScPoison, ScBleeding, ScCurse, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPoison;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScBleeding;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScCurse;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Health>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut Health;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPoison>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScPoison;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBleeding>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScBleeding;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCurse>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScCurse;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScPoison: ScPoison,
        ScBleeding: ScBleeding,
        ScCurse: ScCurse,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut Health;
            type_base.add(self.len).write(Health);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScPoison;
            type_base.add(self.len).write(ScPoison);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScBleeding;
            type_base.add(self.len).write(ScBleeding);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScCurse;
            type_base.add(self.len).write(ScCurse);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[Health],
        &[ScPoison],
        &[ScBleeding],
        &[ScCurse],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const Health,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScPoison,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScBleeding,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScCurse,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [Health],
        &mut [ScPoison],
        &mut [ScBleeding],
        &mut [ScCurse],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut Health,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScPoison,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScBleeding,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScCurse,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPoison(&self) -> &[ScPoison] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPoison,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBleeding(&self) -> &[ScBleeding] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBleeding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurse(&self) -> &[ScCurse] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCurse,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health_mut(&self) -> &mut [Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPoison_mut(&self) -> &mut [ScPoison] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPoison,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBleeding_mut(&self) -> &mut [ScBleeding] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBleeding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurse_mut(&self) -> &mut [ScCurse] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCurse,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MonsterNormalComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Health>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPoison>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPoison;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBleeding>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScBleeding;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCurse>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScCurse;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MonsterNormalEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub Health: *mut Health,
    pub ScPoison: *mut ScPoison,
    pub ScBleeding: *mut ScBleeding,
    pub ScCurse: *mut ScCurse,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MonsterNormalEntityRefs {
    #[inline]
    fn clone(&self) -> MonsterNormalEntityRefs {
        MonsterNormalEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            Health: ::core::clone::Clone::clone(&self.Health),
            ScPoison: ::core::clone::Clone::clone(&self.ScPoison),
            ScBleeding: ::core::clone::Clone::clone(&self.ScBleeding),
            ScCurse: ::core::clone::Clone::clone(&self.ScCurse),
        }
    }
}
impl AsArchEntityEnumRef for MonsterNormalEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::MonsterNormal(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct MonsterNormal {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MonsterNormalComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl MonsterNormal {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MonsterNormalComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScPoison: ScPoison,
        ScBleeding: ScBleeding,
        ScCurse: ScCurse,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                Health,
                ScPoison,
                ScBleeding,
                ScCurse,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::MonsterNormal.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::MonsterNormal.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> MonsterNormalEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::MonsterNormal) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MonsterNormalEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            Health: unsafe { storage.Health_mut().get_unchecked_mut(dense_usize) },
            ScPoison: unsafe { storage.ScPoison_mut().get_unchecked_mut(dense_usize) },
            ScBleeding: unsafe {
                storage.ScBleeding_mut().get_unchecked_mut(dense_usize)
            },
            ScCurse: unsafe { storage.ScCurse_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        unsafe { (*self.storage.get()).Health() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPoison(&self) -> &[ScPoison] {
        unsafe { (*self.storage.get()).ScPoison() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBleeding(&self) -> &[ScBleeding] {
        unsafe { (*self.storage.get()).ScBleeding() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCurse(&self) -> &[ScCurse] {
        unsafe { (*self.storage.get()).ScCurse() }
    }
}
pub struct MonsterBossComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MonsterBossComponentStorage {
    pub const N: usize = 11;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<Health>(), std::mem::align_of::<Health>()),
            (std::mem::size_of::<ScStone>(), std::mem::align_of::<ScStone>()),
            (std::mem::size_of::<ScFreeze>(), std::mem::align_of::<ScFreeze>()),
            (std::mem::size_of::<ScStun>(), std::mem::align_of::<ScStun>()),
            (std::mem::size_of::<ScSleep>(), std::mem::align_of::<ScSleep>()),
            (std::mem::size_of::<ScDeepSleep>(), std::mem::align_of::<ScDeepSleep>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        Health,
        ScStone,
        ScFreeze,
        ScStun,
        ScSleep,
        ScDeepSleep,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScStone;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScFreeze;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScStun;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScSleep;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScDeepSleep;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Health>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut Health;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScStone>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScStone;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFreeze>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScFreeze;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScStun>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScStun;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSleep>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut ScSleep;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScDeepSleep>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut ScDeepSleep;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[9];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[10];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScStone: ScStone,
        ScFreeze: ScFreeze,
        ScStun: ScStun,
        ScSleep: ScSleep,
        ScDeepSleep: ScDeepSleep,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut Health;
            type_base.add(self.len).write(Health);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScStone;
            type_base.add(self.len).write(ScStone);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScFreeze;
            type_base.add(self.len).write(ScFreeze);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScStun;
            type_base.add(self.len).write(ScStun);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut ScSleep;
            type_base.add(self.len).write(ScSleep);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut ScDeepSleep;
            type_base.add(self.len).write(ScDeepSleep);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[9];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[10];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[Health],
        &[ScStone],
        &[ScFreeze],
        &[ScStun],
        &[ScSleep],
        &[ScDeepSleep],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const Health,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScStone,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScFreeze,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScStun,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const ScSleep,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const ScDeepSleep,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[9]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[10]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [Health],
        &mut [ScStone],
        &mut [ScFreeze],
        &mut [ScStun],
        &mut [ScSleep],
        &mut [ScDeepSleep],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut Health,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScStone,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScFreeze,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScStun,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut ScSleep,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut ScDeepSleep,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[9]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[10]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStone(&self) -> &[ScStone] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScStone,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreeze(&self) -> &[ScFreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStun(&self) -> &[ScStun] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScStun,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSleep(&self) -> &[ScSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDeepSleep(&self) -> &[ScDeepSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScDeepSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health_mut(&self) -> &mut [Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStone_mut(&self) -> &mut [ScStone] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScStone,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreeze_mut(&self) -> &mut [ScFreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStun_mut(&self) -> &mut [ScStun] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScStun,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSleep_mut(&self) -> &mut [ScSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDeepSleep_mut(&self) -> &mut [ScDeepSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScDeepSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MonsterBossComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Health>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScStone>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScStone;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFreeze>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScFreeze;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScStun>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScStun;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSleep>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScSleep;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScDeepSleep>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScDeepSleep;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MonsterBossEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub Health: *mut Health,
    pub ScStone: *mut ScStone,
    pub ScFreeze: *mut ScFreeze,
    pub ScStun: *mut ScStun,
    pub ScSleep: *mut ScSleep,
    pub ScDeepSleep: *mut ScDeepSleep,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MonsterBossEntityRefs {
    #[inline]
    fn clone(&self) -> MonsterBossEntityRefs {
        MonsterBossEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            Health: ::core::clone::Clone::clone(&self.Health),
            ScStone: ::core::clone::Clone::clone(&self.ScStone),
            ScFreeze: ::core::clone::Clone::clone(&self.ScFreeze),
            ScStun: ::core::clone::Clone::clone(&self.ScStun),
            ScSleep: ::core::clone::Clone::clone(&self.ScSleep),
            ScDeepSleep: ::core::clone::Clone::clone(&self.ScDeepSleep),
        }
    }
}
impl AsArchEntityEnumRef for MonsterBossEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::MonsterBoss(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct MonsterBoss {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MonsterBossComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl MonsterBoss {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MonsterBossComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScStone: ScStone,
        ScFreeze: ScFreeze,
        ScStun: ScStun,
        ScSleep: ScSleep,
        ScDeepSleep: ScDeepSleep,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                Health,
                ScStone,
                ScFreeze,
                ScStun,
                ScSleep,
                ScDeepSleep,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::MonsterBoss.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::MonsterBoss.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> MonsterBossEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::MonsterBoss) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MonsterBossEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            Health: unsafe { storage.Health_mut().get_unchecked_mut(dense_usize) },
            ScStone: unsafe { storage.ScStone_mut().get_unchecked_mut(dense_usize) },
            ScFreeze: unsafe { storage.ScFreeze_mut().get_unchecked_mut(dense_usize) },
            ScStun: unsafe { storage.ScStun_mut().get_unchecked_mut(dense_usize) },
            ScSleep: unsafe { storage.ScSleep_mut().get_unchecked_mut(dense_usize) },
            ScDeepSleep: unsafe {
                storage.ScDeepSleep_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        unsafe { (*self.storage.get()).Health() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScStone(&self) -> &[ScStone] {
        unsafe { (*self.storage.get()).ScStone() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFreeze(&self) -> &[ScFreeze] {
        unsafe { (*self.storage.get()).ScFreeze() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScStun(&self) -> &[ScStun] {
        unsafe { (*self.storage.get()).ScStun() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSleep(&self) -> &[ScSleep] {
        unsafe { (*self.storage.get()).ScSleep() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScDeepSleep(&self) -> &[ScDeepSleep] {
        unsafe { (*self.storage.get()).ScDeepSleep() }
    }
}
pub struct HomunculusComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl HomunculusComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScSoulLink>(), std::mem::align_of::<ScSoulLink>()),
            (std::mem::size_of::<ScKaite>(), std::mem::align_of::<ScKaite>()),
            (std::mem::size_of::<ScKyrie>(), std::mem::align_of::<ScKyrie>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScSoulLink, ScKaite, ScKyrie, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSoulLink;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScKaite;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScKyrie;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSoulLink>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScSoulLink;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScKaite>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScKaite;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScKyrie>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScKyrie;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSoulLink: ScSoulLink,
        ScKaite: ScKaite,
        ScKyrie: ScKyrie,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScSoulLink;
            type_base.add(self.len).write(ScSoulLink);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScKaite;
            type_base.add(self.len).write(ScKaite);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScKyrie;
            type_base.add(self.len).write(ScKyrie);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScSoulLink],
        &[ScKaite],
        &[ScKyrie],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScSoulLink,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScKaite,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScKyrie,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScSoulLink],
        &mut [ScKaite],
        &mut [ScKyrie],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScSoulLink,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScKaite,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScKyrie,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulLink(&self) -> &[ScSoulLink] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSoulLink,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKaite(&self) -> &[ScKaite] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScKaite,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKyrie(&self) -> &[ScKyrie] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScKyrie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulLink_mut(&self) -> &mut [ScSoulLink] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSoulLink,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKaite_mut(&self) -> &mut [ScKaite] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScKaite,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKyrie_mut(&self) -> &mut [ScKyrie] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScKyrie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for HomunculusComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSoulLink>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSoulLink;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScKaite>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScKaite;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScKyrie>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScKyrie;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct HomunculusEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScSoulLink: *mut ScSoulLink,
    pub ScKaite: *mut ScKaite,
    pub ScKyrie: *mut ScKyrie,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for HomunculusEntityRefs {
    #[inline]
    fn clone(&self) -> HomunculusEntityRefs {
        HomunculusEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScSoulLink: ::core::clone::Clone::clone(&self.ScSoulLink),
            ScKaite: ::core::clone::Clone::clone(&self.ScKaite),
            ScKyrie: ::core::clone::Clone::clone(&self.ScKyrie),
        }
    }
}
impl AsArchEntityEnumRef for HomunculusEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Homunculus(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Homunculus {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<HomunculusComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Homunculus {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(HomunculusComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSoulLink: ScSoulLink,
        ScKaite: ScKaite,
        ScKyrie: ScKyrie,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScSoulLink,
                ScKaite,
                ScKyrie,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Homunculus.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Homunculus.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> HomunculusEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Homunculus) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = HomunculusEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScSoulLink: unsafe {
                storage.ScSoulLink_mut().get_unchecked_mut(dense_usize)
            },
            ScKaite: unsafe { storage.ScKaite_mut().get_unchecked_mut(dense_usize) },
            ScKyrie: unsafe { storage.ScKyrie_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSoulLink(&self) -> &[ScSoulLink] {
        unsafe { (*self.storage.get()).ScSoulLink() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScKaite(&self) -> &[ScKaite] {
        unsafe { (*self.storage.get()).ScKaite() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScKyrie(&self) -> &[ScKyrie] {
        unsafe { (*self.storage.get()).ScKyrie() }
    }
}
pub struct MercenaryComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MercenaryComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<Health>(), std::mem::align_of::<Health>()),
            (std::mem::size_of::<ScMagnificat>(), std::mem::align_of::<ScMagnificat>()),
            (std::mem::size_of::<ScGloria>(), std::mem::align_of::<ScGloria>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, Health, ScMagnificat, ScGloria, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScMagnificat;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScGloria;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Health>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut Health;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMagnificat>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScMagnificat;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScGloria>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScGloria;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScMagnificat: ScMagnificat,
        ScGloria: ScGloria,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut Health;
            type_base.add(self.len).write(Health);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScMagnificat;
            type_base.add(self.len).write(ScMagnificat);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScGloria;
            type_base.add(self.len).write(ScGloria);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[Health],
        &[ScMagnificat],
        &[ScGloria],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const Health,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScMagnificat,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScGloria,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [Health],
        &mut [ScMagnificat],
        &mut [ScGloria],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut Health,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScMagnificat,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScGloria,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagnificat(&self) -> &[ScMagnificat] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMagnificat,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGloria(&self) -> &[ScGloria] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScGloria,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health_mut(&self) -> &mut [Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagnificat_mut(&self) -> &mut [ScMagnificat] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMagnificat,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGloria_mut(&self) -> &mut [ScGloria] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScGloria,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MercenaryComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Health>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut Health;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMagnificat>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScMagnificat;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScGloria>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScGloria;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MercenaryEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub Health: *mut Health,
    pub ScMagnificat: *mut ScMagnificat,
    pub ScGloria: *mut ScGloria,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MercenaryEntityRefs {
    #[inline]
    fn clone(&self) -> MercenaryEntityRefs {
        MercenaryEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            Health: ::core::clone::Clone::clone(&self.Health),
            ScMagnificat: ::core::clone::Clone::clone(&self.ScMagnificat),
            ScGloria: ::core::clone::Clone::clone(&self.ScGloria),
        }
    }
}
impl AsArchEntityEnumRef for MercenaryEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Mercenary(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Mercenary {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MercenaryComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Mercenary {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MercenaryComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        Health: Health,
        ScMagnificat: ScMagnificat,
        ScGloria: ScGloria,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                Health,
                ScMagnificat,
                ScGloria,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Mercenary.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Mercenary.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> MercenaryEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Mercenary) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MercenaryEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            Health: unsafe { storage.Health_mut().get_unchecked_mut(dense_usize) },
            ScMagnificat: unsafe {
                storage.ScMagnificat_mut().get_unchecked_mut(dense_usize)
            },
            ScGloria: unsafe { storage.ScGloria_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        unsafe { (*self.storage.get()).Health() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMagnificat(&self) -> &[ScMagnificat] {
        unsafe { (*self.storage.get()).ScMagnificat() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScGloria(&self) -> &[ScGloria] {
        unsafe { (*self.storage.get()).ScGloria() }
    }
}
pub struct PetComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl PetComponentStorage {
    pub const N: usize = 7;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScEndure>(), std::mem::align_of::<ScEndure>()),
            (std::mem::size_of::<ScAutoguard>(), std::mem::align_of::<ScAutoguard>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScEndure, ScAutoguard, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScEndure;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAutoguard;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScEndure>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScEndure;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAutoguard>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScAutoguard;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScEndure: ScEndure,
        ScAutoguard: ScAutoguard,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScEndure;
            type_base.add(self.len).write(ScEndure);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScAutoguard;
            type_base.add(self.len).write(ScAutoguard);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScEndure],
        &[ScAutoguard],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScEndure,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScAutoguard,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScEndure],
        &mut [ScAutoguard],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScEndure,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScAutoguard,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEndure(&self) -> &[ScEndure] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScEndure,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAutoguard(&self) -> &[ScAutoguard] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAutoguard,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEndure_mut(&self) -> &mut [ScEndure] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScEndure,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAutoguard_mut(&self) -> &mut [ScAutoguard] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAutoguard,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for PetComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScEndure>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScEndure;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAutoguard>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAutoguard;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct PetEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScEndure: *mut ScEndure,
    pub ScAutoguard: *mut ScAutoguard,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for PetEntityRefs {
    #[inline]
    fn clone(&self) -> PetEntityRefs {
        PetEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScEndure: ::core::clone::Clone::clone(&self.ScEndure),
            ScAutoguard: ::core::clone::Clone::clone(&self.ScAutoguard),
        }
    }
}
impl AsArchEntityEnumRef for PetEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Pet(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Pet {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<PetComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Pet {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(PetComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScEndure: ScEndure,
        ScAutoguard: ScAutoguard,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(Position, Velocity, ScEndure, ScAutoguard, dense_index, slot_index, 0);
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Pet.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Pet.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> PetEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Pet) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = PetEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScEndure: unsafe { storage.ScEndure_mut().get_unchecked_mut(dense_usize) },
            ScAutoguard: unsafe {
                storage.ScAutoguard_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScEndure(&self) -> &[ScEndure] {
        unsafe { (*self.storage.get()).ScEndure() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAutoguard(&self) -> &[ScAutoguard] {
        unsafe { (*self.storage.get()).ScAutoguard() }
    }
}
pub struct ElementalComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl ElementalComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScElementalChange>(),
                std::mem::align_of::<ScElementalChange>(),
            ),
            (
                std::mem::size_of::<ScTidalWeapon>(),
                std::mem::align_of::<ScTidalWeapon>(),
            ),
            (
                std::mem::size_of::<ScWaterScreen>(),
                std::mem::align_of::<ScWaterScreen>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScElementalChange,
        ScTidalWeapon,
        ScWaterScreen,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScElementalChange;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScTidalWeapon;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScWaterScreen;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScElementalChange>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScElementalChange;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScTidalWeapon>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScTidalWeapon;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScWaterScreen>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScWaterScreen;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScElementalChange: ScElementalChange,
        ScTidalWeapon: ScTidalWeapon,
        ScWaterScreen: ScWaterScreen,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScElementalChange;
            type_base.add(self.len).write(ScElementalChange);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScTidalWeapon;
            type_base.add(self.len).write(ScTidalWeapon);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScWaterScreen;
            type_base.add(self.len).write(ScWaterScreen);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScElementalChange],
        &[ScTidalWeapon],
        &[ScWaterScreen],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScElementalChange,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScTidalWeapon,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScWaterScreen,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScElementalChange],
        &mut [ScTidalWeapon],
        &mut [ScWaterScreen],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScElementalChange,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScTidalWeapon,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScWaterScreen,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScElementalChange(&self) -> &[ScElementalChange] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScElementalChange,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScTidalWeapon(&self) -> &[ScTidalWeapon] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScTidalWeapon,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWaterScreen(&self) -> &[ScWaterScreen] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScWaterScreen,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScElementalChange_mut(&self) -> &mut [ScElementalChange] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScElementalChange,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScTidalWeapon_mut(&self) -> &mut [ScTidalWeapon] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScTidalWeapon,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWaterScreen_mut(&self) -> &mut [ScWaterScreen] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScWaterScreen,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for ElementalComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScElementalChange>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScElementalChange;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScTidalWeapon>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScTidalWeapon;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScWaterScreen>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScWaterScreen;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct ElementalEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScElementalChange: *mut ScElementalChange,
    pub ScTidalWeapon: *mut ScTidalWeapon,
    pub ScWaterScreen: *mut ScWaterScreen,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for ElementalEntityRefs {
    #[inline]
    fn clone(&self) -> ElementalEntityRefs {
        ElementalEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScElementalChange: ::core::clone::Clone::clone(&self.ScElementalChange),
            ScTidalWeapon: ::core::clone::Clone::clone(&self.ScTidalWeapon),
            ScWaterScreen: ::core::clone::Clone::clone(&self.ScWaterScreen),
        }
    }
}
impl AsArchEntityEnumRef for ElementalEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Elemental(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Elemental {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<ElementalComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Elemental {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(ElementalComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScElementalChange: ScElementalChange,
        ScTidalWeapon: ScTidalWeapon,
        ScWaterScreen: ScWaterScreen,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScElementalChange,
                ScTidalWeapon,
                ScWaterScreen,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Elemental.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Elemental.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> ElementalEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Elemental) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = ElementalEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScElementalChange: unsafe {
                storage.ScElementalChange_mut().get_unchecked_mut(dense_usize)
            },
            ScTidalWeapon: unsafe {
                storage.ScTidalWeapon_mut().get_unchecked_mut(dense_usize)
            },
            ScWaterScreen: unsafe {
                storage.ScWaterScreen_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScElementalChange(&self) -> &[ScElementalChange] {
        unsafe { (*self.storage.get()).ScElementalChange() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScTidalWeapon(&self) -> &[ScTidalWeapon] {
        unsafe { (*self.storage.get()).ScTidalWeapon() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScWaterScreen(&self) -> &[ScWaterScreen] {
        unsafe { (*self.storage.get()).ScWaterScreen() }
    }
}
pub struct WugRiderComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl WugRiderComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScWugRider>(), std::mem::align_of::<ScWugRider>()),
            (std::mem::size_of::<ScCartBoost>(), std::mem::align_of::<ScCartBoost>()),
            (
                std::mem::size_of::<ScIncreaseAgi>(),
                std::mem::align_of::<ScIncreaseAgi>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScWugRider, ScCartBoost, ScIncreaseAgi, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScWugRider;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScCartBoost;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScWugRider>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScWugRider;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCartBoost>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScCartBoost;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScIncreaseAgi>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScWugRider: ScWugRider,
        ScCartBoost: ScCartBoost,
        ScIncreaseAgi: ScIncreaseAgi,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScWugRider;
            type_base.add(self.len).write(ScWugRider);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScCartBoost;
            type_base.add(self.len).write(ScCartBoost);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScIncreaseAgi;
            type_base.add(self.len).write(ScIncreaseAgi);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScWugRider],
        &[ScCartBoost],
        &[ScIncreaseAgi],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScWugRider,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScCartBoost,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScIncreaseAgi,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScWugRider],
        &mut [ScCartBoost],
        &mut [ScIncreaseAgi],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScWugRider,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScCartBoost,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScIncreaseAgi,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWugRider(&self) -> &[ScWugRider] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScWugRider,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCartBoost(&self) -> &[ScCartBoost] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCartBoost,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi(&self) -> &[ScIncreaseAgi] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScIncreaseAgi,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWugRider_mut(&self) -> &mut [ScWugRider] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScWugRider,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCartBoost_mut(&self) -> &mut [ScCartBoost] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCartBoost,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi_mut(&self) -> &mut [ScIncreaseAgi] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScIncreaseAgi,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for WugRiderComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScWugRider>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScWugRider;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCartBoost>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScCartBoost;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScIncreaseAgi>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct WugRiderEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScWugRider: *mut ScWugRider,
    pub ScCartBoost: *mut ScCartBoost,
    pub ScIncreaseAgi: *mut ScIncreaseAgi,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for WugRiderEntityRefs {
    #[inline]
    fn clone(&self) -> WugRiderEntityRefs {
        WugRiderEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScWugRider: ::core::clone::Clone::clone(&self.ScWugRider),
            ScCartBoost: ::core::clone::Clone::clone(&self.ScCartBoost),
            ScIncreaseAgi: ::core::clone::Clone::clone(&self.ScIncreaseAgi),
        }
    }
}
impl AsArchEntityEnumRef for WugRiderEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::WugRider(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct WugRider {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<WugRiderComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl WugRider {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(WugRiderComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScWugRider: ScWugRider,
        ScCartBoost: ScCartBoost,
        ScIncreaseAgi: ScIncreaseAgi,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScWugRider,
                ScCartBoost,
                ScIncreaseAgi,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::WugRider.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::WugRider.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> WugRiderEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::WugRider) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = WugRiderEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScWugRider: unsafe {
                storage.ScWugRider_mut().get_unchecked_mut(dense_usize)
            },
            ScCartBoost: unsafe {
                storage.ScCartBoost_mut().get_unchecked_mut(dense_usize)
            },
            ScIncreaseAgi: unsafe {
                storage.ScIncreaseAgi_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScWugRider(&self) -> &[ScWugRider] {
        unsafe { (*self.storage.get()).ScWugRider() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCartBoost(&self) -> &[ScCartBoost] {
        unsafe { (*self.storage.get()).ScCartBoost() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi(&self) -> &[ScIncreaseAgi] {
        unsafe { (*self.storage.get()).ScIncreaseAgi() }
    }
}
pub struct MadogearUserComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MadogearUserComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScMadogear>(), std::mem::align_of::<ScMadogear>()),
            (std::mem::size_of::<ScHeatBarrel>(), std::mem::align_of::<ScHeatBarrel>()),
            (
                std::mem::size_of::<ScMagicalBullet>(),
                std::mem::align_of::<ScMagicalBullet>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScMadogear, ScHeatBarrel, ScMagicalBullet, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadogear;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMagicalBullet;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMadogear>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScMadogear;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScHeatBarrel>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMagicalBullet>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScMagicalBullet;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadogear: ScMadogear,
        ScHeatBarrel: ScHeatBarrel,
        ScMagicalBullet: ScMagicalBullet,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScMadogear;
            type_base.add(self.len).write(ScMadogear);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScHeatBarrel;
            type_base.add(self.len).write(ScHeatBarrel);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScMagicalBullet;
            type_base.add(self.len).write(ScMagicalBullet);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScMadogear],
        &[ScHeatBarrel],
        &[ScMagicalBullet],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScMadogear,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScHeatBarrel,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScMagicalBullet,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScMadogear],
        &mut [ScHeatBarrel],
        &mut [ScMagicalBullet],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScMadogear,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScHeatBarrel,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScMagicalBullet,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadogear(&self) -> &[ScMadogear] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMadogear,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel(&self) -> &[ScHeatBarrel] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScHeatBarrel,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagicalBullet(&self) -> &[ScMagicalBullet] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMagicalBullet,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadogear_mut(&self) -> &mut [ScMadogear] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMadogear,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel_mut(&self) -> &mut [ScHeatBarrel] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScHeatBarrel,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagicalBullet_mut(&self) -> &mut [ScMagicalBullet] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMagicalBullet,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MadogearUserComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMadogear>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadogear;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScHeatBarrel>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMagicalBullet>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMagicalBullet;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MadogearUserEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScMadogear: *mut ScMadogear,
    pub ScHeatBarrel: *mut ScHeatBarrel,
    pub ScMagicalBullet: *mut ScMagicalBullet,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MadogearUserEntityRefs {
    #[inline]
    fn clone(&self) -> MadogearUserEntityRefs {
        MadogearUserEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScMadogear: ::core::clone::Clone::clone(&self.ScMadogear),
            ScHeatBarrel: ::core::clone::Clone::clone(&self.ScHeatBarrel),
            ScMagicalBullet: ::core::clone::Clone::clone(&self.ScMagicalBullet),
        }
    }
}
impl AsArchEntityEnumRef for MadogearUserEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::MadogearUser(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct MadogearUser {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MadogearUserComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl MadogearUser {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MadogearUserComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadogear: ScMadogear,
        ScHeatBarrel: ScHeatBarrel,
        ScMagicalBullet: ScMagicalBullet,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScMadogear,
                ScHeatBarrel,
                ScMagicalBullet,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::MadogearUser.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::MadogearUser.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> MadogearUserEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::MadogearUser) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MadogearUserEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScMadogear: unsafe {
                storage.ScMadogear_mut().get_unchecked_mut(dense_usize)
            },
            ScHeatBarrel: unsafe {
                storage.ScHeatBarrel_mut().get_unchecked_mut(dense_usize)
            },
            ScMagicalBullet: unsafe {
                storage.ScMagicalBullet_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMadogear(&self) -> &[ScMadogear] {
        unsafe { (*self.storage.get()).ScMadogear() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel(&self) -> &[ScHeatBarrel] {
        unsafe { (*self.storage.get()).ScHeatBarrel() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMagicalBullet(&self) -> &[ScMagicalBullet] {
        unsafe { (*self.storage.get()).ScMagicalBullet() }
    }
}
pub struct GeneticCartComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl GeneticCartComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScCartBoost>(), std::mem::align_of::<ScCartBoost>()),
            (
                std::mem::size_of::<ScPyrotechnic>(),
                std::mem::align_of::<ScPyrotechnic>(),
            ),
            (std::mem::size_of::<ScThornTrap>(), std::mem::align_of::<ScThornTrap>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScCartBoost, ScPyrotechnic, ScThornTrap, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCartBoost;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPyrotechnic;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScThornTrap;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCartBoost>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScCartBoost;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPyrotechnic>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScPyrotechnic;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScThornTrap>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScThornTrap;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCartBoost: ScCartBoost,
        ScPyrotechnic: ScPyrotechnic,
        ScThornTrap: ScThornTrap,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScCartBoost;
            type_base.add(self.len).write(ScCartBoost);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScPyrotechnic;
            type_base.add(self.len).write(ScPyrotechnic);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScThornTrap;
            type_base.add(self.len).write(ScThornTrap);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScCartBoost],
        &[ScPyrotechnic],
        &[ScThornTrap],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScCartBoost,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScPyrotechnic,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScThornTrap,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScCartBoost],
        &mut [ScPyrotechnic],
        &mut [ScThornTrap],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScCartBoost,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScPyrotechnic,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScThornTrap,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCartBoost(&self) -> &[ScCartBoost] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCartBoost,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPyrotechnic(&self) -> &[ScPyrotechnic] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPyrotechnic,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScThornTrap(&self) -> &[ScThornTrap] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScThornTrap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCartBoost_mut(&self) -> &mut [ScCartBoost] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCartBoost,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPyrotechnic_mut(&self) -> &mut [ScPyrotechnic] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPyrotechnic,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScThornTrap_mut(&self) -> &mut [ScThornTrap] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScThornTrap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for GeneticCartComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCartBoost>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCartBoost;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPyrotechnic>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPyrotechnic;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScThornTrap>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScThornTrap;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct GeneticCartEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScCartBoost: *mut ScCartBoost,
    pub ScPyrotechnic: *mut ScPyrotechnic,
    pub ScThornTrap: *mut ScThornTrap,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for GeneticCartEntityRefs {
    #[inline]
    fn clone(&self) -> GeneticCartEntityRefs {
        GeneticCartEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScCartBoost: ::core::clone::Clone::clone(&self.ScCartBoost),
            ScPyrotechnic: ::core::clone::Clone::clone(&self.ScPyrotechnic),
            ScThornTrap: ::core::clone::Clone::clone(&self.ScThornTrap),
        }
    }
}
impl AsArchEntityEnumRef for GeneticCartEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::GeneticCart(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct GeneticCart {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<GeneticCartComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl GeneticCart {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(GeneticCartComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCartBoost: ScCartBoost,
        ScPyrotechnic: ScPyrotechnic,
        ScThornTrap: ScThornTrap,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScCartBoost,
                ScPyrotechnic,
                ScThornTrap,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::GeneticCart.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::GeneticCart.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> GeneticCartEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::GeneticCart) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = GeneticCartEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScCartBoost: unsafe {
                storage.ScCartBoost_mut().get_unchecked_mut(dense_usize)
            },
            ScPyrotechnic: unsafe {
                storage.ScPyrotechnic_mut().get_unchecked_mut(dense_usize)
            },
            ScThornTrap: unsafe {
                storage.ScThornTrap_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCartBoost(&self) -> &[ScCartBoost] {
        unsafe { (*self.storage.get()).ScCartBoost() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPyrotechnic(&self) -> &[ScPyrotechnic] {
        unsafe { (*self.storage.get()).ScPyrotechnic() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScThornTrap(&self) -> &[ScThornTrap] {
        unsafe { (*self.storage.get()).ScThornTrap() }
    }
}
pub struct RuneKnightComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl RuneKnightComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScEnchantBlade>(),
                std::mem::align_of::<ScEnchantBlade>(),
            ),
            (std::mem::size_of::<ScDeathBound>(), std::mem::align_of::<ScDeathBound>()),
            (
                std::mem::size_of::<ScMillenniumShield>(),
                std::mem::align_of::<ScMillenniumShield>(),
            ),
            (std::mem::size_of::<ScRebirth>(), std::mem::align_of::<ScRebirth>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScEnchantBlade,
        ScDeathBound,
        ScMillenniumShield,
        ScRebirth,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScEnchantBlade;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScDeathBound;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMillenniumShield;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScRebirth;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScEnchantBlade>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScEnchantBlade;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScDeathBound>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScDeathBound;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMillenniumShield>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScMillenniumShield;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScRebirth>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScRebirth;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScEnchantBlade: ScEnchantBlade,
        ScDeathBound: ScDeathBound,
        ScMillenniumShield: ScMillenniumShield,
        ScRebirth: ScRebirth,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScEnchantBlade;
            type_base.add(self.len).write(ScEnchantBlade);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScDeathBound;
            type_base.add(self.len).write(ScDeathBound);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScMillenniumShield;
            type_base.add(self.len).write(ScMillenniumShield);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScRebirth;
            type_base.add(self.len).write(ScRebirth);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScEnchantBlade],
        &[ScDeathBound],
        &[ScMillenniumShield],
        &[ScRebirth],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScEnchantBlade,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScDeathBound,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScMillenniumShield,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScRebirth,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScEnchantBlade],
        &mut [ScDeathBound],
        &mut [ScMillenniumShield],
        &mut [ScRebirth],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScEnchantBlade,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScDeathBound,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScMillenniumShield,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScRebirth,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEnchantBlade(&self) -> &[ScEnchantBlade] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScEnchantBlade,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDeathBound(&self) -> &[ScDeathBound] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScDeathBound,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMillenniumShield(&self) -> &[ScMillenniumShield] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMillenniumShield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRebirth(&self) -> &[ScRebirth] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScRebirth,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEnchantBlade_mut(&self) -> &mut [ScEnchantBlade] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScEnchantBlade,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDeathBound_mut(&self) -> &mut [ScDeathBound] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScDeathBound,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMillenniumShield_mut(&self) -> &mut [ScMillenniumShield] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMillenniumShield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRebirth_mut(&self) -> &mut [ScRebirth] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScRebirth,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for RuneKnightComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScEnchantBlade>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScEnchantBlade;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScDeathBound>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScDeathBound;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMillenniumShield>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMillenniumShield;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScRebirth>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScRebirth;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct RuneKnightEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScEnchantBlade: *mut ScEnchantBlade,
    pub ScDeathBound: *mut ScDeathBound,
    pub ScMillenniumShield: *mut ScMillenniumShield,
    pub ScRebirth: *mut ScRebirth,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for RuneKnightEntityRefs {
    #[inline]
    fn clone(&self) -> RuneKnightEntityRefs {
        RuneKnightEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScEnchantBlade: ::core::clone::Clone::clone(&self.ScEnchantBlade),
            ScDeathBound: ::core::clone::Clone::clone(&self.ScDeathBound),
            ScMillenniumShield: ::core::clone::Clone::clone(&self.ScMillenniumShield),
            ScRebirth: ::core::clone::Clone::clone(&self.ScRebirth),
        }
    }
}
impl AsArchEntityEnumRef for RuneKnightEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::RuneKnight(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct RuneKnight {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<RuneKnightComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl RuneKnight {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(RuneKnightComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScEnchantBlade: ScEnchantBlade,
        ScDeathBound: ScDeathBound,
        ScMillenniumShield: ScMillenniumShield,
        ScRebirth: ScRebirth,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScEnchantBlade,
                ScDeathBound,
                ScMillenniumShield,
                ScRebirth,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::RuneKnight.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::RuneKnight.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> RuneKnightEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::RuneKnight) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = RuneKnightEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScEnchantBlade: unsafe {
                storage.ScEnchantBlade_mut().get_unchecked_mut(dense_usize)
            },
            ScDeathBound: unsafe {
                storage.ScDeathBound_mut().get_unchecked_mut(dense_usize)
            },
            ScMillenniumShield: unsafe {
                storage.ScMillenniumShield_mut().get_unchecked_mut(dense_usize)
            },
            ScRebirth: unsafe { storage.ScRebirth_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScEnchantBlade(&self) -> &[ScEnchantBlade] {
        unsafe { (*self.storage.get()).ScEnchantBlade() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScDeathBound(&self) -> &[ScDeathBound] {
        unsafe { (*self.storage.get()).ScDeathBound() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMillenniumShield(&self) -> &[ScMillenniumShield] {
        unsafe { (*self.storage.get()).ScMillenniumShield() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScRebirth(&self) -> &[ScRebirth] {
        unsafe { (*self.storage.get()).ScRebirth() }
    }
}
pub struct RoyalGuardComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl RoyalGuardComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScDefender>(), std::mem::align_of::<ScDefender>()),
            (
                std::mem::size_of::<ScReflectShield>(),
                std::mem::align_of::<ScReflectShield>(),
            ),
            (std::mem::size_of::<ScPrestige>(), std::mem::align_of::<ScPrestige>()),
            (std::mem::size_of::<ScBanding>(), std::mem::align_of::<ScBanding>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScDefender,
        ScReflectShield,
        ScPrestige,
        ScBanding,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScDefender;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScReflectShield;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScPrestige;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScBanding;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScDefender>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScDefender;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScReflectShield>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScReflectShield;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPrestige>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScPrestige;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBanding>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScBanding;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScDefender: ScDefender,
        ScReflectShield: ScReflectShield,
        ScPrestige: ScPrestige,
        ScBanding: ScBanding,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScDefender;
            type_base.add(self.len).write(ScDefender);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScReflectShield;
            type_base.add(self.len).write(ScReflectShield);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScPrestige;
            type_base.add(self.len).write(ScPrestige);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScBanding;
            type_base.add(self.len).write(ScBanding);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScDefender],
        &[ScReflectShield],
        &[ScPrestige],
        &[ScBanding],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScDefender,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScReflectShield,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScPrestige,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScBanding,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScDefender],
        &mut [ScReflectShield],
        &mut [ScPrestige],
        &mut [ScBanding],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScDefender,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScReflectShield,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScPrestige,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScBanding,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDefender(&self) -> &[ScDefender] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScDefender,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReflectShield(&self) -> &[ScReflectShield] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScReflectShield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPrestige(&self) -> &[ScPrestige] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPrestige,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBanding(&self) -> &[ScBanding] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBanding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDefender_mut(&self) -> &mut [ScDefender] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScDefender,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReflectShield_mut(&self) -> &mut [ScReflectShield] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScReflectShield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPrestige_mut(&self) -> &mut [ScPrestige] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPrestige,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBanding_mut(&self) -> &mut [ScBanding] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBanding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for RoyalGuardComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScDefender>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScDefender;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScReflectShield>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScReflectShield;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPrestige>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScPrestige;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBanding>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScBanding;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct RoyalGuardEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScDefender: *mut ScDefender,
    pub ScReflectShield: *mut ScReflectShield,
    pub ScPrestige: *mut ScPrestige,
    pub ScBanding: *mut ScBanding,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for RoyalGuardEntityRefs {
    #[inline]
    fn clone(&self) -> RoyalGuardEntityRefs {
        RoyalGuardEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScDefender: ::core::clone::Clone::clone(&self.ScDefender),
            ScReflectShield: ::core::clone::Clone::clone(&self.ScReflectShield),
            ScPrestige: ::core::clone::Clone::clone(&self.ScPrestige),
            ScBanding: ::core::clone::Clone::clone(&self.ScBanding),
        }
    }
}
impl AsArchEntityEnumRef for RoyalGuardEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::RoyalGuard(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct RoyalGuard {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<RoyalGuardComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl RoyalGuard {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(RoyalGuardComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScDefender: ScDefender,
        ScReflectShield: ScReflectShield,
        ScPrestige: ScPrestige,
        ScBanding: ScBanding,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScDefender,
                ScReflectShield,
                ScPrestige,
                ScBanding,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::RoyalGuard.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::RoyalGuard.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> RoyalGuardEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::RoyalGuard) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = RoyalGuardEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScDefender: unsafe {
                storage.ScDefender_mut().get_unchecked_mut(dense_usize)
            },
            ScReflectShield: unsafe {
                storage.ScReflectShield_mut().get_unchecked_mut(dense_usize)
            },
            ScPrestige: unsafe {
                storage.ScPrestige_mut().get_unchecked_mut(dense_usize)
            },
            ScBanding: unsafe { storage.ScBanding_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScDefender(&self) -> &[ScDefender] {
        unsafe { (*self.storage.get()).ScDefender() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScReflectShield(&self) -> &[ScReflectShield] {
        unsafe { (*self.storage.get()).ScReflectShield() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPrestige(&self) -> &[ScPrestige] {
        unsafe { (*self.storage.get()).ScPrestige() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBanding(&self) -> &[ScBanding] {
        unsafe { (*self.storage.get()).ScBanding() }
    }
}
pub struct MechanicComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MechanicComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScMadogear>(), std::mem::align_of::<ScMadogear>()),
            (
                std::mem::size_of::<ScPowerThrust>(),
                std::mem::align_of::<ScPowerThrust>(),
            ),
            (
                std::mem::size_of::<ScWeaponPerfection>(),
                std::mem::align_of::<ScWeaponPerfection>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScMadogear,
        ScPowerThrust,
        ScWeaponPerfection,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadogear;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPowerThrust;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScWeaponPerfection;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMadogear>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScMadogear;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPowerThrust>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScPowerThrust;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScWeaponPerfection>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScWeaponPerfection;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadogear: ScMadogear,
        ScPowerThrust: ScPowerThrust,
        ScWeaponPerfection: ScWeaponPerfection,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScMadogear;
            type_base.add(self.len).write(ScMadogear);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScPowerThrust;
            type_base.add(self.len).write(ScPowerThrust);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScWeaponPerfection;
            type_base.add(self.len).write(ScWeaponPerfection);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScMadogear],
        &[ScPowerThrust],
        &[ScWeaponPerfection],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScMadogear,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScPowerThrust,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScWeaponPerfection,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScMadogear],
        &mut [ScPowerThrust],
        &mut [ScWeaponPerfection],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScMadogear,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScPowerThrust,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScWeaponPerfection,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadogear(&self) -> &[ScMadogear] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMadogear,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust(&self) -> &[ScPowerThrust] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPowerThrust,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWeaponPerfection(&self) -> &[ScWeaponPerfection] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScWeaponPerfection,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadogear_mut(&self) -> &mut [ScMadogear] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMadogear,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust_mut(&self) -> &mut [ScPowerThrust] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPowerThrust,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWeaponPerfection_mut(&self) -> &mut [ScWeaponPerfection] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScWeaponPerfection,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MechanicComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMadogear>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadogear;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPowerThrust>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPowerThrust;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScWeaponPerfection>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScWeaponPerfection;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MechanicEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScMadogear: *mut ScMadogear,
    pub ScPowerThrust: *mut ScPowerThrust,
    pub ScWeaponPerfection: *mut ScWeaponPerfection,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MechanicEntityRefs {
    #[inline]
    fn clone(&self) -> MechanicEntityRefs {
        MechanicEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScMadogear: ::core::clone::Clone::clone(&self.ScMadogear),
            ScPowerThrust: ::core::clone::Clone::clone(&self.ScPowerThrust),
            ScWeaponPerfection: ::core::clone::Clone::clone(&self.ScWeaponPerfection),
        }
    }
}
impl AsArchEntityEnumRef for MechanicEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Mechanic(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Mechanic {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MechanicComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Mechanic {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MechanicComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadogear: ScMadogear,
        ScPowerThrust: ScPowerThrust,
        ScWeaponPerfection: ScWeaponPerfection,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScMadogear,
                ScPowerThrust,
                ScWeaponPerfection,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Mechanic.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Mechanic.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> MechanicEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Mechanic) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MechanicEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScMadogear: unsafe {
                storage.ScMadogear_mut().get_unchecked_mut(dense_usize)
            },
            ScPowerThrust: unsafe {
                storage.ScPowerThrust_mut().get_unchecked_mut(dense_usize)
            },
            ScWeaponPerfection: unsafe {
                storage.ScWeaponPerfection_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMadogear(&self) -> &[ScMadogear] {
        unsafe { (*self.storage.get()).ScMadogear() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust(&self) -> &[ScPowerThrust] {
        unsafe { (*self.storage.get()).ScPowerThrust() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScWeaponPerfection(&self) -> &[ScWeaponPerfection] {
        unsafe { (*self.storage.get()).ScWeaponPerfection() }
    }
}
pub struct GuillotineCrossComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl GuillotineCrossComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScCloakingExceed>(),
                std::mem::align_of::<ScCloakingExceed>(),
            ),
            (
                std::mem::size_of::<ScRollingCutter>(),
                std::mem::align_of::<ScRollingCutter>(),
            ),
            (
                std::mem::size_of::<ScCrossImpact>(),
                std::mem::align_of::<ScCrossImpact>(),
            ),
            (std::mem::size_of::<ScDarkClaw>(), std::mem::align_of::<ScDarkClaw>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScCloakingExceed,
        ScRollingCutter,
        ScCrossImpact,
        ScDarkClaw,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCloakingExceed;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScRollingCutter;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScCrossImpact;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScDarkClaw;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCloakingExceed>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScCloakingExceed;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScRollingCutter>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScRollingCutter;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCrossImpact>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScCrossImpact;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScDarkClaw>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScDarkClaw;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCloakingExceed: ScCloakingExceed,
        ScRollingCutter: ScRollingCutter,
        ScCrossImpact: ScCrossImpact,
        ScDarkClaw: ScDarkClaw,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScCloakingExceed;
            type_base.add(self.len).write(ScCloakingExceed);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScRollingCutter;
            type_base.add(self.len).write(ScRollingCutter);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScCrossImpact;
            type_base.add(self.len).write(ScCrossImpact);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScDarkClaw;
            type_base.add(self.len).write(ScDarkClaw);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScCloakingExceed],
        &[ScRollingCutter],
        &[ScCrossImpact],
        &[ScDarkClaw],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScCloakingExceed,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScRollingCutter,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScCrossImpact,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScDarkClaw,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScCloakingExceed],
        &mut [ScRollingCutter],
        &mut [ScCrossImpact],
        &mut [ScDarkClaw],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScCloakingExceed,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScRollingCutter,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScCrossImpact,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScDarkClaw,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCloakingExceed(&self) -> &[ScCloakingExceed] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCloakingExceed,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRollingCutter(&self) -> &[ScRollingCutter] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScRollingCutter,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCrossImpact(&self) -> &[ScCrossImpact] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCrossImpact,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDarkClaw(&self) -> &[ScDarkClaw] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScDarkClaw,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCloakingExceed_mut(&self) -> &mut [ScCloakingExceed] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCloakingExceed,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRollingCutter_mut(&self) -> &mut [ScRollingCutter] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScRollingCutter,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCrossImpact_mut(&self) -> &mut [ScCrossImpact] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCrossImpact,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDarkClaw_mut(&self) -> &mut [ScDarkClaw] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScDarkClaw,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for GuillotineCrossComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCloakingExceed>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCloakingExceed;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScRollingCutter>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScRollingCutter;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCrossImpact>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScCrossImpact;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScDarkClaw>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScDarkClaw;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct GuillotineCrossEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScCloakingExceed: *mut ScCloakingExceed,
    pub ScRollingCutter: *mut ScRollingCutter,
    pub ScCrossImpact: *mut ScCrossImpact,
    pub ScDarkClaw: *mut ScDarkClaw,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for GuillotineCrossEntityRefs {
    #[inline]
    fn clone(&self) -> GuillotineCrossEntityRefs {
        GuillotineCrossEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScCloakingExceed: ::core::clone::Clone::clone(&self.ScCloakingExceed),
            ScRollingCutter: ::core::clone::Clone::clone(&self.ScRollingCutter),
            ScCrossImpact: ::core::clone::Clone::clone(&self.ScCrossImpact),
            ScDarkClaw: ::core::clone::Clone::clone(&self.ScDarkClaw),
        }
    }
}
impl AsArchEntityEnumRef for GuillotineCrossEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::GuillotineCross(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct GuillotineCross {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<GuillotineCrossComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl GuillotineCross {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(GuillotineCrossComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCloakingExceed: ScCloakingExceed,
        ScRollingCutter: ScRollingCutter,
        ScCrossImpact: ScCrossImpact,
        ScDarkClaw: ScDarkClaw,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScCloakingExceed,
                ScRollingCutter,
                ScCrossImpact,
                ScDarkClaw,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::GuillotineCross.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::GuillotineCross.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> GuillotineCrossEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::GuillotineCross) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = GuillotineCrossEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScCloakingExceed: unsafe {
                storage.ScCloakingExceed_mut().get_unchecked_mut(dense_usize)
            },
            ScRollingCutter: unsafe {
                storage.ScRollingCutter_mut().get_unchecked_mut(dense_usize)
            },
            ScCrossImpact: unsafe {
                storage.ScCrossImpact_mut().get_unchecked_mut(dense_usize)
            },
            ScDarkClaw: unsafe {
                storage.ScDarkClaw_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCloakingExceed(&self) -> &[ScCloakingExceed] {
        unsafe { (*self.storage.get()).ScCloakingExceed() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScRollingCutter(&self) -> &[ScRollingCutter] {
        unsafe { (*self.storage.get()).ScRollingCutter() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCrossImpact(&self) -> &[ScCrossImpact] {
        unsafe { (*self.storage.get()).ScCrossImpact() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScDarkClaw(&self) -> &[ScDarkClaw] {
        unsafe { (*self.storage.get()).ScDarkClaw() }
    }
}
pub struct ShadowChaserComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl ShadowChaserComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScReproduce>(), std::mem::align_of::<ScReproduce>()),
            (
                std::mem::size_of::<ScAutoShadowSpell>(),
                std::mem::align_of::<ScAutoShadowSpell>(),
            ),
            (std::mem::size_of::<ScShadowForm>(), std::mem::align_of::<ScShadowForm>()),
            (
                std::mem::size_of::<ScFatalMenace>(),
                std::mem::align_of::<ScFatalMenace>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScReproduce,
        ScAutoShadowSpell,
        ScShadowForm,
        ScFatalMenace,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScReproduce;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAutoShadowSpell;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScShadowForm;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScFatalMenace;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScReproduce>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScReproduce;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAutoShadowSpell>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScAutoShadowSpell;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScShadowForm>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScShadowForm;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFatalMenace>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScFatalMenace;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScReproduce: ScReproduce,
        ScAutoShadowSpell: ScAutoShadowSpell,
        ScShadowForm: ScShadowForm,
        ScFatalMenace: ScFatalMenace,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScReproduce;
            type_base.add(self.len).write(ScReproduce);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScAutoShadowSpell;
            type_base.add(self.len).write(ScAutoShadowSpell);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScShadowForm;
            type_base.add(self.len).write(ScShadowForm);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScFatalMenace;
            type_base.add(self.len).write(ScFatalMenace);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScReproduce],
        &[ScAutoShadowSpell],
        &[ScShadowForm],
        &[ScFatalMenace],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScReproduce,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScAutoShadowSpell,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScShadowForm,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScFatalMenace,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScReproduce],
        &mut [ScAutoShadowSpell],
        &mut [ScShadowForm],
        &mut [ScFatalMenace],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScReproduce,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScAutoShadowSpell,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScShadowForm,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScFatalMenace,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReproduce(&self) -> &[ScReproduce] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScReproduce,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAutoShadowSpell(&self) -> &[ScAutoShadowSpell] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAutoShadowSpell,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScShadowForm(&self) -> &[ScShadowForm] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScShadowForm,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFatalMenace(&self) -> &[ScFatalMenace] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFatalMenace,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReproduce_mut(&self) -> &mut [ScReproduce] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScReproduce,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAutoShadowSpell_mut(&self) -> &mut [ScAutoShadowSpell] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAutoShadowSpell,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScShadowForm_mut(&self) -> &mut [ScShadowForm] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScShadowForm,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFatalMenace_mut(&self) -> &mut [ScFatalMenace] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFatalMenace,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for ShadowChaserComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScReproduce>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScReproduce;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAutoShadowSpell>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAutoShadowSpell;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScShadowForm>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScShadowForm;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFatalMenace>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScFatalMenace;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct ShadowChaserEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScReproduce: *mut ScReproduce,
    pub ScAutoShadowSpell: *mut ScAutoShadowSpell,
    pub ScShadowForm: *mut ScShadowForm,
    pub ScFatalMenace: *mut ScFatalMenace,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for ShadowChaserEntityRefs {
    #[inline]
    fn clone(&self) -> ShadowChaserEntityRefs {
        ShadowChaserEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScReproduce: ::core::clone::Clone::clone(&self.ScReproduce),
            ScAutoShadowSpell: ::core::clone::Clone::clone(&self.ScAutoShadowSpell),
            ScShadowForm: ::core::clone::Clone::clone(&self.ScShadowForm),
            ScFatalMenace: ::core::clone::Clone::clone(&self.ScFatalMenace),
        }
    }
}
impl AsArchEntityEnumRef for ShadowChaserEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::ShadowChaser(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct ShadowChaser {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<ShadowChaserComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl ShadowChaser {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(ShadowChaserComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScReproduce: ScReproduce,
        ScAutoShadowSpell: ScAutoShadowSpell,
        ScShadowForm: ScShadowForm,
        ScFatalMenace: ScFatalMenace,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScReproduce,
                ScAutoShadowSpell,
                ScShadowForm,
                ScFatalMenace,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::ShadowChaser.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::ShadowChaser.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> ShadowChaserEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::ShadowChaser) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = ShadowChaserEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScReproduce: unsafe {
                storage.ScReproduce_mut().get_unchecked_mut(dense_usize)
            },
            ScAutoShadowSpell: unsafe {
                storage.ScAutoShadowSpell_mut().get_unchecked_mut(dense_usize)
            },
            ScShadowForm: unsafe {
                storage.ScShadowForm_mut().get_unchecked_mut(dense_usize)
            },
            ScFatalMenace: unsafe {
                storage.ScFatalMenace_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScReproduce(&self) -> &[ScReproduce] {
        unsafe { (*self.storage.get()).ScReproduce() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAutoShadowSpell(&self) -> &[ScAutoShadowSpell] {
        unsafe { (*self.storage.get()).ScAutoShadowSpell() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScShadowForm(&self) -> &[ScShadowForm] {
        unsafe { (*self.storage.get()).ScShadowForm() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFatalMenace(&self) -> &[ScFatalMenace] {
        unsafe { (*self.storage.get()).ScFatalMenace() }
    }
}
pub struct SorcererComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl SorcererComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScSpellBreaker>(),
                std::mem::align_of::<ScSpellBreaker>(),
            ),
            (std::mem::size_of::<ScSoulLink>(), std::mem::align_of::<ScSoulLink>()),
            (std::mem::size_of::<ScPreserve>(), std::mem::align_of::<ScPreserve>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScSpellBreaker, ScSoulLink, ScPreserve, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSpellBreaker;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScSoulLink;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScPreserve;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSpellBreaker>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScSpellBreaker;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSoulLink>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScSoulLink;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPreserve>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScPreserve;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSpellBreaker: ScSpellBreaker,
        ScSoulLink: ScSoulLink,
        ScPreserve: ScPreserve,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScSpellBreaker;
            type_base.add(self.len).write(ScSpellBreaker);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScSoulLink;
            type_base.add(self.len).write(ScSoulLink);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScPreserve;
            type_base.add(self.len).write(ScPreserve);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScSpellBreaker],
        &[ScSoulLink],
        &[ScPreserve],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScSpellBreaker,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScSoulLink,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScPreserve,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScSpellBreaker],
        &mut [ScSoulLink],
        &mut [ScPreserve],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScSpellBreaker,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScSoulLink,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScPreserve,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSpellBreaker(&self) -> &[ScSpellBreaker] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSpellBreaker,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulLink(&self) -> &[ScSoulLink] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSoulLink,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPreserve(&self) -> &[ScPreserve] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPreserve,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSpellBreaker_mut(&self) -> &mut [ScSpellBreaker] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSpellBreaker,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulLink_mut(&self) -> &mut [ScSoulLink] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSoulLink,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPreserve_mut(&self) -> &mut [ScPreserve] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPreserve,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for SorcererComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSpellBreaker>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSpellBreaker;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSoulLink>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScSoulLink;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPreserve>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScPreserve;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct SorcererEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScSpellBreaker: *mut ScSpellBreaker,
    pub ScSoulLink: *mut ScSoulLink,
    pub ScPreserve: *mut ScPreserve,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for SorcererEntityRefs {
    #[inline]
    fn clone(&self) -> SorcererEntityRefs {
        SorcererEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScSpellBreaker: ::core::clone::Clone::clone(&self.ScSpellBreaker),
            ScSoulLink: ::core::clone::Clone::clone(&self.ScSoulLink),
            ScPreserve: ::core::clone::Clone::clone(&self.ScPreserve),
        }
    }
}
impl AsArchEntityEnumRef for SorcererEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Sorcerer(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Sorcerer {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<SorcererComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Sorcerer {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(SorcererComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSpellBreaker: ScSpellBreaker,
        ScSoulLink: ScSoulLink,
        ScPreserve: ScPreserve,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScSpellBreaker,
                ScSoulLink,
                ScPreserve,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Sorcerer.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Sorcerer.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> SorcererEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Sorcerer) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = SorcererEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScSpellBreaker: unsafe {
                storage.ScSpellBreaker_mut().get_unchecked_mut(dense_usize)
            },
            ScSoulLink: unsafe {
                storage.ScSoulLink_mut().get_unchecked_mut(dense_usize)
            },
            ScPreserve: unsafe {
                storage.ScPreserve_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSpellBreaker(&self) -> &[ScSpellBreaker] {
        unsafe { (*self.storage.get()).ScSpellBreaker() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSoulLink(&self) -> &[ScSoulLink] {
        unsafe { (*self.storage.get()).ScSoulLink() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPreserve(&self) -> &[ScPreserve] {
        unsafe { (*self.storage.get()).ScPreserve() }
    }
}
pub struct WarlockComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl WarlockComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScReadingSpellBook>(),
                std::mem::align_of::<ScReadingSpellBook>(),
            ),
            (
                std::mem::size_of::<ScFreezingSpell>(),
                std::mem::align_of::<ScFreezingSpell>(),
            ),
            (std::mem::size_of::<ScSummonBall>(), std::mem::align_of::<ScSummonBall>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScReadingSpellBook,
        ScFreezingSpell,
        ScSummonBall,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScReadingSpellBook;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFreezingSpell;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSummonBall;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScReadingSpellBook>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScReadingSpellBook;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFreezingSpell>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScFreezingSpell;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSummonBall>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScSummonBall;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScReadingSpellBook: ScReadingSpellBook,
        ScFreezingSpell: ScFreezingSpell,
        ScSummonBall: ScSummonBall,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScReadingSpellBook;
            type_base.add(self.len).write(ScReadingSpellBook);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScFreezingSpell;
            type_base.add(self.len).write(ScFreezingSpell);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScSummonBall;
            type_base.add(self.len).write(ScSummonBall);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScReadingSpellBook],
        &[ScFreezingSpell],
        &[ScSummonBall],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScReadingSpellBook,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScFreezingSpell,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScSummonBall,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScReadingSpellBook],
        &mut [ScFreezingSpell],
        &mut [ScSummonBall],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScReadingSpellBook,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScFreezingSpell,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScSummonBall,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReadingSpellBook(&self) -> &[ScReadingSpellBook] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScReadingSpellBook,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreezingSpell(&self) -> &[ScFreezingSpell] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFreezingSpell,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSummonBall(&self) -> &[ScSummonBall] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSummonBall,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScReadingSpellBook_mut(&self) -> &mut [ScReadingSpellBook] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScReadingSpellBook,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreezingSpell_mut(&self) -> &mut [ScFreezingSpell] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFreezingSpell,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSummonBall_mut(&self) -> &mut [ScSummonBall] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSummonBall,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for WarlockComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScReadingSpellBook>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScReadingSpellBook;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFreezingSpell>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFreezingSpell;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSummonBall>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSummonBall;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct WarlockEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScReadingSpellBook: *mut ScReadingSpellBook,
    pub ScFreezingSpell: *mut ScFreezingSpell,
    pub ScSummonBall: *mut ScSummonBall,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for WarlockEntityRefs {
    #[inline]
    fn clone(&self) -> WarlockEntityRefs {
        WarlockEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScReadingSpellBook: ::core::clone::Clone::clone(&self.ScReadingSpellBook),
            ScFreezingSpell: ::core::clone::Clone::clone(&self.ScFreezingSpell),
            ScSummonBall: ::core::clone::Clone::clone(&self.ScSummonBall),
        }
    }
}
impl AsArchEntityEnumRef for WarlockEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Warlock(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Warlock {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<WarlockComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Warlock {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(WarlockComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScReadingSpellBook: ScReadingSpellBook,
        ScFreezingSpell: ScFreezingSpell,
        ScSummonBall: ScSummonBall,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScReadingSpellBook,
                ScFreezingSpell,
                ScSummonBall,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Warlock.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Warlock.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> WarlockEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Warlock) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = WarlockEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScReadingSpellBook: unsafe {
                storage.ScReadingSpellBook_mut().get_unchecked_mut(dense_usize)
            },
            ScFreezingSpell: unsafe {
                storage.ScFreezingSpell_mut().get_unchecked_mut(dense_usize)
            },
            ScSummonBall: unsafe {
                storage.ScSummonBall_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScReadingSpellBook(&self) -> &[ScReadingSpellBook] {
        unsafe { (*self.storage.get()).ScReadingSpellBook() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFreezingSpell(&self) -> &[ScFreezingSpell] {
        unsafe { (*self.storage.get()).ScFreezingSpell() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSummonBall(&self) -> &[ScSummonBall] {
        unsafe { (*self.storage.get()).ScSummonBall() }
    }
}
pub struct ArchbishopComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl ArchbishopComponentStorage {
    pub const N: usize = 9;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScAssumptio>(), std::mem::align_of::<ScAssumptio>()),
            (std::mem::size_of::<ScKyrie>(), std::mem::align_of::<ScKyrie>()),
            (std::mem::size_of::<ScMagnificat>(), std::mem::align_of::<ScMagnificat>()),
            (std::mem::size_of::<ScBenedictum>(), std::mem::align_of::<ScBenedictum>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScAssumptio,
        ScKyrie,
        ScMagnificat,
        ScBenedictum,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScAssumptio;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScKyrie;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMagnificat;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScBenedictum;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAssumptio>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScAssumptio;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScKyrie>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScKyrie;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMagnificat>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScMagnificat;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBenedictum>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScBenedictum;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScAssumptio: ScAssumptio,
        ScKyrie: ScKyrie,
        ScMagnificat: ScMagnificat,
        ScBenedictum: ScBenedictum,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScAssumptio;
            type_base.add(self.len).write(ScAssumptio);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScKyrie;
            type_base.add(self.len).write(ScKyrie);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScMagnificat;
            type_base.add(self.len).write(ScMagnificat);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScBenedictum;
            type_base.add(self.len).write(ScBenedictum);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScAssumptio],
        &[ScKyrie],
        &[ScMagnificat],
        &[ScBenedictum],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScAssumptio,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScKyrie,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScMagnificat,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScBenedictum,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScAssumptio],
        &mut [ScKyrie],
        &mut [ScMagnificat],
        &mut [ScBenedictum],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScAssumptio,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScKyrie,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScMagnificat,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScBenedictum,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAssumptio(&self) -> &[ScAssumptio] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAssumptio,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKyrie(&self) -> &[ScKyrie] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScKyrie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagnificat(&self) -> &[ScMagnificat] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMagnificat,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBenedictum(&self) -> &[ScBenedictum] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBenedictum,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAssumptio_mut(&self) -> &mut [ScAssumptio] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAssumptio,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKyrie_mut(&self) -> &mut [ScKyrie] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScKyrie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMagnificat_mut(&self) -> &mut [ScMagnificat] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMagnificat,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBenedictum_mut(&self) -> &mut [ScBenedictum] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBenedictum,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for ArchbishopComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAssumptio>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScAssumptio;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScKyrie>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScKyrie;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMagnificat>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMagnificat;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBenedictum>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScBenedictum;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct ArchbishopEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScAssumptio: *mut ScAssumptio,
    pub ScKyrie: *mut ScKyrie,
    pub ScMagnificat: *mut ScMagnificat,
    pub ScBenedictum: *mut ScBenedictum,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for ArchbishopEntityRefs {
    #[inline]
    fn clone(&self) -> ArchbishopEntityRefs {
        ArchbishopEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScAssumptio: ::core::clone::Clone::clone(&self.ScAssumptio),
            ScKyrie: ::core::clone::Clone::clone(&self.ScKyrie),
            ScMagnificat: ::core::clone::Clone::clone(&self.ScMagnificat),
            ScBenedictum: ::core::clone::Clone::clone(&self.ScBenedictum),
        }
    }
}
impl AsArchEntityEnumRef for ArchbishopEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Archbishop(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Archbishop {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<ArchbishopComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Archbishop {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(ArchbishopComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScAssumptio: ScAssumptio,
        ScKyrie: ScKyrie,
        ScMagnificat: ScMagnificat,
        ScBenedictum: ScBenedictum,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScAssumptio,
                ScKyrie,
                ScMagnificat,
                ScBenedictum,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Archbishop.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Archbishop.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> ArchbishopEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Archbishop) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = ArchbishopEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScAssumptio: unsafe {
                storage.ScAssumptio_mut().get_unchecked_mut(dense_usize)
            },
            ScKyrie: unsafe { storage.ScKyrie_mut().get_unchecked_mut(dense_usize) },
            ScMagnificat: unsafe {
                storage.ScMagnificat_mut().get_unchecked_mut(dense_usize)
            },
            ScBenedictum: unsafe {
                storage.ScBenedictum_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAssumptio(&self) -> &[ScAssumptio] {
        unsafe { (*self.storage.get()).ScAssumptio() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScKyrie(&self) -> &[ScKyrie] {
        unsafe { (*self.storage.get()).ScKyrie() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMagnificat(&self) -> &[ScMagnificat] {
        unsafe { (*self.storage.get()).ScMagnificat() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBenedictum(&self) -> &[ScBenedictum] {
        unsafe { (*self.storage.get()).ScBenedictum() }
    }
}
pub struct SuraComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl SuraComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScGentleTouch>(),
                std::mem::align_of::<ScGentleTouch>(),
            ),
            (
                std::mem::size_of::<ScPowerThrust>(),
                std::mem::align_of::<ScPowerThrust>(),
            ),
            (
                std::mem::size_of::<ScRisingDragon>(),
                std::mem::align_of::<ScRisingDragon>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScGentleTouch,
        ScPowerThrust,
        ScRisingDragon,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScGentleTouch;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPowerThrust;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScRisingDragon;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScGentleTouch>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScGentleTouch;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPowerThrust>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScPowerThrust;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScRisingDragon>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScRisingDragon;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScGentleTouch: ScGentleTouch,
        ScPowerThrust: ScPowerThrust,
        ScRisingDragon: ScRisingDragon,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScGentleTouch;
            type_base.add(self.len).write(ScGentleTouch);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScPowerThrust;
            type_base.add(self.len).write(ScPowerThrust);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScRisingDragon;
            type_base.add(self.len).write(ScRisingDragon);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScGentleTouch],
        &[ScPowerThrust],
        &[ScRisingDragon],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScGentleTouch,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScPowerThrust,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScRisingDragon,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScGentleTouch],
        &mut [ScPowerThrust],
        &mut [ScRisingDragon],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScGentleTouch,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScPowerThrust,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScRisingDragon,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGentleTouch(&self) -> &[ScGentleTouch] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScGentleTouch,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust(&self) -> &[ScPowerThrust] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPowerThrust,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRisingDragon(&self) -> &[ScRisingDragon] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScRisingDragon,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGentleTouch_mut(&self) -> &mut [ScGentleTouch] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScGentleTouch,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust_mut(&self) -> &mut [ScPowerThrust] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPowerThrust,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScRisingDragon_mut(&self) -> &mut [ScRisingDragon] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScRisingDragon,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for SuraComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScGentleTouch>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScGentleTouch;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPowerThrust>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPowerThrust;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScRisingDragon>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScRisingDragon;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct SuraEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScGentleTouch: *mut ScGentleTouch,
    pub ScPowerThrust: *mut ScPowerThrust,
    pub ScRisingDragon: *mut ScRisingDragon,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for SuraEntityRefs {
    #[inline]
    fn clone(&self) -> SuraEntityRefs {
        SuraEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScGentleTouch: ::core::clone::Clone::clone(&self.ScGentleTouch),
            ScPowerThrust: ::core::clone::Clone::clone(&self.ScPowerThrust),
            ScRisingDragon: ::core::clone::Clone::clone(&self.ScRisingDragon),
        }
    }
}
impl AsArchEntityEnumRef for SuraEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Sura(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Sura {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<SuraComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Sura {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(SuraComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScGentleTouch: ScGentleTouch,
        ScPowerThrust: ScPowerThrust,
        ScRisingDragon: ScRisingDragon,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScGentleTouch,
                ScPowerThrust,
                ScRisingDragon,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Sura.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Sura.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> SuraEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Sura) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = SuraEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScGentleTouch: unsafe {
                storage.ScGentleTouch_mut().get_unchecked_mut(dense_usize)
            },
            ScPowerThrust: unsafe {
                storage.ScPowerThrust_mut().get_unchecked_mut(dense_usize)
            },
            ScRisingDragon: unsafe {
                storage.ScRisingDragon_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScGentleTouch(&self) -> &[ScGentleTouch] {
        unsafe { (*self.storage.get()).ScGentleTouch() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPowerThrust(&self) -> &[ScPowerThrust] {
        unsafe { (*self.storage.get()).ScPowerThrust() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScRisingDragon(&self) -> &[ScRisingDragon] {
        unsafe { (*self.storage.get()).ScRisingDragon() }
    }
}
pub struct RangerComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl RangerComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScWargRider>(), std::mem::align_of::<ScWargRider>()),
            (std::mem::size_of::<ScFearBreeze>(), std::mem::align_of::<ScFearBreeze>()),
            (std::mem::size_of::<ScAimedBolt>(), std::mem::align_of::<ScAimedBolt>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScWargRider, ScFearBreeze, ScAimedBolt, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScWargRider;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFearBreeze;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScAimedBolt;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScWargRider>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScWargRider;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFearBreeze>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScFearBreeze;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAimedBolt>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScAimedBolt;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScWargRider: ScWargRider,
        ScFearBreeze: ScFearBreeze,
        ScAimedBolt: ScAimedBolt,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScWargRider;
            type_base.add(self.len).write(ScWargRider);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScFearBreeze;
            type_base.add(self.len).write(ScFearBreeze);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScAimedBolt;
            type_base.add(self.len).write(ScAimedBolt);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScWargRider],
        &[ScFearBreeze],
        &[ScAimedBolt],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScWargRider,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScFearBreeze,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScAimedBolt,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScWargRider],
        &mut [ScFearBreeze],
        &mut [ScAimedBolt],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScWargRider,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScFearBreeze,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScAimedBolt,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWargRider(&self) -> &[ScWargRider] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScWargRider,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFearBreeze(&self) -> &[ScFearBreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFearBreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAimedBolt(&self) -> &[ScAimedBolt] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAimedBolt,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScWargRider_mut(&self) -> &mut [ScWargRider] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScWargRider,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFearBreeze_mut(&self) -> &mut [ScFearBreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFearBreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAimedBolt_mut(&self) -> &mut [ScAimedBolt] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAimedBolt,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for RangerComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScWargRider>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScWargRider;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFearBreeze>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFearBreeze;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAimedBolt>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScAimedBolt;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct RangerEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScWargRider: *mut ScWargRider,
    pub ScFearBreeze: *mut ScFearBreeze,
    pub ScAimedBolt: *mut ScAimedBolt,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for RangerEntityRefs {
    #[inline]
    fn clone(&self) -> RangerEntityRefs {
        RangerEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScWargRider: ::core::clone::Clone::clone(&self.ScWargRider),
            ScFearBreeze: ::core::clone::Clone::clone(&self.ScFearBreeze),
            ScAimedBolt: ::core::clone::Clone::clone(&self.ScAimedBolt),
        }
    }
}
impl AsArchEntityEnumRef for RangerEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Ranger(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Ranger {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<RangerComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Ranger {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(RangerComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScWargRider: ScWargRider,
        ScFearBreeze: ScFearBreeze,
        ScAimedBolt: ScAimedBolt,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScWargRider,
                ScFearBreeze,
                ScAimedBolt,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Ranger.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Ranger.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> RangerEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Ranger) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = RangerEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScWargRider: unsafe {
                storage.ScWargRider_mut().get_unchecked_mut(dense_usize)
            },
            ScFearBreeze: unsafe {
                storage.ScFearBreeze_mut().get_unchecked_mut(dense_usize)
            },
            ScAimedBolt: unsafe {
                storage.ScAimedBolt_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScWargRider(&self) -> &[ScWargRider] {
        unsafe { (*self.storage.get()).ScWargRider() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFearBreeze(&self) -> &[ScFearBreeze] {
        unsafe { (*self.storage.get()).ScFearBreeze() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAimedBolt(&self) -> &[ScAimedBolt] {
        unsafe { (*self.storage.get()).ScAimedBolt() }
    }
}
pub struct MinstrelWandererComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl MinstrelWandererComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScSongOfLutie>(),
                std::mem::align_of::<ScSongOfLutie>(),
            ),
            (
                std::mem::size_of::<ScDrumOnTheBattlefield>(),
                std::mem::align_of::<ScDrumOnTheBattlefield>(),
            ),
            (
                std::mem::size_of::<ScSaturdayNightFever>(),
                std::mem::align_of::<ScSaturdayNightFever>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScSongOfLutie,
        ScDrumOnTheBattlefield,
        ScSaturdayNightFever,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSongOfLutie;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScDrumOnTheBattlefield;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSaturdayNightFever;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSongOfLutie>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScSongOfLutie;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScDrumOnTheBattlefield>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScDrumOnTheBattlefield;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSaturdayNightFever>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScSaturdayNightFever;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSongOfLutie: ScSongOfLutie,
        ScDrumOnTheBattlefield: ScDrumOnTheBattlefield,
        ScSaturdayNightFever: ScSaturdayNightFever,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScSongOfLutie;
            type_base.add(self.len).write(ScSongOfLutie);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScDrumOnTheBattlefield;
            type_base.add(self.len).write(ScDrumOnTheBattlefield);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScSaturdayNightFever;
            type_base.add(self.len).write(ScSaturdayNightFever);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScSongOfLutie],
        &[ScDrumOnTheBattlefield],
        &[ScSaturdayNightFever],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScSongOfLutie,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScDrumOnTheBattlefield,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScSaturdayNightFever,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScSongOfLutie],
        &mut [ScDrumOnTheBattlefield],
        &mut [ScSaturdayNightFever],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScSongOfLutie,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScDrumOnTheBattlefield,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScSaturdayNightFever,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSongOfLutie(&self) -> &[ScSongOfLutie] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSongOfLutie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDrumOnTheBattlefield(&self) -> &[ScDrumOnTheBattlefield] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScDrumOnTheBattlefield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSaturdayNightFever(&self) -> &[ScSaturdayNightFever] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSaturdayNightFever,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSongOfLutie_mut(&self) -> &mut [ScSongOfLutie] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSongOfLutie,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScDrumOnTheBattlefield_mut(&self) -> &mut [ScDrumOnTheBattlefield] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScDrumOnTheBattlefield,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSaturdayNightFever_mut(&self) -> &mut [ScSaturdayNightFever] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSaturdayNightFever,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for MinstrelWandererComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSongOfLutie>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSongOfLutie;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScDrumOnTheBattlefield>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScDrumOnTheBattlefield;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSaturdayNightFever>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSaturdayNightFever;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct MinstrelWandererEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScSongOfLutie: *mut ScSongOfLutie,
    pub ScDrumOnTheBattlefield: *mut ScDrumOnTheBattlefield,
    pub ScSaturdayNightFever: *mut ScSaturdayNightFever,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for MinstrelWandererEntityRefs {
    #[inline]
    fn clone(&self) -> MinstrelWandererEntityRefs {
        MinstrelWandererEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScSongOfLutie: ::core::clone::Clone::clone(&self.ScSongOfLutie),
            ScDrumOnTheBattlefield: ::core::clone::Clone::clone(
                &self.ScDrumOnTheBattlefield,
            ),
            ScSaturdayNightFever: ::core::clone::Clone::clone(&self.ScSaturdayNightFever),
        }
    }
}
impl AsArchEntityEnumRef for MinstrelWandererEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::MinstrelWanderer(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct MinstrelWanderer {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<MinstrelWandererComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl MinstrelWanderer {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(MinstrelWandererComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSongOfLutie: ScSongOfLutie,
        ScDrumOnTheBattlefield: ScDrumOnTheBattlefield,
        ScSaturdayNightFever: ScSaturdayNightFever,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScSongOfLutie,
                ScDrumOnTheBattlefield,
                ScSaturdayNightFever,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::MinstrelWanderer.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::MinstrelWanderer.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> MinstrelWandererEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::MinstrelWanderer) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = MinstrelWandererEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScSongOfLutie: unsafe {
                storage.ScSongOfLutie_mut().get_unchecked_mut(dense_usize)
            },
            ScDrumOnTheBattlefield: unsafe {
                storage.ScDrumOnTheBattlefield_mut().get_unchecked_mut(dense_usize)
            },
            ScSaturdayNightFever: unsafe {
                storage.ScSaturdayNightFever_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSongOfLutie(&self) -> &[ScSongOfLutie] {
        unsafe { (*self.storage.get()).ScSongOfLutie() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScDrumOnTheBattlefield(&self) -> &[ScDrumOnTheBattlefield] {
        unsafe { (*self.storage.get()).ScDrumOnTheBattlefield() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSaturdayNightFever(&self) -> &[ScSaturdayNightFever] {
        unsafe { (*self.storage.get()).ScSaturdayNightFever() }
    }
}
pub struct SuperNoviceComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl SuperNoviceComponentStorage {
    pub const N: usize = 11;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScAngelus>(), std::mem::align_of::<ScAngelus>()),
            (std::mem::size_of::<ScBlessing>(), std::mem::align_of::<ScBlessing>()),
            (
                std::mem::size_of::<ScIncreaseAgi>(),
                std::mem::align_of::<ScIncreaseAgi>(),
            ),
            (std::mem::size_of::<ScFoodStr>(), std::mem::align_of::<ScFoodStr>()),
            (std::mem::size_of::<ScFoodInt>(), std::mem::align_of::<ScFoodInt>()),
            (std::mem::size_of::<ScFoodLuk>(), std::mem::align_of::<ScFoodLuk>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScAngelus,
        ScBlessing,
        ScIncreaseAgi,
        ScFoodStr,
        ScFoodInt,
        ScFoodLuk,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScAngelus;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScBlessing;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScFoodStr;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScFoodInt;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScFoodLuk;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAngelus>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScAngelus;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBlessing>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScBlessing;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScIncreaseAgi>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFoodStr>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScFoodStr;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFoodInt>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut ScFoodInt;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFoodLuk>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut ScFoodLuk;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[9];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[10];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScAngelus: ScAngelus,
        ScBlessing: ScBlessing,
        ScIncreaseAgi: ScIncreaseAgi,
        ScFoodStr: ScFoodStr,
        ScFoodInt: ScFoodInt,
        ScFoodLuk: ScFoodLuk,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScAngelus;
            type_base.add(self.len).write(ScAngelus);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScBlessing;
            type_base.add(self.len).write(ScBlessing);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScIncreaseAgi;
            type_base.add(self.len).write(ScIncreaseAgi);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScFoodStr;
            type_base.add(self.len).write(ScFoodStr);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut ScFoodInt;
            type_base.add(self.len).write(ScFoodInt);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut ScFoodLuk;
            type_base.add(self.len).write(ScFoodLuk);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[9];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[10];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScAngelus],
        &[ScBlessing],
        &[ScIncreaseAgi],
        &[ScFoodStr],
        &[ScFoodInt],
        &[ScFoodLuk],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScAngelus,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScBlessing,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScIncreaseAgi,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScFoodStr,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const ScFoodInt,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const ScFoodLuk,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[9]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[10]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScAngelus],
        &mut [ScBlessing],
        &mut [ScIncreaseAgi],
        &mut [ScFoodStr],
        &mut [ScFoodInt],
        &mut [ScFoodLuk],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScAngelus,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScBlessing,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScIncreaseAgi,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScFoodStr,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut ScFoodInt,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut ScFoodLuk,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[9]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[10]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAngelus(&self) -> &[ScAngelus] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAngelus,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBlessing(&self) -> &[ScBlessing] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBlessing,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi(&self) -> &[ScIncreaseAgi] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScIncreaseAgi,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodStr(&self) -> &[ScFoodStr] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFoodStr,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodInt(&self) -> &[ScFoodInt] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFoodInt,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodLuk(&self) -> &[ScFoodLuk] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFoodLuk,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAngelus_mut(&self) -> &mut [ScAngelus] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAngelus,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBlessing_mut(&self) -> &mut [ScBlessing] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBlessing,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi_mut(&self) -> &mut [ScIncreaseAgi] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScIncreaseAgi,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodStr_mut(&self) -> &mut [ScFoodStr] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFoodStr,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodInt_mut(&self) -> &mut [ScFoodInt] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFoodInt,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFoodLuk_mut(&self) -> &mut [ScFoodLuk] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFoodLuk,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for SuperNoviceComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAngelus>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScAngelus;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBlessing>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScBlessing;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScIncreaseAgi>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScIncreaseAgi;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFoodStr>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScFoodStr;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFoodInt>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScFoodInt;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFoodLuk>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScFoodLuk;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct SuperNoviceEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScAngelus: *mut ScAngelus,
    pub ScBlessing: *mut ScBlessing,
    pub ScIncreaseAgi: *mut ScIncreaseAgi,
    pub ScFoodStr: *mut ScFoodStr,
    pub ScFoodInt: *mut ScFoodInt,
    pub ScFoodLuk: *mut ScFoodLuk,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for SuperNoviceEntityRefs {
    #[inline]
    fn clone(&self) -> SuperNoviceEntityRefs {
        SuperNoviceEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScAngelus: ::core::clone::Clone::clone(&self.ScAngelus),
            ScBlessing: ::core::clone::Clone::clone(&self.ScBlessing),
            ScIncreaseAgi: ::core::clone::Clone::clone(&self.ScIncreaseAgi),
            ScFoodStr: ::core::clone::Clone::clone(&self.ScFoodStr),
            ScFoodInt: ::core::clone::Clone::clone(&self.ScFoodInt),
            ScFoodLuk: ::core::clone::Clone::clone(&self.ScFoodLuk),
        }
    }
}
impl AsArchEntityEnumRef for SuperNoviceEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::SuperNovice(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct SuperNovice {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<SuperNoviceComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl SuperNovice {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(SuperNoviceComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScAngelus: ScAngelus,
        ScBlessing: ScBlessing,
        ScIncreaseAgi: ScIncreaseAgi,
        ScFoodStr: ScFoodStr,
        ScFoodInt: ScFoodInt,
        ScFoodLuk: ScFoodLuk,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScAngelus,
                ScBlessing,
                ScIncreaseAgi,
                ScFoodStr,
                ScFoodInt,
                ScFoodLuk,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::SuperNovice.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::SuperNovice.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> SuperNoviceEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::SuperNovice) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = SuperNoviceEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScAngelus: unsafe { storage.ScAngelus_mut().get_unchecked_mut(dense_usize) },
            ScBlessing: unsafe {
                storage.ScBlessing_mut().get_unchecked_mut(dense_usize)
            },
            ScIncreaseAgi: unsafe {
                storage.ScIncreaseAgi_mut().get_unchecked_mut(dense_usize)
            },
            ScFoodStr: unsafe { storage.ScFoodStr_mut().get_unchecked_mut(dense_usize) },
            ScFoodInt: unsafe { storage.ScFoodInt_mut().get_unchecked_mut(dense_usize) },
            ScFoodLuk: unsafe { storage.ScFoodLuk_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAngelus(&self) -> &[ScAngelus] {
        unsafe { (*self.storage.get()).ScAngelus() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBlessing(&self) -> &[ScBlessing] {
        unsafe { (*self.storage.get()).ScBlessing() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScIncreaseAgi(&self) -> &[ScIncreaseAgi] {
        unsafe { (*self.storage.get()).ScIncreaseAgi() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFoodStr(&self) -> &[ScFoodStr] {
        unsafe { (*self.storage.get()).ScFoodStr() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFoodInt(&self) -> &[ScFoodInt] {
        unsafe { (*self.storage.get()).ScFoodInt() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFoodLuk(&self) -> &[ScFoodLuk] {
        unsafe { (*self.storage.get()).ScFoodLuk() }
    }
}
pub struct GunslingerComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl GunslingerComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScMadnessCanceler>(),
                std::mem::align_of::<ScMadnessCanceler>(),
            ),
            (std::mem::size_of::<ScLastStand>(), std::mem::align_of::<ScLastStand>()),
            (
                std::mem::size_of::<ScGatlingFever>(),
                std::mem::align_of::<ScGatlingFever>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScMadnessCanceler,
        ScLastStand,
        ScGatlingFever,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadnessCanceler;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScLastStand;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScGatlingFever;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMadnessCanceler>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScMadnessCanceler;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScLastStand>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScLastStand;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScGatlingFever>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScGatlingFever;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadnessCanceler: ScMadnessCanceler,
        ScLastStand: ScLastStand,
        ScGatlingFever: ScGatlingFever,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScMadnessCanceler;
            type_base.add(self.len).write(ScMadnessCanceler);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScLastStand;
            type_base.add(self.len).write(ScLastStand);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScGatlingFever;
            type_base.add(self.len).write(ScGatlingFever);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScMadnessCanceler],
        &[ScLastStand],
        &[ScGatlingFever],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScMadnessCanceler,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScLastStand,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScGatlingFever,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScMadnessCanceler],
        &mut [ScLastStand],
        &mut [ScGatlingFever],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScMadnessCanceler,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScLastStand,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScGatlingFever,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadnessCanceler(&self) -> &[ScMadnessCanceler] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMadnessCanceler,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScLastStand(&self) -> &[ScLastStand] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScLastStand,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGatlingFever(&self) -> &[ScGatlingFever] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScGatlingFever,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMadnessCanceler_mut(&self) -> &mut [ScMadnessCanceler] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMadnessCanceler,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScLastStand_mut(&self) -> &mut [ScLastStand] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScLastStand,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScGatlingFever_mut(&self) -> &mut [ScGatlingFever] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScGatlingFever,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for GunslingerComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMadnessCanceler>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScMadnessCanceler;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScLastStand>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScLastStand;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScGatlingFever>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScGatlingFever;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct GunslingerEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScMadnessCanceler: *mut ScMadnessCanceler,
    pub ScLastStand: *mut ScLastStand,
    pub ScGatlingFever: *mut ScGatlingFever,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for GunslingerEntityRefs {
    #[inline]
    fn clone(&self) -> GunslingerEntityRefs {
        GunslingerEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScMadnessCanceler: ::core::clone::Clone::clone(&self.ScMadnessCanceler),
            ScLastStand: ::core::clone::Clone::clone(&self.ScLastStand),
            ScGatlingFever: ::core::clone::Clone::clone(&self.ScGatlingFever),
        }
    }
}
impl AsArchEntityEnumRef for GunslingerEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Gunslinger(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Gunslinger {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<GunslingerComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Gunslinger {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(GunslingerComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScMadnessCanceler: ScMadnessCanceler,
        ScLastStand: ScLastStand,
        ScGatlingFever: ScGatlingFever,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScMadnessCanceler,
                ScLastStand,
                ScGatlingFever,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Gunslinger.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Gunslinger.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> GunslingerEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Gunslinger) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = GunslingerEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScMadnessCanceler: unsafe {
                storage.ScMadnessCanceler_mut().get_unchecked_mut(dense_usize)
            },
            ScLastStand: unsafe {
                storage.ScLastStand_mut().get_unchecked_mut(dense_usize)
            },
            ScGatlingFever: unsafe {
                storage.ScGatlingFever_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMadnessCanceler(&self) -> &[ScMadnessCanceler] {
        unsafe { (*self.storage.get()).ScMadnessCanceler() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScLastStand(&self) -> &[ScLastStand] {
        unsafe { (*self.storage.get()).ScLastStand() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScGatlingFever(&self) -> &[ScGatlingFever] {
        unsafe { (*self.storage.get()).ScGatlingFever() }
    }
}
pub struct NinjaComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl NinjaComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScCloaking>(), std::mem::align_of::<ScCloaking>()),
            (std::mem::size_of::<ScShadowLeap>(), std::mem::align_of::<ScShadowLeap>()),
            (
                std::mem::size_of::<ScMirrorImage>(),
                std::mem::align_of::<ScMirrorImage>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScCloaking, ScShadowLeap, ScMirrorImage, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCloaking;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScShadowLeap;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMirrorImage;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCloaking>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScCloaking;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScShadowLeap>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScShadowLeap;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScMirrorImage>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScMirrorImage;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCloaking: ScCloaking,
        ScShadowLeap: ScShadowLeap,
        ScMirrorImage: ScMirrorImage,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScCloaking;
            type_base.add(self.len).write(ScCloaking);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScShadowLeap;
            type_base.add(self.len).write(ScShadowLeap);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScMirrorImage;
            type_base.add(self.len).write(ScMirrorImage);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScCloaking],
        &[ScShadowLeap],
        &[ScMirrorImage],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScCloaking,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScShadowLeap,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScMirrorImage,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScCloaking],
        &mut [ScShadowLeap],
        &mut [ScMirrorImage],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScCloaking,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScShadowLeap,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScMirrorImage,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCloaking(&self) -> &[ScCloaking] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCloaking,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScShadowLeap(&self) -> &[ScShadowLeap] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScShadowLeap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMirrorImage(&self) -> &[ScMirrorImage] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScMirrorImage,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCloaking_mut(&self) -> &mut [ScCloaking] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCloaking,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScShadowLeap_mut(&self) -> &mut [ScShadowLeap] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScShadowLeap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScMirrorImage_mut(&self) -> &mut [ScMirrorImage] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScMirrorImage,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for NinjaComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCloaking>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCloaking;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScShadowLeap>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScShadowLeap;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScMirrorImage>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScMirrorImage;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct NinjaEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScCloaking: *mut ScCloaking,
    pub ScShadowLeap: *mut ScShadowLeap,
    pub ScMirrorImage: *mut ScMirrorImage,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for NinjaEntityRefs {
    #[inline]
    fn clone(&self) -> NinjaEntityRefs {
        NinjaEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScCloaking: ::core::clone::Clone::clone(&self.ScCloaking),
            ScShadowLeap: ::core::clone::Clone::clone(&self.ScShadowLeap),
            ScMirrorImage: ::core::clone::Clone::clone(&self.ScMirrorImage),
        }
    }
}
impl AsArchEntityEnumRef for NinjaEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Ninja(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Ninja {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<NinjaComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Ninja {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(NinjaComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCloaking: ScCloaking,
        ScShadowLeap: ScShadowLeap,
        ScMirrorImage: ScMirrorImage,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScCloaking,
                ScShadowLeap,
                ScMirrorImage,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Ninja.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Ninja.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> NinjaEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Ninja) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = NinjaEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScCloaking: unsafe {
                storage.ScCloaking_mut().get_unchecked_mut(dense_usize)
            },
            ScShadowLeap: unsafe {
                storage.ScShadowLeap_mut().get_unchecked_mut(dense_usize)
            },
            ScMirrorImage: unsafe {
                storage.ScMirrorImage_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCloaking(&self) -> &[ScCloaking] {
        unsafe { (*self.storage.get()).ScCloaking() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScShadowLeap(&self) -> &[ScShadowLeap] {
        unsafe { (*self.storage.get()).ScShadowLeap() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScMirrorImage(&self) -> &[ScMirrorImage] {
        unsafe { (*self.storage.get()).ScMirrorImage() }
    }
}
pub struct KagerouOboroComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl KagerouOboroComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScKunaiExplosion>(),
                std::mem::align_of::<ScKunaiExplosion>(),
            ),
            (
                std::mem::size_of::<ScSwirlingPetal>(),
                std::mem::align_of::<ScSwirlingPetal>(),
            ),
            (std::mem::size_of::<ScCrossSlash>(), std::mem::align_of::<ScCrossSlash>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScKunaiExplosion,
        ScSwirlingPetal,
        ScCrossSlash,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScKunaiExplosion;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScSwirlingPetal;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScCrossSlash;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScKunaiExplosion>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScKunaiExplosion;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSwirlingPetal>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScSwirlingPetal;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCrossSlash>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScCrossSlash;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScKunaiExplosion: ScKunaiExplosion,
        ScSwirlingPetal: ScSwirlingPetal,
        ScCrossSlash: ScCrossSlash,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScKunaiExplosion;
            type_base.add(self.len).write(ScKunaiExplosion);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScSwirlingPetal;
            type_base.add(self.len).write(ScSwirlingPetal);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScCrossSlash;
            type_base.add(self.len).write(ScCrossSlash);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScKunaiExplosion],
        &[ScSwirlingPetal],
        &[ScCrossSlash],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScKunaiExplosion,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScSwirlingPetal,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScCrossSlash,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScKunaiExplosion],
        &mut [ScSwirlingPetal],
        &mut [ScCrossSlash],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScKunaiExplosion,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScSwirlingPetal,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScCrossSlash,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKunaiExplosion(&self) -> &[ScKunaiExplosion] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScKunaiExplosion,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSwirlingPetal(&self) -> &[ScSwirlingPetal] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSwirlingPetal,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCrossSlash(&self) -> &[ScCrossSlash] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCrossSlash,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScKunaiExplosion_mut(&self) -> &mut [ScKunaiExplosion] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScKunaiExplosion,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSwirlingPetal_mut(&self) -> &mut [ScSwirlingPetal] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSwirlingPetal,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCrossSlash_mut(&self) -> &mut [ScCrossSlash] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCrossSlash,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for KagerouOboroComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScKunaiExplosion>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScKunaiExplosion;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSwirlingPetal>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScSwirlingPetal;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCrossSlash>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScCrossSlash;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct KagerouOboroEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScKunaiExplosion: *mut ScKunaiExplosion,
    pub ScSwirlingPetal: *mut ScSwirlingPetal,
    pub ScCrossSlash: *mut ScCrossSlash,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for KagerouOboroEntityRefs {
    #[inline]
    fn clone(&self) -> KagerouOboroEntityRefs {
        KagerouOboroEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScKunaiExplosion: ::core::clone::Clone::clone(&self.ScKunaiExplosion),
            ScSwirlingPetal: ::core::clone::Clone::clone(&self.ScSwirlingPetal),
            ScCrossSlash: ::core::clone::Clone::clone(&self.ScCrossSlash),
        }
    }
}
impl AsArchEntityEnumRef for KagerouOboroEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::KagerouOboro(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct KagerouOboro {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<KagerouOboroComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl KagerouOboro {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(KagerouOboroComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScKunaiExplosion: ScKunaiExplosion,
        ScSwirlingPetal: ScSwirlingPetal,
        ScCrossSlash: ScCrossSlash,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScKunaiExplosion,
                ScSwirlingPetal,
                ScCrossSlash,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::KagerouOboro.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::KagerouOboro.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(
        &self,
        handle: ::ecs::Handle,
    ) -> KagerouOboroEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::KagerouOboro) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = KagerouOboroEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScKunaiExplosion: unsafe {
                storage.ScKunaiExplosion_mut().get_unchecked_mut(dense_usize)
            },
            ScSwirlingPetal: unsafe {
                storage.ScSwirlingPetal_mut().get_unchecked_mut(dense_usize)
            },
            ScCrossSlash: unsafe {
                storage.ScCrossSlash_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScKunaiExplosion(&self) -> &[ScKunaiExplosion] {
        unsafe { (*self.storage.get()).ScKunaiExplosion() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSwirlingPetal(&self) -> &[ScSwirlingPetal] {
        unsafe { (*self.storage.get()).ScSwirlingPetal() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCrossSlash(&self) -> &[ScCrossSlash] {
        unsafe { (*self.storage.get()).ScCrossSlash() }
    }
}
pub struct RebellionComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl RebellionComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScHeatBarrel>(), std::mem::align_of::<ScHeatBarrel>()),
            (
                std::mem::size_of::<ScAntiMaterialBlast>(),
                std::mem::align_of::<ScAntiMaterialBlast>(),
            ),
            (
                std::mem::size_of::<ScEternalChain>(),
                std::mem::align_of::<ScEternalChain>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScHeatBarrel,
        ScAntiMaterialBlast,
        ScEternalChain,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAntiMaterialBlast;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScEternalChain;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScHeatBarrel>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScAntiMaterialBlast>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScAntiMaterialBlast;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScEternalChain>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScEternalChain;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScHeatBarrel: ScHeatBarrel,
        ScAntiMaterialBlast: ScAntiMaterialBlast,
        ScEternalChain: ScEternalChain,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScHeatBarrel;
            type_base.add(self.len).write(ScHeatBarrel);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScAntiMaterialBlast;
            type_base.add(self.len).write(ScAntiMaterialBlast);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScEternalChain;
            type_base.add(self.len).write(ScEternalChain);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScHeatBarrel],
        &[ScAntiMaterialBlast],
        &[ScEternalChain],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScHeatBarrel,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScAntiMaterialBlast,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScEternalChain,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScHeatBarrel],
        &mut [ScAntiMaterialBlast],
        &mut [ScEternalChain],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScHeatBarrel,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScAntiMaterialBlast,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScEternalChain,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel(&self) -> &[ScHeatBarrel] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScHeatBarrel,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAntiMaterialBlast(&self) -> &[ScAntiMaterialBlast] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScAntiMaterialBlast,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEternalChain(&self) -> &[ScEternalChain] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScEternalChain,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel_mut(&self) -> &mut [ScHeatBarrel] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScHeatBarrel,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScAntiMaterialBlast_mut(&self) -> &mut [ScAntiMaterialBlast] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScAntiMaterialBlast,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScEternalChain_mut(&self) -> &mut [ScEternalChain] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScEternalChain,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for RebellionComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScHeatBarrel>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScHeatBarrel;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScAntiMaterialBlast>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScAntiMaterialBlast;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScEternalChain>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScEternalChain;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct RebellionEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScHeatBarrel: *mut ScHeatBarrel,
    pub ScAntiMaterialBlast: *mut ScAntiMaterialBlast,
    pub ScEternalChain: *mut ScEternalChain,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for RebellionEntityRefs {
    #[inline]
    fn clone(&self) -> RebellionEntityRefs {
        RebellionEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScHeatBarrel: ::core::clone::Clone::clone(&self.ScHeatBarrel),
            ScAntiMaterialBlast: ::core::clone::Clone::clone(&self.ScAntiMaterialBlast),
            ScEternalChain: ::core::clone::Clone::clone(&self.ScEternalChain),
        }
    }
}
impl AsArchEntityEnumRef for RebellionEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Rebellion(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Rebellion {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<RebellionComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Rebellion {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(RebellionComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScHeatBarrel: ScHeatBarrel,
        ScAntiMaterialBlast: ScAntiMaterialBlast,
        ScEternalChain: ScEternalChain,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScHeatBarrel,
                ScAntiMaterialBlast,
                ScEternalChain,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Rebellion.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Rebellion.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> RebellionEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Rebellion) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = RebellionEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScHeatBarrel: unsafe {
                storage.ScHeatBarrel_mut().get_unchecked_mut(dense_usize)
            },
            ScAntiMaterialBlast: unsafe {
                storage.ScAntiMaterialBlast_mut().get_unchecked_mut(dense_usize)
            },
            ScEternalChain: unsafe {
                storage.ScEternalChain_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScHeatBarrel(&self) -> &[ScHeatBarrel] {
        unsafe { (*self.storage.get()).ScHeatBarrel() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScAntiMaterialBlast(&self) -> &[ScAntiMaterialBlast] {
        unsafe { (*self.storage.get()).ScAntiMaterialBlast() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScEternalChain(&self) -> &[ScEternalChain] {
        unsafe { (*self.storage.get()).ScEternalChain() }
    }
}
pub struct SummonerComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl SummonerComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (
                std::mem::size_of::<ScCatnipMeteor>(),
                std::mem::align_of::<ScCatnipMeteor>(),
            ),
            (std::mem::size_of::<ScPickyPeck>(), std::mem::align_of::<ScPickyPeck>()),
            (
                std::mem::size_of::<ScScarOfTarou>(),
                std::mem::align_of::<ScScarOfTarou>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScCatnipMeteor,
        ScPickyPeck,
        ScScarOfTarou,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCatnipMeteor;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPickyPeck;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScScarOfTarou;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCatnipMeteor>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScCatnipMeteor;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPickyPeck>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScPickyPeck;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScScarOfTarou>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScScarOfTarou;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCatnipMeteor: ScCatnipMeteor,
        ScPickyPeck: ScPickyPeck,
        ScScarOfTarou: ScScarOfTarou,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScCatnipMeteor;
            type_base.add(self.len).write(ScCatnipMeteor);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScPickyPeck;
            type_base.add(self.len).write(ScPickyPeck);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScScarOfTarou;
            type_base.add(self.len).write(ScScarOfTarou);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScCatnipMeteor],
        &[ScPickyPeck],
        &[ScScarOfTarou],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScCatnipMeteor,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScPickyPeck,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScScarOfTarou,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScCatnipMeteor],
        &mut [ScPickyPeck],
        &mut [ScScarOfTarou],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScCatnipMeteor,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScPickyPeck,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScScarOfTarou,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCatnipMeteor(&self) -> &[ScCatnipMeteor] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCatnipMeteor,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPickyPeck(&self) -> &[ScPickyPeck] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPickyPeck,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScScarOfTarou(&self) -> &[ScScarOfTarou] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScScarOfTarou,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCatnipMeteor_mut(&self) -> &mut [ScCatnipMeteor] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCatnipMeteor,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPickyPeck_mut(&self) -> &mut [ScPickyPeck] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPickyPeck,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScScarOfTarou_mut(&self) -> &mut [ScScarOfTarou] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScScarOfTarou,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for SummonerComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCatnipMeteor>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScCatnipMeteor;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPickyPeck>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScPickyPeck;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScScarOfTarou>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScScarOfTarou;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct SummonerEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScCatnipMeteor: *mut ScCatnipMeteor,
    pub ScPickyPeck: *mut ScPickyPeck,
    pub ScScarOfTarou: *mut ScScarOfTarou,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for SummonerEntityRefs {
    #[inline]
    fn clone(&self) -> SummonerEntityRefs {
        SummonerEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScCatnipMeteor: ::core::clone::Clone::clone(&self.ScCatnipMeteor),
            ScPickyPeck: ::core::clone::Clone::clone(&self.ScPickyPeck),
            ScScarOfTarou: ::core::clone::Clone::clone(&self.ScScarOfTarou),
        }
    }
}
impl AsArchEntityEnumRef for SummonerEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Summoner(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Summoner {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<SummonerComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Summoner {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(SummonerComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScCatnipMeteor: ScCatnipMeteor,
        ScPickyPeck: ScPickyPeck,
        ScScarOfTarou: ScScarOfTarou,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScCatnipMeteor,
                ScPickyPeck,
                ScScarOfTarou,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Summoner.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Summoner.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> SummonerEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Summoner) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = SummonerEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScCatnipMeteor: unsafe {
                storage.ScCatnipMeteor_mut().get_unchecked_mut(dense_usize)
            },
            ScPickyPeck: unsafe {
                storage.ScPickyPeck_mut().get_unchecked_mut(dense_usize)
            },
            ScScarOfTarou: unsafe {
                storage.ScScarOfTarou_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCatnipMeteor(&self) -> &[ScCatnipMeteor] {
        unsafe { (*self.storage.get()).ScCatnipMeteor() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPickyPeck(&self) -> &[ScPickyPeck] {
        unsafe { (*self.storage.get()).ScPickyPeck() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScScarOfTarou(&self) -> &[ScScarOfTarou] {
        unsafe { (*self.storage.get()).ScScarOfTarou() }
    }
}
pub struct StarEmperorComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl StarEmperorComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScSolarBurst>(), std::mem::align_of::<ScSolarBurst>()),
            (
                std::mem::size_of::<ScFullMoonKick>(),
                std::mem::align_of::<ScFullMoonKick>(),
            ),
            (
                std::mem::size_of::<ScFallingStar>(),
                std::mem::align_of::<ScFallingStar>(),
            ),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Velocity,
        ScSolarBurst,
        ScFullMoonKick,
        ScFallingStar,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSolarBurst;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFullMoonKick;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScFallingStar;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSolarBurst>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScSolarBurst;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFullMoonKick>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScFullMoonKick;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFallingStar>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScFallingStar;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSolarBurst: ScSolarBurst,
        ScFullMoonKick: ScFullMoonKick,
        ScFallingStar: ScFallingStar,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScSolarBurst;
            type_base.add(self.len).write(ScSolarBurst);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScFullMoonKick;
            type_base.add(self.len).write(ScFullMoonKick);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScFallingStar;
            type_base.add(self.len).write(ScFallingStar);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScSolarBurst],
        &[ScFullMoonKick],
        &[ScFallingStar],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScSolarBurst,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScFullMoonKick,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScFallingStar,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScSolarBurst],
        &mut [ScFullMoonKick],
        &mut [ScFallingStar],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScSolarBurst,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScFullMoonKick,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScFallingStar,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSolarBurst(&self) -> &[ScSolarBurst] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSolarBurst,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFullMoonKick(&self) -> &[ScFullMoonKick] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFullMoonKick,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFallingStar(&self) -> &[ScFallingStar] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFallingStar,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSolarBurst_mut(&self) -> &mut [ScSolarBurst] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSolarBurst,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFullMoonKick_mut(&self) -> &mut [ScFullMoonKick] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFullMoonKick,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFallingStar_mut(&self) -> &mut [ScFallingStar] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFallingStar,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for StarEmperorComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSolarBurst>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSolarBurst;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFullMoonKick>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFullMoonKick;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFallingStar>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScFallingStar;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct StarEmperorEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScSolarBurst: *mut ScSolarBurst,
    pub ScFullMoonKick: *mut ScFullMoonKick,
    pub ScFallingStar: *mut ScFallingStar,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for StarEmperorEntityRefs {
    #[inline]
    fn clone(&self) -> StarEmperorEntityRefs {
        StarEmperorEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScSolarBurst: ::core::clone::Clone::clone(&self.ScSolarBurst),
            ScFullMoonKick: ::core::clone::Clone::clone(&self.ScFullMoonKick),
            ScFallingStar: ::core::clone::Clone::clone(&self.ScFallingStar),
        }
    }
}
impl AsArchEntityEnumRef for StarEmperorEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::StarEmperor(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct StarEmperor {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<StarEmperorComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl StarEmperor {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(StarEmperorComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSolarBurst: ScSolarBurst,
        ScFullMoonKick: ScFullMoonKick,
        ScFallingStar: ScFallingStar,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScSolarBurst,
                ScFullMoonKick,
                ScFallingStar,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::StarEmperor.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::StarEmperor.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> StarEmperorEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::StarEmperor) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = StarEmperorEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScSolarBurst: unsafe {
                storage.ScSolarBurst_mut().get_unchecked_mut(dense_usize)
            },
            ScFullMoonKick: unsafe {
                storage.ScFullMoonKick_mut().get_unchecked_mut(dense_usize)
            },
            ScFallingStar: unsafe {
                storage.ScFallingStar_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSolarBurst(&self) -> &[ScSolarBurst] {
        unsafe { (*self.storage.get()).ScSolarBurst() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFullMoonKick(&self) -> &[ScFullMoonKick] {
        unsafe { (*self.storage.get()).ScFullMoonKick() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFallingStar(&self) -> &[ScFallingStar] {
        unsafe { (*self.storage.get()).ScFallingStar() }
    }
}
pub struct SoulReaperComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl SoulReaperComponentStorage {
    pub const N: usize = 8;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Velocity>(), std::mem::align_of::<Velocity>()),
            (std::mem::size_of::<ScSoulReap>(), std::mem::align_of::<ScSoulReap>()),
            (
                std::mem::size_of::<ScCurseOfSoul>(),
                std::mem::align_of::<ScCurseOfSoul>(),
            ),
            (std::mem::size_of::<ScSoulUnity>(), std::mem::align_of::<ScSoulUnity>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (Position, Velocity, ScSoulReap, ScCurseOfSoul, ScSoulUnity, u32, u32, u32) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSoulReap;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScCurseOfSoul;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSoulUnity;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Velocity>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Velocity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSoulReap>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScSoulReap;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCurseOfSoul>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScCurseOfSoul;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSoulUnity>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScSoulUnity;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSoulReap: ScSoulReap,
        ScCurseOfSoul: ScCurseOfSoul,
        ScSoulUnity: ScSoulUnity,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Velocity;
            type_base.add(self.len).write(Velocity);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScSoulReap;
            type_base.add(self.len).write(ScSoulReap);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScCurseOfSoul;
            type_base.add(self.len).write(ScCurseOfSoul);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScSoulUnity;
            type_base.add(self.len).write(ScSoulUnity);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Velocity],
        &[ScSoulReap],
        &[ScCurseOfSoul],
        &[ScSoulUnity],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScSoulReap,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScCurseOfSoul,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScSoulUnity,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Velocity],
        &mut [ScSoulReap],
        &mut [ScCurseOfSoul],
        &mut [ScSoulUnity],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Velocity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScSoulReap,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScCurseOfSoul,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScSoulUnity,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulReap(&self) -> &[ScSoulReap] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSoulReap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurseOfSoul(&self) -> &[ScCurseOfSoul] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCurseOfSoul,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulUnity(&self) -> &[ScSoulUnity] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSoulUnity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Velocity_mut(&self) -> &mut [Velocity] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Velocity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulReap_mut(&self) -> &mut [ScSoulReap] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSoulReap,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurseOfSoul_mut(&self) -> &mut [ScCurseOfSoul] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCurseOfSoul,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSoulUnity_mut(&self) -> &mut [ScSoulUnity] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSoulUnity,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for SoulReaperComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Velocity>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Velocity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSoulReap>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScSoulReap;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCurseOfSoul>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScCurseOfSoul;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSoulUnity>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScSoulUnity;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct SoulReaperEntityRefs {
    pub Position: *mut Position,
    pub Velocity: *mut Velocity,
    pub ScSoulReap: *mut ScSoulReap,
    pub ScCurseOfSoul: *mut ScCurseOfSoul,
    pub ScSoulUnity: *mut ScSoulUnity,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for SoulReaperEntityRefs {
    #[inline]
    fn clone(&self) -> SoulReaperEntityRefs {
        SoulReaperEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Velocity: ::core::clone::Clone::clone(&self.Velocity),
            ScSoulReap: ::core::clone::Clone::clone(&self.ScSoulReap),
            ScCurseOfSoul: ::core::clone::Clone::clone(&self.ScCurseOfSoul),
            ScSoulUnity: ::core::clone::Clone::clone(&self.ScSoulUnity),
        }
    }
}
impl AsArchEntityEnumRef for SoulReaperEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::SoulReaper(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct SoulReaper {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<SoulReaperComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl SoulReaper {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(SoulReaperComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Velocity: Velocity,
        ScSoulReap: ScSoulReap,
        ScCurseOfSoul: ScCurseOfSoul,
        ScSoulUnity: ScSoulUnity,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Velocity,
                ScSoulReap,
                ScCurseOfSoul,
                ScSoulUnity,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::SoulReaper.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::SoulReaper.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> SoulReaperEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::SoulReaper) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = SoulReaperEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Velocity: unsafe { storage.Velocity_mut().get_unchecked_mut(dense_usize) },
            ScSoulReap: unsafe {
                storage.ScSoulReap_mut().get_unchecked_mut(dense_usize)
            },
            ScCurseOfSoul: unsafe {
                storage.ScCurseOfSoul_mut().get_unchecked_mut(dense_usize)
            },
            ScSoulUnity: unsafe {
                storage.ScSoulUnity_mut().get_unchecked_mut(dense_usize)
            },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Velocity(&self) -> &[Velocity] {
        unsafe { (*self.storage.get()).Velocity() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSoulReap(&self) -> &[ScSoulReap] {
        unsafe { (*self.storage.get()).ScSoulReap() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCurseOfSoul(&self) -> &[ScCurseOfSoul] {
        unsafe { (*self.storage.get()).ScCurseOfSoul() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSoulUnity(&self) -> &[ScSoulUnity] {
        unsafe { (*self.storage.get()).ScSoulUnity() }
    }
}
pub struct DummyComponentStorage {
    ptr: *mut u8,
    len: usize,
    cap: usize,
}
impl DummyComponentStorage {
    pub const N: usize = 13;
    pub fn new() -> Self {
        Self {
            ptr: std::ptr::null_mut(),
            len: 0,
            cap: 0,
        }
    }
    pub fn with_capacity(cap: usize) -> Self {
        let mut s = Self::new();
        s.reallocate_to(cap);
        s
    }
    pub fn len(&self) -> usize {
        self.len
    }
    pub fn capacity(&self) -> usize {
        self.cap
    }
    const fn type_infos() -> [(usize, usize); Self::N] {
        [
            (std::mem::size_of::<Position>(), std::mem::align_of::<Position>()),
            (std::mem::size_of::<Health>(), std::mem::align_of::<Health>()),
            (std::mem::size_of::<ScStone>(), std::mem::align_of::<ScStone>()),
            (std::mem::size_of::<ScFreeze>(), std::mem::align_of::<ScFreeze>()),
            (std::mem::size_of::<ScStun>(), std::mem::align_of::<ScStun>()),
            (std::mem::size_of::<ScSleep>(), std::mem::align_of::<ScSleep>()),
            (std::mem::size_of::<ScBurning>(), std::mem::align_of::<ScBurning>()),
            (std::mem::size_of::<ScPoison>(), std::mem::align_of::<ScPoison>()),
            (std::mem::size_of::<ScBleeding>(), std::mem::align_of::<ScBleeding>()),
            (std::mem::size_of::<ScCurse>(), std::mem::align_of::<ScCurse>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
            (std::mem::size_of::<u32>(), std::mem::align_of::<u32>()),
        ]
    }
    fn offsets(cap: usize) -> [usize; Self::N] {
        let infos = Self::type_infos();
        let mut out = [0; Self::N];
        let mut current_offset = 0;
        let mut i = 0;
        while i < Self::N {
            let (size, align) = infos[i];
            if align > 0 {
                let remainder = current_offset % align;
                if remainder != 0 {
                    current_offset += align - remainder;
                }
            }
            out[i] = current_offset;
            current_offset += cap * size;
            i += 1;
        }
        out
    }
    fn layout(cap: usize) -> std::alloc::Layout {
        if cap == 0 {
            return std::alloc::Layout::new::<u8>();
        }
        let infos = Self::type_infos();
        let offsets = Self::offsets(cap);
        let last_idx = Self::N - 1;
        let (last_size, _) = infos[last_idx];
        let total_size = offsets[last_idx] + (cap * last_size);
        let mut max_align = 1;
        let mut i = 0;
        while i < Self::N {
            let (_, align) = infos[i];
            if align > max_align {
                max_align = align;
            }
            i += 1;
        }
        std::alloc::Layout::from_size_align(total_size, max_align).unwrap()
    }
    fn reallocate_to(&mut self, new_cap: usize) {
        let old_cap = self.cap;
        if new_cap == old_cap {
            return;
        }
        if new_cap == 0 {
            if old_cap > 0 {
                unsafe {
                    std::alloc::dealloc(self.ptr, Self::layout(old_cap));
                }
            }
            self.ptr = std::ptr::null_mut();
            self.cap = 0;
            self.len = 0;
            return;
        }
        let old_layout = Self::layout(old_cap);
        let new_layout = Self::layout(new_cap);
        unsafe {
            let new_ptr = if old_cap == 0 {
                std::alloc::alloc(new_layout)
            } else {
                std::alloc::realloc(self.ptr, old_layout, new_layout.size())
            };
            if new_ptr.is_null() {
                std::alloc::handle_alloc_error(new_layout);
            }
            self.ptr = new_ptr;
            self.cap = new_cap;
            if self.len > 0 && old_cap > 0 {
                let old_offsets = Self::offsets(old_cap);
                let new_offsets = Self::offsets(new_cap);
                let infos = Self::type_infos();
                for i in (1..Self::N).rev() {
                    let (size, _) = infos[i];
                    let size_bytes = self.len * size;
                    let src = self.ptr.add(old_offsets[i]);
                    let dst = self.ptr.add(new_offsets[i]);
                    std::ptr::copy(src, dst, size_bytes);
                }
            }
        }
    }
    fn grow(&mut self) {
        let new_cap = if self.cap == 0 { 4 } else { self.cap * 2 };
        self.reallocate_to(new_cap);
    }
    pub fn swap_remove(
        &mut self,
        index: usize,
    ) -> (
        Position,
        Health,
        ScStone,
        ScFreeze,
        ScStun,
        ScSleep,
        ScBurning,
        ScPoison,
        ScBleeding,
        ScCurse,
        u32,
        u32,
        u32,
    ) {
        if true {
            if !(index < self.len) {
                ::core::panicking::panic("assertion failed: index < self.len")
            }
        }
        let offsets = Self::offsets(self.cap);
        let last_idx = self.len - 1;
        unsafe {
            let result = (
                {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Health;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScStone;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFreeze;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScStun;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScSleep;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScBurning;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScPoison;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut ScBleeding;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut ScCurse;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[11];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
                {
                    let offset = offsets[12];
                    let base = self.ptr.add(offset) as *mut u32;
                    let dst = base.add(index);
                    let val = std::ptr::read(dst);
                    if index != last_idx {
                        let src = base.add(last_idx);
                        std::ptr::copy_nonoverlapping(src, dst, 1);
                    }
                    val
                },
            );
            self.len -= 1;
            result
        }
    }
    pub fn clear(&mut self) {
        if self.len == 0 {
            return;
        }
        let offsets = Self::offsets(self.cap);
        let len = self.len;
        unsafe {
            if std::mem::needs_drop::<Position>() {
                let offset = offsets[0];
                let base = self.ptr.add(offset) as *mut Position;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<Health>() {
                let offset = offsets[1];
                let base = self.ptr.add(offset) as *mut Health;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScStone>() {
                let offset = offsets[2];
                let base = self.ptr.add(offset) as *mut ScStone;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScFreeze>() {
                let offset = offsets[3];
                let base = self.ptr.add(offset) as *mut ScFreeze;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScStun>() {
                let offset = offsets[4];
                let base = self.ptr.add(offset) as *mut ScStun;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScSleep>() {
                let offset = offsets[5];
                let base = self.ptr.add(offset) as *mut ScSleep;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBurning>() {
                let offset = offsets[6];
                let base = self.ptr.add(offset) as *mut ScBurning;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScPoison>() {
                let offset = offsets[7];
                let base = self.ptr.add(offset) as *mut ScPoison;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScBleeding>() {
                let offset = offsets[8];
                let base = self.ptr.add(offset) as *mut ScBleeding;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<ScCurse>() {
                let offset = offsets[9];
                let base = self.ptr.add(offset) as *mut ScCurse;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[10];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[11];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
            if std::mem::needs_drop::<u32>() {
                let offset = offsets[12];
                let base = self.ptr.add(offset) as *mut u32;
                for i in 0..len {
                    std::ptr::drop_in_place(base.add(i));
                }
            }
        }
        self.len = 0;
    }
    #[allow(nonstandard_style)]
    pub fn push(
        &mut self,
        Position: Position,
        Health: Health,
        ScStone: ScStone,
        ScFreeze: ScFreeze,
        ScStun: ScStun,
        ScSleep: ScSleep,
        ScBurning: ScBurning,
        ScPoison: ScPoison,
        ScBleeding: ScBleeding,
        ScCurse: ScCurse,
        slots: u32,
        dense_to_slot: u32,
        free_slots: u32,
    ) {
        if self.len == self.cap {
            self.grow();
        }
        let offsets = Self::offsets(self.cap);
        unsafe {
            let offset = offsets[0];
            let type_base = self.ptr.add(offset) as *mut Position;
            type_base.add(self.len).write(Position);
            let offset = offsets[1];
            let type_base = self.ptr.add(offset) as *mut Health;
            type_base.add(self.len).write(Health);
            let offset = offsets[2];
            let type_base = self.ptr.add(offset) as *mut ScStone;
            type_base.add(self.len).write(ScStone);
            let offset = offsets[3];
            let type_base = self.ptr.add(offset) as *mut ScFreeze;
            type_base.add(self.len).write(ScFreeze);
            let offset = offsets[4];
            let type_base = self.ptr.add(offset) as *mut ScStun;
            type_base.add(self.len).write(ScStun);
            let offset = offsets[5];
            let type_base = self.ptr.add(offset) as *mut ScSleep;
            type_base.add(self.len).write(ScSleep);
            let offset = offsets[6];
            let type_base = self.ptr.add(offset) as *mut ScBurning;
            type_base.add(self.len).write(ScBurning);
            let offset = offsets[7];
            let type_base = self.ptr.add(offset) as *mut ScPoison;
            type_base.add(self.len).write(ScPoison);
            let offset = offsets[8];
            let type_base = self.ptr.add(offset) as *mut ScBleeding;
            type_base.add(self.len).write(ScBleeding);
            let offset = offsets[9];
            let type_base = self.ptr.add(offset) as *mut ScCurse;
            type_base.add(self.len).write(ScCurse);
            let offset = offsets[10];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(slots);
            let offset = offsets[11];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(dense_to_slot);
            let offset = offsets[12];
            let type_base = self.ptr.add(offset) as *mut u32;
            type_base.add(self.len).write(free_slots);
        }
        self.len += 1;
    }
    pub fn as_slices(
        &self,
    ) -> (
        &[Position],
        &[Health],
        &[ScStone],
        &[ScFreeze],
        &[ScStun],
        &[ScSleep],
        &[ScBurning],
        &[ScPoison],
        &[ScBleeding],
        &[ScCurse],
        &[u32],
        &[u32],
        &[u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[0]) as *const Position,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[1]) as *const Health,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[2]) as *const ScStone,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[3]) as *const ScFreeze,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[4]) as *const ScStun,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[5]) as *const ScSleep,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[6]) as *const ScBurning,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[7]) as *const ScPoison,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[8]) as *const ScBleeding,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[9]) as *const ScCurse,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[10]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[11]) as *const u32,
                    self.len,
                ),
                std::slice::from_raw_parts(
                    self.ptr.add(offsets[12]) as *const u32,
                    self.len,
                ),
            )
        }
    }
    pub fn as_mut_slices(
        &mut self,
    ) -> (
        &mut [Position],
        &mut [Health],
        &mut [ScStone],
        &mut [ScFreeze],
        &mut [ScStun],
        &mut [ScSleep],
        &mut [ScBurning],
        &mut [ScPoison],
        &mut [ScBleeding],
        &mut [ScCurse],
        &mut [u32],
        &mut [u32],
        &mut [u32],
    ) {
        let offsets = Self::offsets(self.cap);
        unsafe {
            (
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[0]) as *mut Position,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[1]) as *mut Health,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[2]) as *mut ScStone,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[3]) as *mut ScFreeze,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[4]) as *mut ScStun,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[5]) as *mut ScSleep,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[6]) as *mut ScBurning,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[7]) as *mut ScPoison,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[8]) as *mut ScBleeding,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[9]) as *mut ScCurse,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[10]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[11]) as *mut u32,
                    self.len,
                ),
                std::slice::from_raw_parts_mut(
                    self.ptr.add(offsets[12]) as *mut u32,
                    self.len,
                ),
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStone(&self) -> &[ScStone] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScStone,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreeze(&self) -> &[ScFreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScFreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStun(&self) -> &[ScStun] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScStun,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSleep(&self) -> &[ScSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBurning(&self) -> &[ScBurning] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBurning,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPoison(&self) -> &[ScPoison] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScPoison,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBleeding(&self) -> &[ScBleeding] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScBleeding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurse(&self) -> &[ScCurse] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const ScCurse,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 11;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots(&self) -> &[u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 12;
        unsafe {
            std::slice::from_raw_parts(
                self.ptr.add(offsets[idx]) as *const u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Position_mut(&self) -> &mut [Position] {
        let offsets = Self::offsets(self.cap);
        let idx = 0;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Position,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn Health_mut(&self) -> &mut [Health] {
        let offsets = Self::offsets(self.cap);
        let idx = 1;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut Health,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStone_mut(&self) -> &mut [ScStone] {
        let offsets = Self::offsets(self.cap);
        let idx = 2;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScStone,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScFreeze_mut(&self) -> &mut [ScFreeze] {
        let offsets = Self::offsets(self.cap);
        let idx = 3;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScFreeze,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScStun_mut(&self) -> &mut [ScStun] {
        let offsets = Self::offsets(self.cap);
        let idx = 4;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScStun,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScSleep_mut(&self) -> &mut [ScSleep] {
        let offsets = Self::offsets(self.cap);
        let idx = 5;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScSleep,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBurning_mut(&self) -> &mut [ScBurning] {
        let offsets = Self::offsets(self.cap);
        let idx = 6;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBurning,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScPoison_mut(&self) -> &mut [ScPoison] {
        let offsets = Self::offsets(self.cap);
        let idx = 7;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScPoison,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScBleeding_mut(&self) -> &mut [ScBleeding] {
        let offsets = Self::offsets(self.cap);
        let idx = 8;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScBleeding,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn ScCurse_mut(&self) -> &mut [ScCurse] {
        let offsets = Self::offsets(self.cap);
        let idx = 9;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut ScCurse,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 10;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn dense_to_slot_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 11;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
    #[allow(nonstandard_style)]
    pub fn free_slots_mut(&self) -> &mut [u32] {
        let offsets = Self::offsets(self.cap);
        let idx = 12;
        unsafe {
            std::slice::from_raw_parts_mut(
                self.ptr.add(offsets[idx]) as *mut u32,
                self.len,
            )
        }
    }
}
impl Drop for DummyComponentStorage {
    fn drop(&mut self) {
        if self.cap > 0 {
            if true {
                if !!self.ptr.is_null() {
                    ::core::panicking::panic("assertion failed: !self.ptr.is_null()")
                }
            }
            let offsets = Self::offsets(self.cap);
            unsafe {
                if std::mem::needs_drop::<Position>() {
                    let offset = offsets[0];
                    let base = self.ptr.add(offset) as *mut Position;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<Health>() {
                    let offset = offsets[1];
                    let base = self.ptr.add(offset) as *mut Health;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScStone>() {
                    let offset = offsets[2];
                    let base = self.ptr.add(offset) as *mut ScStone;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScFreeze>() {
                    let offset = offsets[3];
                    let base = self.ptr.add(offset) as *mut ScFreeze;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScStun>() {
                    let offset = offsets[4];
                    let base = self.ptr.add(offset) as *mut ScStun;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScSleep>() {
                    let offset = offsets[5];
                    let base = self.ptr.add(offset) as *mut ScSleep;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBurning>() {
                    let offset = offsets[6];
                    let base = self.ptr.add(offset) as *mut ScBurning;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScPoison>() {
                    let offset = offsets[7];
                    let base = self.ptr.add(offset) as *mut ScPoison;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScBleeding>() {
                    let offset = offsets[8];
                    let base = self.ptr.add(offset) as *mut ScBleeding;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<ScCurse>() {
                    let offset = offsets[9];
                    let base = self.ptr.add(offset) as *mut ScCurse;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[10];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[11];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                if std::mem::needs_drop::<u32>() {
                    let offset = offsets[12];
                    let base = self.ptr.add(offset) as *mut u32;
                    for i in 0..self.len {
                        std::ptr::drop_in_place(base.add(i));
                    }
                }
                std::alloc::dealloc(self.ptr, Self::layout(self.cap));
            }
        }
    }
}
#[allow(nonstandard_style)]
pub struct DummyEntityRefs {
    pub Position: *mut Position,
    pub Health: *mut Health,
    pub ScStone: *mut ScStone,
    pub ScFreeze: *mut ScFreeze,
    pub ScStun: *mut ScStun,
    pub ScSleep: *mut ScSleep,
    pub ScBurning: *mut ScBurning,
    pub ScPoison: *mut ScPoison,
    pub ScBleeding: *mut ScBleeding,
    pub ScCurse: *mut ScCurse,
}
#[automatically_derived]
#[allow(nonstandard_style)]
impl ::core::clone::Clone for DummyEntityRefs {
    #[inline]
    fn clone(&self) -> DummyEntityRefs {
        DummyEntityRefs {
            Position: ::core::clone::Clone::clone(&self.Position),
            Health: ::core::clone::Clone::clone(&self.Health),
            ScStone: ::core::clone::Clone::clone(&self.ScStone),
            ScFreeze: ::core::clone::Clone::clone(&self.ScFreeze),
            ScStun: ::core::clone::Clone::clone(&self.ScStun),
            ScSleep: ::core::clone::Clone::clone(&self.ScSleep),
            ScBurning: ::core::clone::Clone::clone(&self.ScBurning),
            ScPoison: ::core::clone::Clone::clone(&self.ScPoison),
            ScBleeding: ::core::clone::Clone::clone(&self.ScBleeding),
            ScCurse: ::core::clone::Clone::clone(&self.ScCurse),
        }
    }
}
impl AsArchEntityEnumRef for DummyEntityRefs {
    fn as_arch_ref(&self) -> ArchEntityRefs {
        ArchEntityRefs::Dummy(self.clone())
    }
}
#[allow(nonstandard_style)]
pub struct Dummy {
    /// Component storage (including slots).
    pub storage: std::cell::UnsafeCell<DummyComponentStorage>,
    /// Number of valid items in the `free_slots` component array (in storage).
    pub free_slots_len: usize,
    /// Generation counter per slot (u16). Only used in debug builds.
    pub slot_generations: Vec<u16>,
}
#[allow(nonstandard_style)]
impl Dummy {
    pub fn new() -> Self {
        Self {
            storage: std::cell::UnsafeCell::new(DummyComponentStorage::new()),
            free_slots_len: 0,
            slot_generations: Vec::new(),
        }
    }
    /// Spawn an entity into this archetype.
    /// Returns the handle (slot_index, generation).
    /// Since you know which arch it comes from, you can access it with zero arch lookup overhead.
    #[allow(nonstandard_style)]
    pub fn spawn(
        &mut self,
        Position: Position,
        Health: Health,
        ScStone: ScStone,
        ScFreeze: ScFreeze,
        ScStun: ScStun,
        ScSleep: ScSleep,
        ScBurning: ScBurning,
        ScPoison: ScPoison,
        ScBleeding: ScBleeding,
        ScCurse: ScCurse,
    ) -> ::ecs::Handle {
        let storage = unsafe { &mut *self.storage.get() };
        let slot_index: u32 = if self.free_slots_len > 0 {
            self.free_slots_len -= 1;
            unsafe { *storage.free_slots().get_unchecked(self.free_slots_len) }
        } else {
            storage.len() as u32
        };
        {
            if (slot_index as usize) >= self.slot_generations.len() {
                self.slot_generations.push(0u16);
            }
        }
        let dense_index = storage.len() as u32;
        storage
            .push(
                Position,
                Health,
                ScStone,
                ScFreeze,
                ScStun,
                ScSleep,
                ScBurning,
                ScPoison,
                ScBleeding,
                ScCurse,
                dense_index,
                slot_index,
                0,
            );
        let generation = self.slot_generations[slot_index as usize];
        let arch_id = ArchId::Dummy.as_u8();
        ::ecs::Handle {
            arch_id,
            slot_index,
            generation,
        }
    }
    /// Despawn an entity given its handle. Note: since this is architecture-specific despawn,
    /// i.e. in release mode arch_id is actually not used in any way.
    pub fn despawn(&mut self, handle: ::ecs::Handle) {
        let storage = unsafe { &mut *self.storage.get() };
        if true {
            match (&handle.arch_id, &ArchId::Dummy.as_u8()) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let slot_index_usize = handle.slot_index as usize;
        let dense_index = unsafe { *storage.slots().get_unchecked(slot_index_usize) };
        let dense_index_usize = dense_index as usize;
        if true {
            match (&dense_index, &u32::MAX) {
                (left_val, right_val) => {
                    if *left_val == *right_val {
                        let kind = ::core::panicking::AssertKind::Ne;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        if true {
            if !(dense_index_usize < storage.len()) {
                ::core::panicking::panic(
                    "assertion failed: dense_index_usize < storage.len()",
                )
            }
        }
        let last_dense_index_usize = storage.len() - 1;
        storage.swap_remove(dense_index_usize);
        let was_last = dense_index_usize == last_dense_index_usize;
        if !was_last {
            let moved_slot_index = unsafe {
                *storage.dense_to_slot().get_unchecked(dense_index_usize)
            };
            unsafe {
                *storage.slots_mut().get_unchecked_mut(moved_slot_index as usize) = dense_index;
            }
        }
        unsafe {
            let free_slots_ptr = storage.free_slots_mut().as_mut_ptr();
            *free_slots_ptr.add(self.free_slots_len) = handle.slot_index;
        }
        self.free_slots_len += 1;
        {
            let generation = &mut self.slot_generations[slot_index_usize];
            if true {
                match (&handle.generation, &*generation) {
                    (left_val, right_val) => {
                        if !(*left_val == *right_val) {
                            let kind = ::core::panicking::AssertKind::Eq;
                            ::core::panicking::assert_failed(
                                kind,
                                &*left_val,
                                &*right_val,
                                ::core::option::Option::None,
                            );
                        }
                    }
                };
            }
            *generation = generation.wrapping_add(1);
        }
        unsafe {
            let slots_ptr = storage.slots_mut().as_mut_ptr();
            *slots_ptr.add(slot_index_usize) = u32::MAX;
        }
    }
    pub fn dense_len(&self) -> usize {
        unsafe { (*self.storage.get()).len() }
    }
    pub fn clear_preserve_capacity(&mut self) {
        let storage = unsafe { &mut *self.storage.get() };
        for generation in &mut self.slot_generations {
            *generation = generation.wrapping_add(1);
        }
        self.free_slots_len = 0;
        storage.clear();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> DummyEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        if true {
            match (&arch_enum, &ArchId::Dummy) {
                (left_val, right_val) => {
                    if !(*left_val == *right_val) {
                        let kind = ::core::panicking::AssertKind::Eq;
                        ::core::panicking::assert_failed(
                            kind,
                            &*left_val,
                            &*right_val,
                            ::core::option::Option::None,
                        );
                    }
                }
            };
        }
        let storage = unsafe { &mut *self.storage.get() };
        let slot_usize = handle.slot_index as usize;
        let dense = unsafe { *storage.slots().get_unchecked(slot_usize) };
        let dense_usize = dense as usize;
        if dense == u32::MAX || dense_usize >= storage.len() {
            ::core::panicking::panic("explicit panic");
        }
        let entity_refs = DummyEntityRefs {
            Position: unsafe { storage.Position_mut().get_unchecked_mut(dense_usize) },
            Health: unsafe { storage.Health_mut().get_unchecked_mut(dense_usize) },
            ScStone: unsafe { storage.ScStone_mut().get_unchecked_mut(dense_usize) },
            ScFreeze: unsafe { storage.ScFreeze_mut().get_unchecked_mut(dense_usize) },
            ScStun: unsafe { storage.ScStun_mut().get_unchecked_mut(dense_usize) },
            ScSleep: unsafe { storage.ScSleep_mut().get_unchecked_mut(dense_usize) },
            ScBurning: unsafe { storage.ScBurning_mut().get_unchecked_mut(dense_usize) },
            ScPoison: unsafe { storage.ScPoison_mut().get_unchecked_mut(dense_usize) },
            ScBleeding: unsafe {
                storage.ScBleeding_mut().get_unchecked_mut(dense_usize)
            },
            ScCurse: unsafe { storage.ScCurse_mut().get_unchecked_mut(dense_usize) },
        };
        entity_refs
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Position(&self) -> &[Position] {
        unsafe { (*self.storage.get()).Position() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn Health(&self) -> &[Health] {
        unsafe { (*self.storage.get()).Health() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScStone(&self) -> &[ScStone] {
        unsafe { (*self.storage.get()).ScStone() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScFreeze(&self) -> &[ScFreeze] {
        unsafe { (*self.storage.get()).ScFreeze() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScStun(&self) -> &[ScStun] {
        unsafe { (*self.storage.get()).ScStun() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScSleep(&self) -> &[ScSleep] {
        unsafe { (*self.storage.get()).ScSleep() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBurning(&self) -> &[ScBurning] {
        unsafe { (*self.storage.get()).ScBurning() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScPoison(&self) -> &[ScPoison] {
        unsafe { (*self.storage.get()).ScPoison() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScBleeding(&self) -> &[ScBleeding] {
        unsafe { (*self.storage.get()).ScBleeding() }
    }
    #[inline]
    #[allow(nonstandard_style)]
    pub fn ScCurse(&self) -> &[ScCurse] {
        unsafe { (*self.storage.get()).ScCurse() }
    }
}
/// The world contains one archetype struct per declared archetype.
pub struct StressWorld {
    pub Player: Player,
    pub MonsterNormal: MonsterNormal,
    pub MonsterBoss: MonsterBoss,
    pub Homunculus: Homunculus,
    pub Mercenary: Mercenary,
    pub Pet: Pet,
    pub Elemental: Elemental,
    pub WugRider: WugRider,
    pub MadogearUser: MadogearUser,
    pub GeneticCart: GeneticCart,
    pub RuneKnight: RuneKnight,
    pub RoyalGuard: RoyalGuard,
    pub Mechanic: Mechanic,
    pub GuillotineCross: GuillotineCross,
    pub ShadowChaser: ShadowChaser,
    pub Sorcerer: Sorcerer,
    pub Warlock: Warlock,
    pub Archbishop: Archbishop,
    pub Sura: Sura,
    pub Ranger: Ranger,
    pub MinstrelWanderer: MinstrelWanderer,
    pub SuperNovice: SuperNovice,
    pub Gunslinger: Gunslinger,
    pub Ninja: Ninja,
    pub KagerouOboro: KagerouOboro,
    pub Rebellion: Rebellion,
    pub Summoner: Summoner,
    pub StarEmperor: StarEmperor,
    pub SoulReaper: SoulReaper,
    pub Dummy: Dummy,
}
impl StressWorld {
    pub fn new() -> Self {
        Self {
            Player: Player::new(),
            MonsterNormal: MonsterNormal::new(),
            MonsterBoss: MonsterBoss::new(),
            Homunculus: Homunculus::new(),
            Mercenary: Mercenary::new(),
            Pet: Pet::new(),
            Elemental: Elemental::new(),
            WugRider: WugRider::new(),
            MadogearUser: MadogearUser::new(),
            GeneticCart: GeneticCart::new(),
            RuneKnight: RuneKnight::new(),
            RoyalGuard: RoyalGuard::new(),
            Mechanic: Mechanic::new(),
            GuillotineCross: GuillotineCross::new(),
            ShadowChaser: ShadowChaser::new(),
            Sorcerer: Sorcerer::new(),
            Warlock: Warlock::new(),
            Archbishop: Archbishop::new(),
            Sura: Sura::new(),
            Ranger: Ranger::new(),
            MinstrelWanderer: MinstrelWanderer::new(),
            SuperNovice: SuperNovice::new(),
            Gunslinger: Gunslinger::new(),
            Ninja: Ninja::new(),
            KagerouOboro: KagerouOboro::new(),
            Rebellion: Rebellion::new(),
            Summoner: Summoner::new(),
            StarEmperor: StarEmperor::new(),
            SoulReaper: SoulReaper::new(),
            Dummy: Dummy::new(),
        }
    }
    pub fn clear_preserve_capacity(&mut self) {
        self.Player.clear_preserve_capacity();
        self.MonsterNormal.clear_preserve_capacity();
        self.MonsterBoss.clear_preserve_capacity();
        self.Homunculus.clear_preserve_capacity();
        self.Mercenary.clear_preserve_capacity();
        self.Pet.clear_preserve_capacity();
        self.Elemental.clear_preserve_capacity();
        self.WugRider.clear_preserve_capacity();
        self.MadogearUser.clear_preserve_capacity();
        self.GeneticCart.clear_preserve_capacity();
        self.RuneKnight.clear_preserve_capacity();
        self.RoyalGuard.clear_preserve_capacity();
        self.Mechanic.clear_preserve_capacity();
        self.GuillotineCross.clear_preserve_capacity();
        self.ShadowChaser.clear_preserve_capacity();
        self.Sorcerer.clear_preserve_capacity();
        self.Warlock.clear_preserve_capacity();
        self.Archbishop.clear_preserve_capacity();
        self.Sura.clear_preserve_capacity();
        self.Ranger.clear_preserve_capacity();
        self.MinstrelWanderer.clear_preserve_capacity();
        self.SuperNovice.clear_preserve_capacity();
        self.Gunslinger.clear_preserve_capacity();
        self.Ninja.clear_preserve_capacity();
        self.KagerouOboro.clear_preserve_capacity();
        self.Rebellion.clear_preserve_capacity();
        self.Summoner.clear_preserve_capacity();
        self.StarEmperor.clear_preserve_capacity();
        self.SoulReaper.clear_preserve_capacity();
        self.Dummy.clear_preserve_capacity();
    }
    /// Returns mutable pointers to all entity components.
    pub unsafe fn get_entity_mut(&self, handle: ::ecs::Handle) -> ArchEntityRefs {
        let arch_enum = unsafe { std::mem::transmute::<u8, ArchId>(handle.arch_id) };
        match arch_enum {
            ArchId::Player => {
                let arch = &self.Player;
                ArchEntityRefs::Player(arch.get_entity_mut(handle))
            }
            ArchId::MonsterNormal => {
                let arch = &self.MonsterNormal;
                ArchEntityRefs::MonsterNormal(arch.get_entity_mut(handle))
            }
            ArchId::MonsterBoss => {
                let arch = &self.MonsterBoss;
                ArchEntityRefs::MonsterBoss(arch.get_entity_mut(handle))
            }
            ArchId::Homunculus => {
                let arch = &self.Homunculus;
                ArchEntityRefs::Homunculus(arch.get_entity_mut(handle))
            }
            ArchId::Mercenary => {
                let arch = &self.Mercenary;
                ArchEntityRefs::Mercenary(arch.get_entity_mut(handle))
            }
            ArchId::Pet => {
                let arch = &self.Pet;
                ArchEntityRefs::Pet(arch.get_entity_mut(handle))
            }
            ArchId::Elemental => {
                let arch = &self.Elemental;
                ArchEntityRefs::Elemental(arch.get_entity_mut(handle))
            }
            ArchId::WugRider => {
                let arch = &self.WugRider;
                ArchEntityRefs::WugRider(arch.get_entity_mut(handle))
            }
            ArchId::MadogearUser => {
                let arch = &self.MadogearUser;
                ArchEntityRefs::MadogearUser(arch.get_entity_mut(handle))
            }
            ArchId::GeneticCart => {
                let arch = &self.GeneticCart;
                ArchEntityRefs::GeneticCart(arch.get_entity_mut(handle))
            }
            ArchId::RuneKnight => {
                let arch = &self.RuneKnight;
                ArchEntityRefs::RuneKnight(arch.get_entity_mut(handle))
            }
            ArchId::RoyalGuard => {
                let arch = &self.RoyalGuard;
                ArchEntityRefs::RoyalGuard(arch.get_entity_mut(handle))
            }
            ArchId::Mechanic => {
                let arch = &self.Mechanic;
                ArchEntityRefs::Mechanic(arch.get_entity_mut(handle))
            }
            ArchId::GuillotineCross => {
                let arch = &self.GuillotineCross;
                ArchEntityRefs::GuillotineCross(arch.get_entity_mut(handle))
            }
            ArchId::ShadowChaser => {
                let arch = &self.ShadowChaser;
                ArchEntityRefs::ShadowChaser(arch.get_entity_mut(handle))
            }
            ArchId::Sorcerer => {
                let arch = &self.Sorcerer;
                ArchEntityRefs::Sorcerer(arch.get_entity_mut(handle))
            }
            ArchId::Warlock => {
                let arch = &self.Warlock;
                ArchEntityRefs::Warlock(arch.get_entity_mut(handle))
            }
            ArchId::Archbishop => {
                let arch = &self.Archbishop;
                ArchEntityRefs::Archbishop(arch.get_entity_mut(handle))
            }
            ArchId::Sura => {
                let arch = &self.Sura;
                ArchEntityRefs::Sura(arch.get_entity_mut(handle))
            }
            ArchId::Ranger => {
                let arch = &self.Ranger;
                ArchEntityRefs::Ranger(arch.get_entity_mut(handle))
            }
            ArchId::MinstrelWanderer => {
                let arch = &self.MinstrelWanderer;
                ArchEntityRefs::MinstrelWanderer(arch.get_entity_mut(handle))
            }
            ArchId::SuperNovice => {
                let arch = &self.SuperNovice;
                ArchEntityRefs::SuperNovice(arch.get_entity_mut(handle))
            }
            ArchId::Gunslinger => {
                let arch = &self.Gunslinger;
                ArchEntityRefs::Gunslinger(arch.get_entity_mut(handle))
            }
            ArchId::Ninja => {
                let arch = &self.Ninja;
                ArchEntityRefs::Ninja(arch.get_entity_mut(handle))
            }
            ArchId::KagerouOboro => {
                let arch = &self.KagerouOboro;
                ArchEntityRefs::KagerouOboro(arch.get_entity_mut(handle))
            }
            ArchId::Rebellion => {
                let arch = &self.Rebellion;
                ArchEntityRefs::Rebellion(arch.get_entity_mut(handle))
            }
            ArchId::Summoner => {
                let arch = &self.Summoner;
                ArchEntityRefs::Summoner(arch.get_entity_mut(handle))
            }
            ArchId::StarEmperor => {
                let arch = &self.StarEmperor;
                ArchEntityRefs::StarEmperor(arch.get_entity_mut(handle))
            }
            ArchId::SoulReaper => {
                let arch = &self.SoulReaper;
                ArchEntityRefs::SoulReaper(arch.get_entity_mut(handle))
            }
            ArchId::Dummy => {
                let arch = &self.Dummy;
                ArchEntityRefs::Dummy(arch.get_entity_mut(handle))
            }
        }
    }
}
extern crate test;
#[rustc_test_marker = "stress_compilation_time"]
#[doc(hidden)]
pub const stress_compilation_time: test::TestDescAndFn = test::TestDescAndFn {
    desc: test::TestDesc {
        name: test::StaticTestName("stress_compilation_time"),
        ignore: false,
        ignore_message: ::core::option::Option::None,
        source_file: "tests\\bloated.rs",
        start_line: 152usize,
        start_col: 4usize,
        end_line: 152usize,
        end_col: 27usize,
        compile_fail: false,
        no_run: false,
        should_panic: test::ShouldPanic::No,
        test_type: test::TestType::IntegrationTest,
    },
    testfn: test::StaticTestFn(
        #[coverage(off)]
        || test::assert_test_result(stress_compilation_time()),
    ),
};
fn stress_compilation_time() {
    let mut world = StressWorld::new();
    world
        .Player
        .spawn(
            Position::default(),
            Velocity::default(),
            Health::default(),
            ScBerserk,
            ScQuicken,
            ScAngelus,
            ScAdrenaline,
            ScInspiration,
        );
    world
        .MonsterBoss
        .spawn(
            Position::default(),
            Velocity::default(),
            Health::default(),
            ScStone,
            ScFreeze,
            ScStun,
            ScSleep,
            ScDeepSleep,
        );
    world
        .MadogearUser
        .spawn(
            Position::default(),
            Velocity::default(),
            ScMadogear,
            ScHeatBarrel,
            ScMagicalBullet,
        );
    world
        .Gunslinger
        .spawn(
            Position::default(),
            Velocity::default(),
            ScMadnessCanceler,
            ScLastStand,
            ScGatlingFever,
        );
    world
        .KagerouOboro
        .spawn(
            Position::default(),
            Velocity::default(),
            ScKunaiExplosion,
            ScSwirlingPetal,
            ScCrossSlash,
        );
    {
        let processor = |p: &mut Position, v: &mut Velocity| {
            p.x += v.x;
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Player.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Player.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.MonsterNormal.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.MonsterNormal.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.MonsterBoss.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.MonsterBoss.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Homunculus.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Homunculus.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Mercenary.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Mercenary.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Pet.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe { &mut *world.Pet.storage.get() };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Elemental.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Elemental.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.WugRider.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.WugRider.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.MadogearUser.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.MadogearUser.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.GeneticCart.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.GeneticCart.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.RuneKnight.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.RuneKnight.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.RoyalGuard.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.RoyalGuard.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Mechanic.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Mechanic.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.GuillotineCross.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.GuillotineCross.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.ShadowChaser.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.ShadowChaser.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Sorcerer.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Sorcerer.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Warlock.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Warlock.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Archbishop.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Archbishop.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Sura.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Sura.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Ranger.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Ranger.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.MinstrelWanderer.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.MinstrelWanderer.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.SuperNovice.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.SuperNovice.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Gunslinger.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Gunslinger.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Ninja.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Ninja.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.KagerouOboro.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.KagerouOboro.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Rebellion.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Rebellion.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.Summoner.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.Summoner.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.StarEmperor.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.StarEmperor.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        {
            {
                {
                    {
                        {
                            {
                                let len = world.SoulReaper.dense_len();
                                if len > 0 {
                                    let arch_mut_ref = unsafe {
                                        &mut *world.SoulReaper.storage.get()
                                    };
                                    let p = unsafe { arch_mut_ref.Position_mut() };
                                    let v = unsafe { arch_mut_ref.Velocity_mut() };
                                    for i in 0..len {
                                        let p = if true {
                                            &mut p[i]
                                        } else {
                                            unsafe { p.get_unchecked_mut(i) }
                                        };
                                        let v = if true {
                                            &mut v[i]
                                        } else {
                                            unsafe { v.get_unchecked_mut(i) }
                                        };
                                        processor(p, v);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |p: &mut Position, h: &mut Health, b: &mut ScBerserk| {
            p.y += 1.0;
        };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        let len = world.Player.dense_len();
                                                        if len > 0 {
                                                            let arch_mut_ref = unsafe {
                                                                &mut *world.Player.storage.get()
                                                            };
                                                            let p = unsafe { arch_mut_ref.Position_mut() };
                                                            let h = unsafe { arch_mut_ref.Health_mut() };
                                                            let b = unsafe { arch_mut_ref.ScBerserk_mut() };
                                                            for i in 0..len {
                                                                let p = if true {
                                                                    &mut p[i]
                                                                } else {
                                                                    unsafe { p.get_unchecked_mut(i) }
                                                                };
                                                                let h = if true {
                                                                    &mut h[i]
                                                                } else {
                                                                    unsafe { h.get_unchecked_mut(i) }
                                                                };
                                                                let b = if true {
                                                                    &mut b[i]
                                                                } else {
                                                                    unsafe { b.get_unchecked_mut(i) }
                                                                };
                                                                processor(p, h, b);
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { {} } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { {} } } } } } } } };
        { { { { { { { { {} } } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { {} } } } } } } } };
        { { { { { { { { {} } } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { {} } } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { {} } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            b: &mut ScBerserk,
            q: &mut ScQuicken,
            a: &mut ScAngelus|
        {
            p.x += v.x * 0.1;
        };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    {
                                                                        {
                                                                            {
                                                                                {
                                                                                    {
                                                                                        {
                                                                                            {
                                                                                                {
                                                                                                    {
                                                                                                        {
                                                                                                            {
                                                                                                                {
                                                                                                                    {
                                                                                                                        let len = world.Player.dense_len();
                                                                                                                        if len > 0 {
                                                                                                                            let arch_mut_ref = unsafe {
                                                                                                                                &mut *world.Player.storage.get()
                                                                                                                            };
                                                                                                                            let p = unsafe { arch_mut_ref.Position_mut() };
                                                                                                                            let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                                                                            let h = unsafe { arch_mut_ref.Health_mut() };
                                                                                                                            let b = unsafe { arch_mut_ref.ScBerserk_mut() };
                                                                                                                            let q = unsafe { arch_mut_ref.ScQuicken_mut() };
                                                                                                                            let a = unsafe { arch_mut_ref.ScAngelus_mut() };
                                                                                                                            for i in 0..len {
                                                                                                                                let p = if true {
                                                                                                                                    &mut p[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { p.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let v = if true {
                                                                                                                                    &mut v[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { v.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let h = if true {
                                                                                                                                    &mut h[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { h.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let b = if true {
                                                                                                                                    &mut b[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { b.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let q = if true {
                                                                                                                                    &mut q[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { q.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let a = if true {
                                                                                                                                    &mut a[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { a.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                processor(p, v, h, b, q, a);
                                                                                                                            }
                                                                                                                        }
                                                                                                                    }
                                                                                                                }
                                                                                                            }
                                                                                                        }
                                                                                                    }
                                                                                                }
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            m: &mut ScMadogear,
            hb: &mut ScHeatBarrel,
            mb: &mut ScMagicalBullet|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    {
                                                                        {
                                                                            {
                                                                                {
                                                                                    {
                                                                                        {
                                                                                            let len = world.MadogearUser.dense_len();
                                                                                            if len > 0 {
                                                                                                let arch_mut_ref = unsafe {
                                                                                                    &mut *world.MadogearUser.storage.get()
                                                                                                };
                                                                                                let p = unsafe { arch_mut_ref.Position_mut() };
                                                                                                let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                                                let m = unsafe { arch_mut_ref.ScMadogear_mut() };
                                                                                                let hb = unsafe { arch_mut_ref.ScHeatBarrel_mut() };
                                                                                                let mb = unsafe { arch_mut_ref.ScMagicalBullet_mut() };
                                                                                                for i in 0..len {
                                                                                                    let p = if true {
                                                                                                        &mut p[i]
                                                                                                    } else {
                                                                                                        unsafe { p.get_unchecked_mut(i) }
                                                                                                    };
                                                                                                    let v = if true {
                                                                                                        &mut v[i]
                                                                                                    } else {
                                                                                                        unsafe { v.get_unchecked_mut(i) }
                                                                                                    };
                                                                                                    let m = if true {
                                                                                                        &mut m[i]
                                                                                                    } else {
                                                                                                        unsafe { m.get_unchecked_mut(i) }
                                                                                                    };
                                                                                                    let hb = if true {
                                                                                                        &mut hb[i]
                                                                                                    } else {
                                                                                                        unsafe { hb.get_unchecked_mut(i) }
                                                                                                    };
                                                                                                    let mb = if true {
                                                                                                        &mut mb[i]
                                                                                                    } else {
                                                                                                        unsafe { mb.get_unchecked_mut(i) }
                                                                                                    };
                                                                                                    processor(p, v, m, hb, mb);
                                                                                                }
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            s: &mut ScStone,
            f: &mut ScFreeze,
            st: &mut ScStun|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    {
                                                                        {
                                                                            {
                                                                                {
                                                                                    {
                                                                                        {
                                                                                            {
                                                                                                {
                                                                                                    {
                                                                                                        {
                                                                                                            {
                                                                                                                {
                                                                                                                    {
                                                                                                                        let len = world.MonsterBoss.dense_len();
                                                                                                                        if len > 0 {
                                                                                                                            let arch_mut_ref = unsafe {
                                                                                                                                &mut *world.MonsterBoss.storage.get()
                                                                                                                            };
                                                                                                                            let p = unsafe { arch_mut_ref.Position_mut() };
                                                                                                                            let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                                                                            let h = unsafe { arch_mut_ref.Health_mut() };
                                                                                                                            let s = unsafe { arch_mut_ref.ScStone_mut() };
                                                                                                                            let f = unsafe { arch_mut_ref.ScFreeze_mut() };
                                                                                                                            let st = unsafe { arch_mut_ref.ScStun_mut() };
                                                                                                                            for i in 0..len {
                                                                                                                                let p = if true {
                                                                                                                                    &mut p[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { p.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let v = if true {
                                                                                                                                    &mut v[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { v.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let h = if true {
                                                                                                                                    &mut h[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { h.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let s = if true {
                                                                                                                                    &mut s[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { s.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let f = if true {
                                                                                                                                    &mut f[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { f.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let st = if true {
                                                                                                                                    &mut st[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { st.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                processor(p, v, h, s, f, st);
                                                                                                                            }
                                                                                                                        }
                                                                                                                    }
                                                                                                                }
                                                                                                            }
                                                                                                        }
                                                                                                    }
                                                                                                }
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            c: &mut ScCloakingExceed,
            r: &mut ScRollingCutter,
            ci: &mut ScCrossImpact,
            dc: &mut ScDarkClaw|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    {
                                                                        {
                                                                            {
                                                                                {
                                                                                    {
                                                                                        {
                                                                                            {
                                                                                                {
                                                                                                    {
                                                                                                        {
                                                                                                            {
                                                                                                                {
                                                                                                                    {
                                                                                                                        let len = world.GuillotineCross.dense_len();
                                                                                                                        if len > 0 {
                                                                                                                            let arch_mut_ref = unsafe {
                                                                                                                                &mut *world.GuillotineCross.storage.get()
                                                                                                                            };
                                                                                                                            let p = unsafe { arch_mut_ref.Position_mut() };
                                                                                                                            let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                                                                            let c = unsafe { arch_mut_ref.ScCloakingExceed_mut() };
                                                                                                                            let r = unsafe { arch_mut_ref.ScRollingCutter_mut() };
                                                                                                                            let ci = unsafe { arch_mut_ref.ScCrossImpact_mut() };
                                                                                                                            let dc = unsafe { arch_mut_ref.ScDarkClaw_mut() };
                                                                                                                            for i in 0..len {
                                                                                                                                let p = if true {
                                                                                                                                    &mut p[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { p.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let v = if true {
                                                                                                                                    &mut v[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { v.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let c = if true {
                                                                                                                                    &mut c[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { c.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let r = if true {
                                                                                                                                    &mut r[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { r.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let ci = if true {
                                                                                                                                    &mut ci[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { ci.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                let dc = if true {
                                                                                                                                    &mut dc[i]
                                                                                                                                } else {
                                                                                                                                    unsafe { dc.get_unchecked_mut(i) }
                                                                                                                                };
                                                                                                                                processor(p, v, c, r, ci, dc);
                                                                                                                            }
                                                                                                                        }
                                                                                                                    }
                                                                                                                }
                                                                                                            }
                                                                                                        }
                                                                                                    }
                                                                                                }
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            sp: &mut ScSpellBreaker,
            sl: &mut ScSoulLink,
            pr: &mut ScPreserve|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            fs: &mut ScFoodStr,
            fa: &mut ScFoodAgi,
            fv: &mut ScFoodVit,
            fi: &mut ScFoodInt|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            cart: &mut ScCartBoost,
            wind: &mut ScWindWalk,
            agi: &mut ScIncreaseAgi|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            end: &mut ScEndure,
            ag: &mut ScAutoguard,
            rs: &mut ScReflectShield,
            pr: &mut ScPrestige|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            strip: &mut ScStripAccessory,
            inv: &mut ScInvisibility|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            song: &mut ScSongOfLutie,
            drum: &mut ScDrumOnTheBattlefield|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            cat: &mut ScCatnipMeteor,
            peck: &mut ScPickyPeck|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    let len = world.Summoner.dense_len();
                                                                    if len > 0 {
                                                                        let arch_mut_ref = unsafe {
                                                                            &mut *world.Summoner.storage.get()
                                                                        };
                                                                        let p = unsafe { arch_mut_ref.Position_mut() };
                                                                        let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                        let cat = unsafe { arch_mut_ref.ScCatnipMeteor_mut() };
                                                                        let peck = unsafe { arch_mut_ref.ScPickyPeck_mut() };
                                                                        for i in 0..len {
                                                                            let p = if true {
                                                                                &mut p[i]
                                                                            } else {
                                                                                unsafe { p.get_unchecked_mut(i) }
                                                                            };
                                                                            let v = if true {
                                                                                &mut v[i]
                                                                            } else {
                                                                                unsafe { v.get_unchecked_mut(i) }
                                                                            };
                                                                            let cat = if true {
                                                                                &mut cat[i]
                                                                            } else {
                                                                                unsafe { cat.get_unchecked_mut(i) }
                                                                            };
                                                                            let peck = if true {
                                                                                &mut peck[i]
                                                                            } else {
                                                                                unsafe { peck.get_unchecked_mut(i) }
                                                                            };
                                                                            processor(p, v, cat, peck);
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            sb: &mut ScSolarBurst,
            fmk: &mut ScFullMoonKick|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            reap: &mut ScSoulReap,
            curse: &mut ScCurseOfSoul|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    let len = world.SoulReaper.dense_len();
                                                                    if len > 0 {
                                                                        let arch_mut_ref = unsafe {
                                                                            &mut *world.SoulReaper.storage.get()
                                                                        };
                                                                        let p = unsafe { arch_mut_ref.Position_mut() };
                                                                        let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                        let reap = unsafe { arch_mut_ref.ScSoulReap_mut() };
                                                                        let curse = unsafe { arch_mut_ref.ScCurseOfSoul_mut() };
                                                                        for i in 0..len {
                                                                            let p = if true {
                                                                                &mut p[i]
                                                                            } else {
                                                                                unsafe { p.get_unchecked_mut(i) }
                                                                            };
                                                                            let v = if true {
                                                                                &mut v[i]
                                                                            } else {
                                                                                unsafe { v.get_unchecked_mut(i) }
                                                                            };
                                                                            let reap = if true {
                                                                                &mut reap[i]
                                                                            } else {
                                                                                unsafe { reap.get_unchecked_mut(i) }
                                                                            };
                                                                            let curse = if true {
                                                                                &mut curse[i]
                                                                            } else {
                                                                                unsafe { curse.get_unchecked_mut(i) }
                                                                            };
                                                                            processor(p, v, reap, curse);
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            cpw: &mut ScCpWeapon,
            cpa: &mut ScCpArmor,
            cps: &mut ScCpShield,
            cph: &mut ScCpHelm|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            paint: &mut ScPalletPaint,
            pyro: &mut ScPyrotechnic|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            thorn: &mut ScThornTrap,
            spore: &mut ScSporeExplosion|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            ma: &mut ScManuAtk,
            md: &mut ScManuDef|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            sa: &mut ScSplAtk,
            sd: &mut ScSplDef|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            hb: &mut ScHeatBarrel,
            amb: &mut ScAntiMaterialBlast|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    let len = world.Rebellion.dense_len();
                                                                    if len > 0 {
                                                                        let arch_mut_ref = unsafe {
                                                                            &mut *world.Rebellion.storage.get()
                                                                        };
                                                                        let p = unsafe { arch_mut_ref.Position_mut() };
                                                                        let v = unsafe { arch_mut_ref.Velocity_mut() };
                                                                        let hb = unsafe { arch_mut_ref.ScHeatBarrel_mut() };
                                                                        let amb = unsafe { arch_mut_ref.ScAntiMaterialBlast_mut() };
                                                                        for i in 0..len {
                                                                            let p = if true {
                                                                                &mut p[i]
                                                                            } else {
                                                                                unsafe { p.get_unchecked_mut(i) }
                                                                            };
                                                                            let v = if true {
                                                                                &mut v[i]
                                                                            } else {
                                                                                unsafe { v.get_unchecked_mut(i) }
                                                                            };
                                                                            let hb = if true {
                                                                                &mut hb[i]
                                                                            } else {
                                                                                unsafe { hb.get_unchecked_mut(i) }
                                                                            };
                                                                            let amb = if true {
                                                                                &mut amb[i]
                                                                            } else {
                                                                                unsafe { amb.get_unchecked_mut(i) }
                                                                            };
                                                                            processor(p, v, hb, amb);
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            edp: &mut ScEdp,
            maxp: &mut ScMaximizePower|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            rb: &mut ScReboundShield,
            hb2: &mut ScHeatBarrel|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            h: &mut Health,
            tarot: &mut ScTarotCard,
            ref_: &mut ScRefresh,
            lux: &mut ScLuxAnima,
            her: &mut ScHermode|
        {};
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } };
        { { { { { { { { { { { { { { { { { {} } } } } } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { { {} } } } } } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
    {
        let processor = |
            p: &mut Position,
            v: &mut Velocity,
            stone: &mut ScStone,
            f: &mut ScFreeze,
            s: &mut ScStun,
            sleep: &mut ScSleep,
            burn: &mut ScBurning,
            poi: &mut ScPoison|
        {};
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        {
            {
                {
                    {
                        {
                            {
                                {
                                    {
                                        {
                                            {
                                                {
                                                    {
                                                        {
                                                            {
                                                                {
                                                                    {
                                                                        {
                                                                            {
                                                                                {
                                                                                    {
                                                                                        {
                                                                                            {
                                                                                                {
                                                                                                    {
                                                                                                        {
                                                                                                            { { { { { { { { { { { { { { {} } } } } } } } } } } } } } }
                                                                                                        }
                                                                                                    }
                                                                                                }
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { {} } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { {} } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { { {} } } } } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { {} } } } } } } } } } };
        { { { { { { { { { { { { {} } } } } } } } } } } } };
    };
}
#[rustc_main]
#[coverage(off)]
#[doc(hidden)]
pub fn main() -> () {
    extern crate test;
    test::test_main_static(&[&stress_compilation_time])
}
